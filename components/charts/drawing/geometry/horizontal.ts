/**
 * Horizontal line geometry — a constant price across all time.
 *
 * Session 4B-1 (F9): hoisted into `@trading-alerts/types`. This file is a
 * thin re-export so every existing `../geometry/horizontal` import keeps
 * working unchanged. Never fork this math back into this tree — edit
 * `packages/types/src/geometry/horizontal.ts` instead.
 *
 * @module components/charts/drawing/geometry/horizontal
 */

export { horizontalValue } from '@trading-alerts/types/geometry';
