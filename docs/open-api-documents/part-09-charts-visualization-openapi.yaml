openapi: 3.0.3
info:
  title: Trading Alerts SaaS - Charts & Visualization Components (Part 09)
  version: 3.0.0
  description: |
    Part 09: Charts & Visualization Components

    **Note:** This part consists entirely of UI components and has no backend API endpoints.
    Chart data is fetched from `/api/indicators/{symbol}/{timeframe}` which proxies to the Flask MT5 API (Part 06).

    ## Components Included (10 total):

    ### Page Components (3 files):
    1. **Charts Listing Page** (`app/(dashboard)/charts/page.tsx`)
       - Symbol/timeframe grid selectors
       - Tier-based filtering (FREE: 5 symbols × 3 timeframes = 15 combinations; PRO: 15 symbols × 9 timeframes = 135 combinations)
       - Tier statistics dashboard showing available symbols, timeframes, and chart combinations
       - Quick access links to popular charts (XAUUSD/H1, BTCUSD/H4, EURUSD/D1, US30/H1)
       - Upgrade prompts for FREE users
       - Server-side rendered with session-based tier validation

    2. **Individual Chart Page** (`app/(dashboard)/charts/[symbol]/[timeframe]/page.tsx`)
       - Dynamic routing for symbol/timeframe combinations (e.g., `/charts/XAUUSD/H1`)
       - Server-side tier access validation with 404 for invalid combinations
       - Access denied UI with upgrade prompt for locked charts
       - Renders ChartControls and TradingChartClient components
       - Pre-generates static pages for FREE tier combinations (15 pages)

    3. **Trading Chart Client Wrapper** (`app/(dashboard)/charts/[symbol]/[timeframe]/trading-chart-client.tsx`)
       - Client component wrapper for dynamic import of TradingChart
       - Reduces initial bundle size (~200KB for lightweight-charts)
       - Loading state with spinner
       - Server-side rendering disabled (ssr: false) for canvas-based chart

    ### Chart Components (5 files):
    4. **TradingChart** (`components/charts/trading-chart.tsx`)
       - Lightweight Charts integration for candlestick display
       - Dark TradingView-style theme (#1e222d background)
       - Auto-refresh based on tier (FREE: 60s, PRO: 30s)
       - Fetches OHLCV data from `/api/indicators/{symbol}/{timeframe}`
       - Responsive design with window resize handling
       - **Current Implementation:** Displays OHLCV candlesticks only (fractals and trendlines NOT rendered due to calculation issues)
       - Error handling with user-friendly messages
       - Loading states and last updated timestamp

    5. **ProIndicatorOverlay** (`components/charts/pro-indicator-overlay.tsx`)
       - Renders PRO-only indicators on the chart as line series
       - **Keltner Channels:** 10-band ATR channel system (purple gradient)
       - **Moving Averages:** TEMA (gray), HRMA (cyan), SMMA (blue)
       - **ZigZag:** Peak/bottom structure detection with connecting line (orange)
       - Uses lightweight-charts ISeriesApi for overlay management
       - Handles undefined/null values in data arrays
       - Auto-cleanup on unmount to prevent memory leaks
       - **Note:** Momentum candles handled separately by candle color modification

    6. **IndicatorToggles** (`components/charts/indicator-toggles.tsx`)
       - Checkbox UI for toggling chart indicators
       - Collapsible sections for Basic and PRO indicators
       - Lock icons (lucide-react) for tier-gated indicators
       - Upgrade prompt for FREE users with feature list
       - Uses session data from NextAuth for tier detection
       - Color-coded indicators with metadata from lib/tier/constants

    7. **ChartControls** (`components/charts/chart-controls.tsx`)
       - Symbol dropdown with category grouping (Commodity, Forex, Crypto, Index)
       - Timeframe selector integration
       - PRO badges on locked options
       - Upgrade modal for tier-restricted access
       - Navigation via Next.js router to `/charts/{symbol}/{timeframe}`
       - Symbol metadata with display names (e.g., XAUUSD → "Gold")

    8. **TimeframeSelector** (`components/charts/timeframe-selector.tsx`)
       - Timeframe button grid (3×3 grid for 9 timeframes)
       - PRO badges and lock icons for restricted timeframes
       - Timeframe display info (M5 → "5 Minutes - Scalping")
       - Upgrade prompt for FREE users
       - Controlled component with callback-based selection

    ### Hooks (2 files):
    9. **useIndicators** (`hooks/use-indicators.ts`)
       - Custom React hook for fetching indicator data
       - Fetches from `/api/indicators/{symbol}/{timeframe}`
       - Auto-refresh based on tier (FREE: 60s, PRO: 30s)
       - Error handling with retry capability
       - Returns: data, isLoading, error, refetch, lastUpdated
       - Helper functions: hasValidOHLC, getLatestPrice, getPriceChange, formatTimeSince

    10. **useAuth** (`hooks/use-auth.ts`)
        - Custom React hook for authentication and tier management
        - Built on NextAuth useSession
        - Provides tier-based access helpers: canAccessSymbol, canAccessTimeframe, canAccessChart
        - Returns: session, tier, userId, userEmail, isAuthenticated, logout
        - Additional hooks: useRequireAuth (redirect if not authenticated), useRequireTier (tier validation)

    ## Database Schema: 61-Column Flat Schema

    The system uses a NEW 61-column flat database schema (replacing the old 14-column JSON structure):

    ### System Columns (9 - accessible to all tiers):
    - `symbol`, `timestamp`, `open`, `high`, `low`, `close`, `volume`, `timeframe`, `collected_at`

    ### FREE Tier Indicators (16 columns - 2 groups):
    1. **Fractal Diagonal Lines (8 columns):**
       - `diag_asc_line_1`, `diag_asc_line_2`, `diag_asc_line_3`
       - `diag_desc_line_1`, `diag_desc_line_2`, `diag_desc_line_3`
       - `diag_high_map`, `diag_low_map`
       - Colors: ascending (#00c853), descending (#f23645)
       - Data Pattern: sparse (5-15% of bars)

    2. **Fractal Horizontal Lines (8 columns):**
       - `horiz_peak_line_1`, `horiz_peak_line_2`, `horiz_peak_line_3`
       - `horiz_bottom_line_1`, `horiz_bottom_line_2`, `horiz_bottom_line_3`
       - `horiz_high_map`, `horiz_low_map`
       - Colors: peaks (#f23645), bottoms (#00c853)
       - Data Pattern: sparse (5-15% of bars)

    ### PRO Tier Indicators (36 columns - 8 groups):
    3. **Moving Averages (3 columns):**
       - `tema`, `hrma`, `smma`
       - Colors: TEMA (#808080), HRMA (#00CED1), SMMA (#0000FF)
       - Data Pattern: continuous (every bar)

    4. **Body Size Momentum (2 columns):**
       - `body_size`, `body_direction`
       - Classification: 0=UP_NORMAL, 1=UP_LARGE, 2=UP_EXTREME, 3=DOWN_NORMAL, 4=DOWN_LARGE, 5=DOWN_EXTREME
       - Colors: 6 different colors based on classification
       - Data Pattern: continuous (every bar)

    5. **Heiken Ashi (7 columns):**
       - `ha_open`, `ha_high`, `ha_low`, `ha_close`
       - `ha_color`, `ha_trend`, `ha_strength`
       - Colors: bullish (#00c853), bearish (#f23645)
       - Data Pattern: continuous (every bar)

    6. **Keltner Channels (10 columns):**
       - `kc_upper`, `kc_middle`, `kc_lower`
       - `kc_upper_ema`, `kc_middle_ema`, `kc_lower_ema`
       - `kc_squeeze`, `kc_squeeze_pro`, `kc_width`, `kc_width_ema`
       - Colors: Purple gradient (#9C27B0 to #CE93D8) with orange middle bands
       - Data Pattern: continuous (every bar)

    7. **Support & Resistance (8 columns):**
       - `sr_1`, `sr_2`, `sr_3`, `sr_4`, `sr_5`, `sr_6`, `sr_7`, `sr_8`
       - Colors: support (#00c853), resistance (#f23645)
       - Data Pattern: sparse (10-30% of bars)

    8. **ZigZag + EMA (3 columns):**
       - `zigzag_high`, `zigzag_low`, `ema`
       - Colors: peaks (#f23645), bottoms (#00c853), EMA (#ffa726)
       - Data Pattern: peaks/bottoms sparse (2-5%), EMA continuous

    9. **Dual TEMA High/Low (2 columns):**
       - `dual_tema_high`, `dual_tema_low`
       - Colors: high (#00CED1), low (#FF6B6B)
       - Data Pattern: continuous (every bar)

    10. **Pinbar Detection (1 column):**
        - `pinbar`
        - Colors: bullish (#00c853), bearish (#f23645)
        - Data Pattern: sparse (only bars with pinbar patterns)

    **Total Columns:**
    - FREE Tier: 9 system + 16 indicator = **25 columns**
    - PRO Tier: 9 system + 52 indicator = **61 columns**

    ## Indicator Groups (lib/tier/constants.ts):

    ### FREE Tier Indicators (2 groups):
    - `fractal_diagonal` - Dynamic trendlines based on fractal analysis
    - `fractal_horizontal` - Horizontal support/resistance levels from fractals

    ### PRO-Only Indicators (8 groups):
    - `moving_averages` - TEMA, HRMA, SMMA trend indicators
    - `body_momentum` - Candle body size Z-score classification
    - `heiken_ashi` - Smoothed candlesticks with classification
    - `keltner_channels` - 10-band volatility channel system
    - `support_resistance` - Fractal-based S/R levels
    - `zigzag` - Market structure with EMA trend
    - `dual_tema_hl` - Dual TEMA applied to highs and lows
    - `pinbar_detection` - Pinbar reversal pattern detection

    ## Tier-Based Features:

    ### FREE Tier:
    - **Symbols:** 5 (BTCUSD, EURUSD, USDJPY, US30, XAUUSD)
    - **Timeframes:** 3 (H1, H4, D1)
    - **Chart Combinations:** 15 (5 × 3)
    - **Refresh Rate:** 60 seconds
    - **Indicators:** 2 groups (fractal_diagonal, fractal_horizontal)
    - **Database Access:** 25 columns

    ### PRO Tier:
    - **Symbols:** 15 (adds AUDJPY, AUDUSD, ETHUSD, GBPJPY, GBPUSD, NDX100, NZDUSD, USDCAD, USDCHF, XAGUSD)
    - **Timeframes:** 9 (adds M5, M15, M30, H2, H8, H12)
    - **Chart Combinations:** 135 (15 × 9)
    - **Refresh Rate:** 30 seconds
    - **Indicators:** 10 groups (all FREE + 8 PRO groups)
    - **Database Access:** 61 columns

    ## Data Flow:
    1. User navigates to `/charts` or `/charts/{symbol}/{timeframe}`
    2. Server-side page validates tier access via getSession()
    3. Client component (TradingChart) fetches data from `/api/indicators/{symbol}/{timeframe}`
    4. API endpoint proxies request to Flask MT5 service (Part 06)
    5. Response includes OHLCV data and indicators based on tier
    6. TradingChart renders candlesticks using Lightweight Charts
    7. ProIndicatorOverlay adds indicator lines (if PRO tier and selected)
    8. Auto-refresh triggers new fetch based on tier interval

    ## Technical Stack:
    - **Chart Library:** lightweight-charts v4.x (TradingView)
    - **State Management:** React hooks (useState, useEffect, useCallback)
    - **Data Fetching:** fetch API with auto-retry
    - **Authentication:** NextAuth with getServerSession (server) and useSession (client)
    - **Routing:** Next.js App Router with dynamic routes
    - **Styling:** Tailwind CSS with cn() utility
    - **Icons:** lucide-react (Lock, ChevronDown, ChevronUp)

    ## Implementation Notes:
    1. **IMPORTANT:** The current TradingChart implementation displays OHLCV candlesticks only. Fractal plots and trendlines are NOT rendered because they were calculated incorrectly from OHLCV data. The database columns exist and data is fetched, but rendering is disabled.

    2. All components follow React best practices:
       - TypeScript strict mode with proper type annotations
       - No `any` types used
       - Proper error boundaries and loading states
       - Cleanup functions in useEffect hooks
       - Memoized callbacks to prevent unnecessary re-renders

    3. Tier validation happens at multiple levels:
       - Server-side: Route validation in page components
       - Client-side: useAuth hook helpers
       - Component-level: Disabled states and lock icons
       - API-level: Backend validates tier before returning data

    4. The system uses the NEW 61-column flat schema (introduced in recent migration):
       - Replaces old 14-column JSON structure
       - All indicator data stored as individual columns (no JSON parsing)
       - Better query performance with column-specific indexes
       - Sparse data (nulls) for indicators that don't apply to every bar

  contact:
    name: API Support
    email: support@tradingalerts.example.com

servers:
  - url: http://localhost:3000
    description: Development server
  - url: https://tradingalerts.example.com
    description: Production server

paths: {}

components:
  schemas:
    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    # UI COMPONENT DOCUMENTATION
    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    UIComponentReference:
      type: object
      description: |
        Part 09 contains UI components only (no API endpoints).
        Chart data is fetched from \`/api/indicators/{symbol}/{timeframe}\`.
      properties:
        componentCount:
          type: integer
          example: 10
          description: Total number of files in Part 09
        pageComponents:
          type: array
          items:
            type: string
          example:
            - 'app/(dashboard)/charts/page.tsx'
            - 'app/(dashboard)/charts/[symbol]/[timeframe]/page.tsx'
            - 'app/(dashboard)/charts/[symbol]/[timeframe]/trading-chart-client.tsx'
        chartComponents:
          type: array
          items:
            type: string
          example:
            - 'components/charts/trading-chart.tsx'
            - 'components/charts/pro-indicator-overlay.tsx'
            - 'components/charts/indicator-toggles.tsx'
            - 'components/charts/chart-controls.tsx'
            - 'components/charts/timeframe-selector.tsx'
        hooks:
          type: array
          items:
            type: string
          example:
            - 'hooks/use-indicators.ts'
            - 'hooks/use-auth.ts'

    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    # INDICATOR TYPES (NEW 61-COLUMN SCHEMA)
    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    IndicatorId:
      type: string
      description: Indicator group identifier (10 total groups)
      enum:
        # FREE Tier (2 groups)
        - fractal_diagonal
        - fractal_horizontal
        # PRO Tier (8 groups)
        - moving_averages
        - body_momentum
        - heiken_ashi
        - keltner_channels
        - support_resistance
        - zigzag
        - dual_tema_hl
        - pinbar_detection

    IndicatorCategory:
      type: string
      description: Indicator category for grouping
      enum:
        - trendlines
        - support_resistance
        - trend
        - momentum
        - candlesticks
        - volatility

    IndicatorMetadata:
      type: object
      description: Complete indicator metadata with column mappings
      required:
        - id
        - label
        - description
        - category
        - tier
        - columns
        - colors
        - dataPattern
      properties:
        id:
          $ref: '#/components/schemas/IndicatorId'
        label:
          type: string
          example: 'Keltner Channels'
        description:
          type: string
          example: '10-band volatility channel system'
        category:
          $ref: '#/components/schemas/IndicatorCategory'
        tier:
          type: string
          enum: [FREE, PRO]
          description: Minimum tier required to access this indicator
        columns:
          type: array
          items:
            type: string
          description: Database column names for this indicator
          example: ['kc_upper', 'kc_middle', 'kc_lower']
        colors:
          type: object
          additionalProperties:
            type: string
          description: Color scheme for visualization (hex codes)
          example:
            ultra_extreme: '#9c27b0'
            extreme: '#ff5722'
            uppermost: '#ff9800'
        dataPattern:
          type: string
          enum: [sparse, continuous]
          description: Data density pattern (sparse = 5-30% of bars, continuous = every bar)

    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    # DATABASE SCHEMA TYPES (61-COLUMN FLAT SCHEMA)
    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    SystemColumns:
      type: object
      description: System columns - accessible by FREE + PRO (9 columns)
      required:
        - symbol
        - timestamp
        - open
        - high
        - low
        - close
      properties:
        symbol:
          type: string
          description: Trading symbol (BTCUSD, EURUSD, etc.)
          example: XAUUSD
        timestamp:
          type: integer
          description: Unix timestamp in seconds
          example: 1704067200
        open:
          type: number
          format: double
          example: 2045.50
        high:
          type: number
          format: double
          example: 2050.25
        low:
          type: number
          format: double
          example: 2042.00
        close:
          type: number
          format: double
          example: 2048.75
        volume:
          type: number
          format: double
          nullable: true
          example: 12500.0
        timeframe:
          type: string
          example: 'H1'
        collected_at:
          type: integer
          nullable: true
          description: Collection timestamp
          example: 1704067260

    FractalDiagonalData:
      type: object
      description: FREE tier indicator - Fractal Diagonal Lines (8 columns)
      properties:
        diag_asc_line_1:
          type: number
          nullable: true
        diag_asc_line_2:
          type: number
          nullable: true
        diag_asc_line_3:
          type: number
          nullable: true
        diag_desc_line_1:
          type: number
          nullable: true
        diag_desc_line_2:
          type: number
          nullable: true
        diag_desc_line_3:
          type: number
          nullable: true
        diag_high_map:
          type: number
          nullable: true
        diag_low_map:
          type: number
          nullable: true

    FractalHorizontalData:
      type: object
      description: FREE tier indicator - Fractal Horizontal Lines (8 columns)
      properties:
        horiz_peak_line_1:
          type: number
          nullable: true
        horiz_peak_line_2:
          type: number
          nullable: true
        horiz_peak_line_3:
          type: number
          nullable: true
        horiz_bottom_line_1:
          type: number
          nullable: true
        horiz_bottom_line_2:
          type: number
          nullable: true
        horiz_bottom_line_3:
          type: number
          nullable: true
        horiz_high_map:
          type: number
          nullable: true
        horiz_low_map:
          type: number
          nullable: true

    MovingAveragesColumns:
      type: object
      description: PRO tier indicator - Moving Averages (3 columns)
      properties:
        tema:
          type: number
          nullable: true
          description: Triple Exponential Moving Average
        hrma:
          type: number
          nullable: true
          description: Hull-like Responsive Moving Average
        smma:
          type: number
          nullable: true
          description: Smoothed Moving Average

    BodyMomentumData:
      type: object
      description: PRO tier indicator - Body Size Momentum (2 columns)
      properties:
        body_size:
          type: number
          nullable: true
          description: Z-score of candle body size
        body_direction:
          type: integer
          nullable: true
          description: 0=UP_NORMAL, 1=UP_LARGE, 2=UP_EXTREME, 3=DOWN_NORMAL, 4=DOWN_LARGE, 5=DOWN_EXTREME
          enum: [0, 1, 2, 3, 4, 5]

    HeikenAshiData:
      type: object
      description: PRO tier indicator - Heiken Ashi (7 columns)
      properties:
        ha_open:
          type: number
          nullable: true
        ha_high:
          type: number
          nullable: true
        ha_low:
          type: number
          nullable: true
        ha_close:
          type: number
          nullable: true
        ha_color:
          type: integer
          nullable: true
        ha_trend:
          type: number
          nullable: true
        ha_strength:
          type: number
          nullable: true

    KeltnerChannelsData:
      type: object
      description: PRO tier indicator - Keltner Channels (10 columns)
      properties:
        kc_upper:
          type: number
          nullable: true
        kc_middle:
          type: number
          nullable: true
        kc_lower:
          type: number
          nullable: true
        kc_upper_ema:
          type: number
          nullable: true
        kc_middle_ema:
          type: number
          nullable: true
        kc_lower_ema:
          type: number
          nullable: true
        kc_squeeze:
          type: number
          nullable: true
        kc_squeeze_pro:
          type: number
          nullable: true
        kc_width:
          type: number
          nullable: true
        kc_width_ema:
          type: number
          nullable: true

    SupportResistanceData:
      type: object
      description: PRO tier indicator - Support & Resistance (8 columns)
      properties:
        sr_1:
          type: number
          nullable: true
        sr_2:
          type: number
          nullable: true
        sr_3:
          type: number
          nullable: true
        sr_4:
          type: number
          nullable: true
        sr_5:
          type: number
          nullable: true
        sr_6:
          type: number
          nullable: true
        sr_7:
          type: number
          nullable: true
        sr_8:
          type: number
          nullable: true

    ZigZagColumns:
      type: object
      description: PRO tier indicator - ZigZag + EMA (3 columns)
      properties:
        zigzag_high:
          type: number
          nullable: true
          description: Swing high price level (sparse data)
        zigzag_low:
          type: number
          nullable: true
          description: Swing low price level (sparse data)
        ema:
          type: number
          nullable: true
          description: Exponential Moving Average (renamed from ema_26 in v3.0, continuous data)

    DualTemaHLData:
      type: object
      description: PRO tier indicator - Dual TEMA High/Low (2 columns)
      properties:
        dual_tema_high:
          type: number
          nullable: true
          description: Dual TEMA applied to highs (continuous data)
        dual_tema_low:
          type: number
          nullable: true
          description: Dual TEMA applied to lows (continuous data)

    PinbarData:
      type: object
      description: PRO tier indicator - Pinbar Detection (1 column)
      properties:
        pinbar:
          type: number
          nullable: true
          description: Pinbar reversal pattern detection (sparse data)

    FreeMarketData:
      allOf:
        - $ref: '#/components/schemas/SystemColumns'
        - $ref: '#/components/schemas/FractalDiagonalData'
        - $ref: '#/components/schemas/FractalHorizontalData'
      description: FREE tier market data (24 columns total)

    CompleteMarketData:
      allOf:
        - $ref: '#/components/schemas/SystemColumns'
        - $ref: '#/components/schemas/FractalDiagonalData'
        - $ref: '#/components/schemas/FractalHorizontalData'
        - $ref: '#/components/schemas/MovingAveragesColumns'
        - $ref: '#/components/schemas/BodyMomentumData'
        - $ref: '#/components/schemas/HeikenAshiData'
        - $ref: '#/components/schemas/KeltnerChannelsData'
        - $ref: '#/components/schemas/SupportResistanceData'
        - $ref: '#/components/schemas/ZigZagColumns'
        - $ref: '#/components/schemas/DualTemaHLData'
        - $ref: '#/components/schemas/PinbarData'
      description: PRO tier market data (61 columns total)

    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    # TIER CONFIGURATION
    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    TierConfig:
      type: object
      description: Tier-based access configuration for charts
      required:
        - tier
        - symbols
        - timeframes
        - refreshInterval
        - indicators
        - databaseColumns
      properties:
        tier:
          type: string
          enum: [FREE, PRO]
        symbols:
          type: array
          items:
            type: string
          description: Available trading symbols
        timeframes:
          type: array
          items:
            type: string
          description: Available timeframes
        refreshInterval:
          type: integer
          description: Data refresh interval in milliseconds
        indicators:
          type: array
          items:
            $ref: '#/components/schemas/IndicatorId'
          description: Available indicator groups
        databaseColumns:
          type: integer
          description: Total accessible database columns
          example: 25

    FreeTierConfig:
      allOf:
        - $ref: '#/components/schemas/TierConfig'
        - type: object
          properties:
            tier:
              example: 'FREE'
            symbols:
              example: ['BTCUSD', 'EURUSD', 'USDJPY', 'US30', 'XAUUSD']
            timeframes:
              example: ['H1', 'H4', 'D1']
            refreshInterval:
              example: 60000
            indicators:
              example: ['fractal_diagonal', 'fractal_horizontal']
            databaseColumns:
              example: 25

    ProTierConfig:
      allOf:
        - $ref: '#/components/schemas/TierConfig'
        - type: object
          properties:
            tier:
              example: 'PRO'
            symbols:
              example:
                [
                  'AUDJPY',
                  'AUDUSD',
                  'BTCUSD',
                  'ETHUSD',
                  'EURUSD',
                  'GBPJPY',
                  'GBPUSD',
                  'NDX100',
                  'NZDUSD',
                  'US30',
                  'USDCAD',
                  'USDCHF',
                  'USDJPY',
                  'XAGUSD',
                  'XAUUSD',
                ]
            timeframes:
              example: ['M5', 'M15', 'M30', 'H1', 'H2', 'H4', 'H8', 'H12', 'D1']
            refreshInterval:
              example: 30000
            indicators:
              example:
                [
                  'fractal_diagonal',
                  'fractal_horizontal',
                  'moving_averages',
                  'body_momentum',
                  'heiken_ashi',
                  'keltner_channels',
                  'support_resistance',
                  'zigzag',
                  'dual_tema_hl',
                  'pinbar_detection',
                ]
            databaseColumns:
              example: 61

    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━
    # COLOR CONSTANTS
    # ━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━━

    ColorConstants:
      type: object
      description: Color constants used for chart rendering
      properties:
        keltnerChannels:
          type: object
          properties:
            ultraExtremeUpper:
              example: '#9C27B0'
            extremeUpper:
              example: '#AB47BC'
            upperMost:
              example: '#BA68C8'
            upper:
              example: '#CE93D8'
            upperMiddle:
              example: '#FF5722'
            lowerMiddle:
              example: '#FF7043'
            lower:
              example: '#CE93D8'
            lowerMost:
              example: '#BA68C8'
            extremeLower:
              example: '#AB47BC'
            ultraExtremeLower:
              example: '#9C27B0'
        momentumCandles:
          type: object
          properties:
            UP_NORMAL:
              example: '#26a69a'
            UP_LARGE:
              example: '#00897b'
            UP_EXTREME:
              example: '#004d40'
            DOWN_NORMAL:
              example: '#ef5350'
            DOWN_LARGE:
              example: '#c62828'
            DOWN_EXTREME:
              example: '#7f0000'
        movingAverages:
          type: object
          properties:
            tema:
              example: '#808080'
            hrma:
              example: '#00CED1'
            smma:
              example: '#0000FF'
        zigzag:
          type: object
          properties:
            peaks:
              example: '#f23645'
            bottoms:
              example: '#00c853'
            line:
              example: '#ffa726'
        fractalDiagonal:
          type: object
          properties:
            ascending:
              example: '#00c853'
            descending:
              example: '#f23645'
        fractalHorizontal:
          type: object
          properties:
            peaks:
              example: '#f23645'
            bottoms:
              example: '#00c853'
