# Phase 6 Frontend Gap Matrix (F11) — Session 6-1

> Produced per `6-1-gap-matrix-f11.migration-order.md` Steps 1-3. Source input:
> `docs/files-completion-list/ui-page-gap-analysis.md` + `ui-page-gap-register.xlsx`
> (2026-08-10) — **re-verified against live code at Session 6-1 CONFIRM/close (2026-08-10)**,
> not adopted on trust. Every row below was independently checked by the Executor via direct
> file/grep inspection of the working tree at commit `a25a8f43` (+ uncommitted docs).
>
> **Triage column is Davin's, not the Executor's** (order Rule 3). It is unfilled below —
> this session closes with **F11 still OPEN**, pending row-by-row triage. See
> `DECISION-LOG.md` F11.

## How to read this matrix

- **Verified** = `yes` (independently re-checked this session, file:line confirmed), `yes*`
  (re-checked with a minor citation correction — see note), or `not re-checked` (Section B
  product-judgment rows verified only for the underlying page/link's non-existence, not
  re-derived from first principles).
- **Target session** is the Advisor's proposal from the order's Step 3 table. Rows marked
  `⚠ doesn't fit cleanly` are flagged per Rule "flag any row that does not fit cleanly rather
  than forcing it."
- **Triage**: `—` = not yet triaged.

---

## Section A1 — EXISTING pages to MODIFY (code-backed)

| ID    | Surface / route                                | User type(s)      | Backing evidence                                                                                                                                                                                                                                                                                                                                                                                                                                                                                               | Verified?                        | Target session                                     | Triage |
| ----- | ---------------------------------------------- | ----------------- | -------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------------------------- | -------------------------------------------------- | ------ |
| A1-1  | `/settings/billing`                            | PRO               | Zero `fetch(` in 439 lines; `mockInvoices` L61 (cited L60-61, off by one); `useState(mockInvoices)` L91 exact; `GET /api/invoices`, `GET /api/subscription`, `POST /api/subscription/cancel` all exist, zero UI consumers (confirmed: only `lib/api/index.ts` references either GET route)                                                                                                                                                                                                                     | yes\*                            | 6-1b                                               | —      |
| A1-2  | `/admin/fraud-alerts/[id]`                     | ADMIN             | `MOCK DATA` comment L63, `MOCK_ALERT` L66, `setAlert(MOCK_ALERT)` L112 — all exact. `GET /api/admin/fraud-alerts/[id]` exists, real shape confirmed (`FraudAlertStatus` enum: PENDING/REVIEWED/DISMISSED/BLOCKED), zero consumer (only a `Link` to the page itself, never a `fetch` to the API)                                                                                                                                                                                                                | yes                              | 6-1b                                               | —      |
| A1-3  | `/admin` Executive Dashboard                   | ADMIN             | (a) mock activity comment L82 exact, real `fetch('/api/admin/analytics')` at L74 confirmed live; (b) nav split confirmed independently: 15 pages under `app/(dashboard)/admin/*`, 8 under `app/admin/*` (counted directly), `app/admin/layout.tsx` confirmed **absent**                                                                                                                                                                                                                                        | yes                              | (a) 6-1b · (b) nav rebuild → 6-2                   | —      |
| A1-4  | `/settings` Overview                           | FREE/PRO          | `alerts: 3, // Mock data` at L41 exact; grid links to exactly 4 of 9 subpages confirmed (`/pricing`×2, `/settings/billing`×2, `/settings/profile`, `/settings/appearance`, `/settings/privacy`) — `account`/`security`/`help`/`language`/`terms` pages all exist but are unlinked, confirmed                                                                                                                                                                                                                   | yes                              | (a) alert count → 6-1b · (b) grid completion → 6-2 | —      |
| A1-5  | `/admin/disbursement/config`                   | ADMIN             | Provider radio confirmed `MOCK`/`RISE` only (L261-291); zero `WISE` reference anywhere in the file                                                                                                                                                                                                                                                                                                                                                                                                             | yes                              | 6-6                                                | —      |
| A1-6  | `/admin/disbursement/accounts`                 | ADMIN             | Confirmed entirely RiseWorks-driven — imports `RiseWorksKycStatus`, doc comment cites `/api/disbursement/riseworks/accounts`/`riseworks/sync`                                                                                                                                                                                                                                                                                                                                                                  | yes                              | 6-6                                                | —      |
| A1-7  | `/pricing` (+ `CountrySelector`)               | Public            | `app/api/geo/` confirmed absent. Both call sites confirmed exact: `pricing/page.tsx:155`, `CountrySelector.tsx:69`. **New finding not in the source artifact:** `lib/geo/detect-country.ts` already implements `detectCountry(headers)`/`detectCountryFromIP(ip)`, 100%-line-covered by its own test, with **zero importers anywhere** — the fix is a thin route wrapper around existing logic, not new detection code                                                                                         | yes\* (scope-narrowing addition) | 6-8                                                | —      |
| A1-8  | `/checkout`                                    | Public            | dLocal `window.location.href = data.paymentUrl` at exact L160; `GET /api/payments/dlocal/[paymentId]` confirmed zero consumers; `POST /api/checkout/validate-code` confirmed zero consumers; `GET /api/payments/dlocal/exchange-rate` confirmed zero consumers (orphan, decide wire-or-delete)                                                                                                                                                                                                                 | yes                              | 6-8                                                | —      |
| A1-9  | `/settings/security`                           | FREE/PRO          | Login history `?limit=20` cap confirmed; `SecurityAlert` confirmed zero UI consumers anywhere; writers confirmed to be exactly `app/api/user/password/route.ts`, `app/api/user/2fa/disable/route.ts`, `app/api/user/2fa/verify-setup/route.ts` (monolith) + `operation-service`'s `two-factor.service.ts`/`users.service.ts` — no more, no fewer                                                                                                                                                               | yes                              | 6-5                                                | —      |
| A1-10 | `/settings/account`                            | FREE/PRO          | Confirmed: page only calls `POST /api/user/account/deletion-request` (L323); no reference to `deletion-confirm`, `deletion-cancel`, or `AccountDeletionRequest.expiresAt` anywhere in the page — no pending-deletion banner exists                                                                                                                                                                                                                                                                             | yes                              | 6-5                                                | —      |
| A1-11 | `/alerts` + `/alerts/new`                      | PRO               | `alert-form.tsx` L70 comment "for creating and editing price alerts" confirmed exact; no `alerts/[id]/edit` route exists anywhere (`find` returned nothing). `GET /api/tier/{symbols,combinations}`, `GET /api/tier/check/[symbol]` confirmed zero UI consumers (only the route files themselves match)                                                                                                                                                                                                        | yes                              | 6-3                                                | —      |
| A1-12 | `sidebar.tsx` + `mobile-nav.tsx`               | FREE/PRO (global) | `/analytics` and `/indicators` confirmed present in both nav files, confirmed absent as real pages; no `/notifications` nav entry present                                                                                                                                                                                                                                                                                                                                                                      | yes                              | 6-2                                                | —      |
| A1-13 | `notification-bell.tsx`                        | global            | `href="/notifications"` confirmed at exact L477 (file is 503 lines); `/notifications/page.tsx` confirmed absent                                                                                                                                                                                                                                                                                                                                                                                                | yes                              | 6-4 (page ships; bell already points at it)        | —      |
| A1-14 | `/admin/affiliates/reports/code-inventory`     | ADMIN             | `POST /api/admin/codes/[code]/cancel` confirmed to exist with zero UI consumer anywhere                                                                                                                                                                                                                                                                                                                                                                                                                        | yes                              | 6-6                                                | —      |
| A1-15 | `/affiliate/dashboard/commissions`             | AFFILIATE         | Confirmed: page shows only a static "Ready for payout" string (L233); no reference to `WiseTransfer`/`WiseBatchGroup`/`DisbursementTransaction`/`PaymentBatch` anywhere in the file                                                                                                                                                                                                                                                                                                                            | yes                              | 6-7                                                | —      |
| A1-16 | `/affiliate/dashboard/profile/payment`         | AFFILIATE         | Both `.../profile/payment/page.tsx` and `/affiliate/settings/payout/page.tsx` confirmed to exist. **Correction to source claim:** `app/affiliate/dashboard/layout.tsx`'s nav links directly to `/affiliate/dashboard/profile` (parent of the legacy page) and to `/affiliate/settings/payout` — it does not link `profile/payment` directly by that literal path; the legacy page is reached via the Profile page, not the top nav. Net effect (two live, un-consolidated payment-setup surfaces) is unchanged | yes\* (corrected)                | 6-7                                                | —      |
| A1-17 | `/admin/users`                                 | ADMIN             | Confirmed: no link/route pattern to a per-user detail page found anywhere in the page file                                                                                                                                                                                                                                                                                                                                                                                                                     | yes                              | 6-6 (detail page → A2-10)                          | —      |
| A1-18 | `(marketing)/layout.tsx` + `register-form.tsx` | Public            | All 10 marketing-footer links confirmed exact: `/about`, `/blog`, `/careers`, `/changelog`, `/docs`, `/help`, `/affiliate`, `/privacy`, `/terms`, `/disclaimer`. `register-form.tsx` links confirmed at exact L534 (`/terms`), L541 (`/privacy`), L617 (`/affiliate/join`)                                                                                                                                                                                                                                     | yes                              | 6-2 (nav) + 6-10 (destination pages)               | —      |

## Section A2 — NEW pages to CREATE (code-backed)

| ID    | Route                                          | User type(s) | Backing evidence                                                                                                                                                                                                                               | Verified?      | Target session | Triage |
| ----- | ---------------------------------------------- | ------------ | ---------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | -------------- | -------------- | ------ |
| A2-1  | `/notifications`                               | ALL          | `notification-bell.tsx:477` confirmed points here; `GET/DELETE /api/notifications[/[id]]`, `POST /api/notifications/[id]/read` all live (Slice 9)                                                                                              | yes            | 6-4            | —      |
| A2-2  | `/account/deletion-confirm`                    | FREE/PRO     | `POST /api/user/account/deletion-confirm` confirmed to exist, zero UI consumers                                                                                                                                                                | yes            | 6-5            | —      |
| A2-3  | `/account/deletion-cancel`                     | FREE/PRO     | `POST /api/user/account/deletion-cancel` confirmed to exist, zero UI consumers                                                                                                                                                                 | yes            | 6-5            | —      |
| A2-4  | `/alerts/[id]/edit`                            | PRO          | `GET`/`PATCH /api/alerts/[id]` confirmed live (Slice 7, cut over); no edit route exists (see A1-11)                                                                                                                                            | yes            | 6-3            | —      |
| A2-5  | `/admin/affiliates/reports/code-flows`         | ADMIN        | `GET /api/admin/affiliates/reports/code-flows` confirmed to exist, zero UI consumer (only self-match)                                                                                                                                          | yes            | 6-6            | —      |
| A2-6  | `/affiliate/dashboard/code-inventory`          | AFFILIATE    | `GET /api/affiliate/dashboard/code-inventory` confirmed to exist, zero UI consumer                                                                                                                                                             | yes            | 6-7            | —      |
| A2-7  | `/admin/disbursement/affiliates/[affiliateId]` | ADMIN        | Not independently re-checked this session beyond confirming the disbursement route tree exists (out of the exhaustive-sample scope; carried from source)                                                                                       | not re-checked | 6-6            | —      |
| A2-8  | `/checkout/return`                             | FREE→PRO     | `GET /api/payments/dlocal/[paymentId]` confirmed orphaned (see A1-8)                                                                                                                                                                           | yes            | 6-8            | —      |
| A2-9  | `/upgrade/success`                             | FREE→PRO     | `successUrl`/`cancelUrl` confirmed at exact `app/api/checkout/route.ts:147-148`, byte-identical pattern mirrored in `money-service/src/stripe/stripe-checkout.controller.ts:95-96`; confirmed no page anywhere reads the `upgrade` query param | yes            | 6-8            | —      |
| A2-10 | `/admin/users/[id]`                            | ADMIN        | See A1-17                                                                                                                                                                                                                                      | yes            | 6-6            | —      |
| A2-11 | `/affiliate/dashboard/payouts`                 | AFFILIATE    | See A1-15                                                                                                                                                                                                                                      | yes            | 6-7            | —      |
| A2-12 | `/settings/security/activity`                  | FREE/PRO     | See A1-9                                                                                                                                                                                                                                       | yes            | 6-5            | —      |

## Section B1 — Existing surfaces to extend (no backing endpoint required)

| ID   | Surface                                | Gap                                                                                                             | Verified?      | Target session                               | Triage |
| ---- | -------------------------------------- | --------------------------------------------------------------------------------------------------------------- | -------------- | -------------------------------------------- | ------ |
| B1-1 | `app/` root                            | `app/not-found.tsx` confirmed **absent**                                                                        | yes            | 6-2                                          | —      |
| B1-2 | `app/` root                            | `app/global-error.tsx` confirmed absent; `app/error.tsx` confirmed present                                      | yes            | 6-2                                          | —      |
| B1-3 | `/settings/help`                       | Stub comment confirmed at exact L148 ("In a real implementation, this would send to a support system")          | yes            | 6-10 (or 6-5, doesn't fit cleanly — flagged) | —      |
| B1-4 | `/settings/terms`, `/settings/privacy` | Confirmed auth-gated only (under `(dashboard)`); no public duplicate exists at `/terms` or `/privacy` (see C-3) | yes            | 6-10 (blocked on F63)                        | —      |
| B1-5 | `(marketing)/page.tsx`                 | `#features`/`#affiliate` anchor targets not independently re-checked this session                               | not re-checked | 6-10                                         | —      |

## Section B2 — New pages to create (no backing endpoint yet)

| ID    | Route                             | Notes                                                                                                               | Verified?                                       | Target session                                                                   | Triage |
| ----- | --------------------------------- | ------------------------------------------------------------------------------------------------------------------- | ----------------------------------------------- | -------------------------------------------------------------------------------- | ------ |
| B2-1  | `/about`                          | Dead link, confirmed absent                                                                                         | yes                                             | 6-10                                                                             | —      |
| B2-2  | `/docs`                           | Dead link, confirmed absent                                                                                         | yes                                             | 6-10                                                                             | —      |
| B2-3  | `/blog`                           | Dead link, confirmed absent                                                                                         | yes                                             | 6-10                                                                             | —      |
| B2-4  | `/changelog`                      | Dead link, confirmed absent                                                                                         | yes                                             | 6-10                                                                             | —      |
| B2-5  | `/careers`                        | Dead link, confirmed absent                                                                                         | yes                                             | 6-10                                                                             | —      |
| B2-6  | `/disclaimer`                     | Dead link, confirmed absent; compliance-relevant (F63)                                                              | yes                                             | 6-10 (blocked on F63)                                                            | —      |
| B2-7  | `/terms` (public)                 | Dead link, confirmed absent; consent checkbox links here (F63)                                                      | yes                                             | 6-10 (blocked on F63)                                                            | —      |
| B2-8  | `/privacy` (public)               | Dead link, confirmed absent; consent checkbox links here (F63)                                                      | yes                                             | 6-10 (blocked on F63)                                                            | —      |
| B2-9  | `/help` (public)                  | Dead link, confirmed absent (distinct from auth-gated `/settings/help`)                                             | yes                                             | 6-10                                                                             | —      |
| B2-10 | `/affiliate` (landing)            | Dead link, confirmed absent                                                                                         | yes                                             | 6-10                                                                             | —      |
| B2-11 | `/affiliate/join`                 | Dead link, confirmed absent at `register-form.tsx:617`                                                              | yes                                             | 6-10                                                                             | —      |
| B2-12 | `/status`                         | External link target, not a local route — not independently re-checked                                              | not re-checked                                  | 6-10                                                                             | —      |
| B2-13 | `/welcome` (onboarding)           | No onboarding flow exists anywhere; not independently re-checked beyond confirming no `app/welcome/page.tsx`        | yes                                             | ⚠ doesn't fit cleanly — not in Step 3 table; recommend 6-2 (IA) or its own slot | —      |
| B2-14 | `/admin/system/terminals`         | 5 OpenAPI endpoints (`flask-api`), zero UI; not independently re-checked against the OpenAPI spec this session      | not re-checked                                  | 6-11                                                                             | —      |
| B2-15 | `/admin/system/jobs`              | 8 `/api/cron/*` endpoints exist (confirmed via earlier phase route census); no run history/manual trigger UI        | not re-checked (this session)                   | 6-11                                                                             | —      |
| B2-16 | `/admin/system/outbox`            | `OutboxEvent`/`OutboxPublisherCron` confirmed live in production (per `DECISION-LOG.md` F14/Session 4A-12); zero UI | yes (model/cron liveness, via CLAUDE.md record) | 6-11                                                                             | —      |
| B2-17 | `/admin/system/config-history`    | `SystemConfigHistory` model referenced in schema; UI absence not independently re-checked this session              | not re-checked                                  | 6-11                                                                             | —      |
| B2-18 | `/admin/notifications/broadcast`  | No broadcast-send UI found; not independently re-checked against `Notification` model schema this session           | not re-checked                                  | ⚠ doesn't fit cleanly — not in Step 3 table; recommend 6-11                     | —      |
| B2-19 | `/affiliate/dashboard/statements` | `cron/send-monthly-reports` cited; not independently re-checked this session                                        | not re-checked                                  | ⚠ doesn't fit cleanly — not in Step 3 table; recommend 6-7                      | —      |
| B2-20 | `/affiliate/resources`            | No marketing-asset page found; not independently re-checked this session                                            | not re-checked                                  | ⚠ doesn't fit cleanly — not in Step 3 table; recommend 6-7                      | —      |

## Section C — Structural findings

| ID  | Finding                                                                                                                                                                                                                                                                                                                                                                               | Verified? | Target session                            | Triage |
| --- | ------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------------- | --------- | ----------------------------------------- | ------ |
| C-1 | Admin split across two trees — independently counted: **15** pages under `app/(dashboard)/admin/*`, **8** under `app/admin/*` = 23 total, matches source exactly. `app/admin/layout.tsx` confirmed absent (no shared guard/nav); `app/(dashboard)/admin/layout.tsx` confirmed present. Registered as `DECISION-LOG.md` F62.                                                           | yes       | 6-2                                       | —      |
| C-2 | `middleware.ts` matcher confirmed exactly `/dashboard/:path*`, `/alerts/:path*`, `/charts/:path*`, `/settings/:path*` — `/affiliate` and `/admin` confirmed uncovered; admin exclusion confirmed deliberate (code comment cites the layout-level guard)                                                                                                                               | yes       | 6-2                                       | —      |
| C-3 | 14 dead internal links — **all 14 individually checked** against `app/<route>/page.tsx`: `/about`, `/affiliate`, `/affiliate/join`, `/analytics`, `/blog`, `/careers`, `/changelog`, `/disclaimer`, `/docs`, `/help`, `/indicators`, `/notifications`, `/privacy`, `/terms` — every one confirmed genuinely missing at the top-level route. `not-found.tsx` absence confirmed (B1-1). | yes       | 6-2                                       | —      |
| C-4 | `lib/api/index.ts` orphan convention — confirmed `/api/invoices` and `/api/subscription` have `lib/api/index.ts` as their sole non-route reference anywhere in `app/`, `components/`, `lib/`, `hooks/`. `/api/candles/[symbol]` not re-checked this session (source notes it's by-design, not a gap).                                                                                 | yes       | n/a (convention note, not a build target) | —      |

---

## Corrections found this session (Step 1 re-verification)

1. **A1-1:** `mockInvoices` declaration is at line 61, not "60-61" as cited (trivial off-by-one).
2. **A1-7 / F61:** `lib/geo/detect-country.ts` already contains a complete, tested
   `detectCountry(headers)` / `detectCountryFromIP(ip)` implementation with **zero importers
   anywhere** — not mentioned in the source artifact. This narrows F61's real fix to a thin
   route wrapper, not new detection logic from scratch. Recorded as a Deviation, not a drift
   (an addition, not an error).
3. **A1-16:** the source artifact's claim that `app/affiliate/dashboard/layout.tsx` "links to
   both" payment pages is imprecise — the layout links directly to `/affiliate/dashboard/profile`
   (the legacy page's parent) and to `/affiliate/settings/payout`; the legacy `profile/payment`
   page is reached via Profile, not the top nav. The substantive finding (two live,
   un-consolidated payment-setup surfaces) is unchanged.
4. **Section B total count label is inconsistent across artifacts:** `DECISION-LOG.md`'s F11
   entry says "Section B UX gaps (22 NEW)"; `ui-page-gap-analysis.md`'s own Section 5 summary
   says "New pages — Section B: 20" (deduped). Neither figure includes Section B1's 5
   extend-existing rows. This matrix itemizes 5 (B1) + 20 (B2) = 25 total Section B rows —
   treat both "22" and "20" as informal summary labels, not a row count to reconcile against.
5. Six rows (A2-7, B1-5, B2-12, B2-14, B2-15, B2-17) were **not independently re-verified**
   this session beyond what the source artifact and prior-session records already established —
   flagged explicitly above rather than silently marked "yes."
6. Four rows (B2-13 welcome/onboarding, B2-18 admin broadcast, B2-19 affiliate statements,
   B2-20 affiliate resources) don't fit cleanly into the order's own Step 3 session-assignment
   table — flagged with a recommendation rather than forced into a session per the order's own
   Rule.

---

**Produced:** 2026-08-10 (Session 6-1) · **Status:** F11 stays OPEN — Triage column awaits Davin.
