The job failed because the test expects the /api/indicators endpoint to include an indicator with id 'FRACTAL_HORIZONTAL', but in your backend implementation (app/api/indicators/route.ts, ref: 7892c7e6d86aba1c455b9e8ccd6066ad8fcc316e), the indicator IDs are lowercase (e.g., 'fractal_horizontal' instead of 'FRACTAL_HORIZONTAL'). However, your test (see **tests**/api/indicators.test.ts) is searching for the uppercase variant.

To resolve this, make the indicator IDs in INDICATOR_TYPES use uppercase (matching the test). Example fix for app/api/indicators/route.ts (ref: 7892c7e6d86aba1c455b9e8ccd6066ad8fcc316e):

Change:

```typescript
{
  id: 'fractal_horizontal',
  name: 'Fractal Horizontal Lines',
  // ...
}
```

To:

```typescript
{
  id: 'FRACTAL_HORIZONTAL',
  name: 'Fractal Horizontal Lines',
  // ...
}
```

Apply this change to all indicator definitions in INDICATOR_TYPES, ensuring the IDs match exactly what your tests expect.

For reference, compare with frontend/app/api/indicators/route.ts (ref: 7892c7e6d86aba1c455b9e8ccd6066ad8fcc316e), which already uses uppercase IDs for indicators.

After updating the IDs to uppercase, rerun your tests—the failing test should pass.
