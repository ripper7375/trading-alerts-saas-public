I'll analyze the failing job logs to identify the issue and provide a solution. Now let me get the file that has the errors: ## Solution

The build is failing due to **TypeScript ESLint violations** in `app/test-api/page.tsx`. The `@typescript-eslint/no-explicit-any` rule is rejecting three instances of the `any` type at:

- Line 7, column 40
- Line 13, column 28
- Line 24, column 19

### Fix

Replace the `any` types with proper TypeScript types. Here are your options:

**Option 1: Use specific types** (Recommended)

```typescript
// Instead of:
const data:  any = ...

// Use specific types like:
const data:  { key: string; value: number } = ...
// or
const response: Response = ...
// or
const error: Error = ...
```

**Option 2: Use `unknown` type** (if the type is truly unknown)

```typescript
// For truly dynamic data:
const data: unknown = ...
// Then narrow the type with type guards before use
```

**Option 3: Disable the rule temporarily** (Not recommended)
If you need a quick workaround, add `// eslint-disable-next-line @typescript-eslint/no-explicit-any` above each line, but this should only be temporary.

Since I couldn't access the file contents, please check what these `any` types represent and replace them with appropriate types based on the actual data structure being used.
