The failing job 60555816581 triggers this step in the workflow:

```yaml
- name: Fail if any test failed
  if: |
    needs.unit-and-component-tests.result == 'failure' ||
    needs.integration-tests.result == 'failure' ||
    needs.build-check.result == 'failure'
  run: |
    echo "❌ One or more test jobs failed"
    exit 1
```

This error means that at least one of the dependent jobs—unit-and-component-tests, integration-tests, or build-check—failed. To fix this, check the results of these jobs in the workflow. Address any underlying errors in those jobs (for example, failed tests, lint issues, bundle size too large, build errors, or dependency installation problems).

Once you correct the cause of failure in a dependent job, this summary job will succeed. If you need help with a specific failed step in those jobs, provide its log for targeted troubleshooting.

You can review and edit the workflow in .github/workflows/tests.yml at ref 79cad39ff5ac1bdb8759c6b7f32e72d3695ed2a2.
