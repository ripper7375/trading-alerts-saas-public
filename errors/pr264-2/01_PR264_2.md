The failing job shows two main types of issues:

1. **React Testing Library Warning:**  
   Many tests (especially PlanSelector and related components) throw:

   > An update to PlanSelector inside a test was not wrapped in act(...).

   **Solution:**
   - Wrap any state updates (including async code that triggers rendering) in your tests with `await act(async () => { ... })`.
   - For example, when triggering events, do:

     ```js
     import { act } from 'react-dom/test-utils';

     await act(async () => {
       // Your state update or render code
     });
     ```

   - If you use `@testing-library/react`, most of its `fireEvent` and `render` helpers auto-wrap with `act`, but not all async updates. When using SWR or similar (as shown in the logs), wrap async logic in `act`.
   - Double check that your `PlanSelector` tests use `await waitFor(...)` or `await act(...)` when waiting for data fetching or updates.

2. **Indicator Tests Failures:**  
   Tests are failing around indicator access and constants (e.g., "momentum_candles" for the PRO tier), typical in:
   - `__tests__/lib/tier-validation.test.ts`
   - `__tests__/lib/tier/validator.test.ts`
   - `__tests__/components/charts/indicator-toggles.test.tsx`
   - `__tests__/lib/tier/constants.test.ts`

   **Solution:**
   - Review your PRO_ONLY_INDICATORS and access logic to ensure "momentum_candles" is properly included for the PRO tier.
   - Example for checking constants:
     ```ts
     // lib/tier/constants.ts
     export const PRO_ONLY_INDICATORS = [
       // ...other indicators,
       'momentum_candles',
     ];
     ```
   - Update validator logic to return true when checking PRO tier access for "momentum_candles".

**General Recommendations:**

- Address the act() warning first: this can mask real state sync/test issues.
- Run the failing tests locally (names listed above) and address assertion errors by syncing your business logic ("momentum_candles" for PRO) and test expectations.

Want help with a specific test file or want to see a concrete code edit for any of these failures?
