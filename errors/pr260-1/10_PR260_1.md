The failing job 60420762859 is due to the linting step failing in your security check workflow. According to the logs:

- Linting: FAILED
- All other checks passed

The relevant workflow definition runs ESLint with pnpm run lint. To resolve this:

1. Run pnpm run lint locally or check the output in your workflow logs for details about linting errors.
2. Fix all reported ESLint issues in your codebase.
3. Commit and push the fixes to retrigger the workflow.

Example: If you see an error like Missing semicolon, add the required semicolon. For other rule violations, follow the ESLint suggestions.

If you need to see the exact errors, run:

```bash
pnpm run lint
```

This will output all issues that must be fixed for the CI job to succeed. After fixing, the job should pass.
