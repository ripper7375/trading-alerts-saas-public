The failing job is due to the CRON_SECRET environment variable not being configured in the test environment. The error occurs in app/api/cron/process-pending-disbursements/route.ts at line 24, where the code checks:

```typescript
if (!cronSecret) {
  console.error('CRON_SECRET not configured');
  return NextResponse.json(
    { error: 'Cron job not configured' },
    { status: 500 }
  );
}
```

([source](https://github.com/ripper7375/trading-alerts-saas-public/blob/6128f0608de1372e6e55673a02da6940c8bda884/app/api/cron/process-pending-disbursements/route.ts#L23-L28))

The corresponding test in **tests**/api/cron/process-pending.test.ts expects a 500 error and the message 'Cron job not configured' when the CRON_SECRET is missing. However, the main problem shown in the logs is that tests which require CRON_SECRET often run in an environment where it's missing or not injected, triggering the failure.

**Solution:**

- Ensure that the CRON_SECRET environment variable is set during test runs in the CI pipeline.
- In your workflow file (.github/workflows/ci-nextjs-progressive.yml), add an env section under the test job:

```yaml
jobs:
  test:
    ...
    steps:
      ...
      - name: Run tests
        run: |
          pnpm test
        env:
          CI: true
          CRON_SECRET: test-cron-secret
```

- Alternatively, configure this variable in your test runner setup or in the package.json test script if running locally.

**Summary of Fix:**
Set the CRON_SECRET env variable in your test environment to match what your tests expect (e.g., test-cron-secret). This will resolve the "CRON_SECRET not configured" error and let tests involving the cron endpoint pass.
