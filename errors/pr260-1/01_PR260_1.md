The job failed due to a lint error in components/indicators/indicator-selector.tsx at line 27:

Error: 'BASIC*INDICATORS' is assigned a value but only used as a type. Allowed unused vars must match /^*/u. @typescript-eslint/no-unused-vars

To fix this, remove the unused BASIC_INDICATORS declaration or prefix it with an underscore if you intend to keep it as a placeholder for type consistency. Suggested solution:

Replace:

```ts
const BASIC_INDICATORS = [] as const;
```

With:

```ts
const _BASIC_INDICATORS = [] as const;
```

Or, if not needed at all, simply remove the line. This will resolve the ESLint error and allow the build to pass. See the full code at ref 6128f0608de1372e6e55673a02da6940c8bda884 for more context.
