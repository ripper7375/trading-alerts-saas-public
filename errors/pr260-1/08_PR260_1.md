The failing job is caused by an ESLint error in components/indicators/indicator-selector.tsx. The error is:

Error: 'BASIC*INDICATORS' is assigned a value but only used as a type. Allowed unused vars must match /^*/u. @typescript-eslint/no-unused-vars

This happens because BASIC_INDICATORS is defined as an empty array (line 27), but is never used except in the IndicatorId type. ESLint is complaining that this is an unused variable.

To fix this, you can change the definition of BASIC_INDICATORS to use a type-only import, or prefix the variable name with an underscore to satisfy the lint rule. The best solution in this context is to prefix it with an underscore since you want to keep the type reference for IndicatorId.

Replace:

```tsx
const BASIC_INDICATORS = [] as const;
```

With:

```tsx
const _BASIC_INDICATORS = [] as const;
// Update the IndicatorId type accordingly:
type IndicatorId =
  | (typeof _BASIC_INDICATORS)[number]['id']
  | (typeof PRO_ONLY_INDICATORS)[number]['id'];
```

Or, if BASIC_INDICATORS is not needed at all, you can remove it and adjust the type accordingly.

Make these changes in components/indicators/indicator-selector.tsx (ref: 6128f0608de1372e6e55673a02da6940c8bda884) to resolve the issue and pass the lint step.
