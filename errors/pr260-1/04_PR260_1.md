The job failed due to a TypeScript/ESLint error in components/indicators/indicator-selector.tsx:

Error: 'BASIC*INDICATORS' is assigned a value but only used as a type. Allowed unused vars must match /^*/u. @typescript-eslint/no-unused-vars

Solution:

The BASIC_INDICATORS constant is declared but never actually used except in a type, triggering the linter. To fix this, you can prefix the variable name with an underscore to indicate it's intentionally unused, or remove it entirely if not required.

Recommended code fix (line 27):

```tsx
// Change this:
const BASIC_INDICATORS = [] as const;

// To this:
const _BASIC_INDICATORS = [] as const;
```

or simply remove the declaration if it's unnecessary.

With this change, the linter error will be resolved and your build should complete successfully.

Reference: components/indicators/indicator-selector.tsx (ref: 6128f0608de1372e6e55673a02da6940c8bda884)
