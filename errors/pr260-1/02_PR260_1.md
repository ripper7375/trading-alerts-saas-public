The failing job is due to a TypeScript ESLint error in components/indicators/indicator-selector.tsx at line 27:

'BASIC*INDICATORS' is assigned a value but only used as a type. Allowed unused vars must match /^*/u. @typescript-eslint/no-unused-vars

Solution: Since BASIC_INDICATORS is defined but not used (it's just an empty array), you should remove the BASIC_INDICATORS variable entirely if it's not needed, or prefix it with an underscore to comply with your lint rules. For example, change:

```tsx
const BASIC_INDICATORS = [] as const;
```

to:

```tsx
const _BASIC_INDICATORS = [] as const;
```

Or, if BASIC_INDICATORS is not needed at all, just delete the line. This will resolve the lint error and allow your job to pass. See the file at this ref for context: components/indicators/indicator-selector.tsx (ref: 6128f0608de1372e6e55673a02da6940c8bda884).
