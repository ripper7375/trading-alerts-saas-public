The job failed due to an ESLint error in components/indicators/indicator-selector.tsx (ref: 6128f0608de1372e6e55673a02da6940c8bda884). The error is:

Error: 'BASIC*INDICATORS' is assigned a value but only used as a type. Allowed unused vars must match /^*/u. @typescript-eslint/no-unused-vars

Solution:
Since BASIC_INDICATORS is not used except in type definitions, and ESLint is enforcing no unused variables unless they start with an underscore, update the following line:

```ts
const BASIC_INDICATORS = [] as const;
```

to

```ts
const _BASIC_INDICATORS = [] as const;
```

Then update the `IndicatorId` type definition:

```ts
type IndicatorId =
  | (typeof _BASIC_INDICATORS)[number]['id']
  | (typeof PRO_ONLY_INDICATORS)[number]['id'];
```

This resolves the ESLint unused variable error by using the underscore naming convention.

View the file with the error here: components/indicators/indicator-selector.tsx (ref: 6128f0608de1372e6e55673a02da6940c8bda884).
