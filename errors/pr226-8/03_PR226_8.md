The integration test job failed during the "Run integration tests" step, which runs pnpm run test:integration. To resolve this:

1. Check the logs for pnpm run test:integration in the integration-tests job to identify specific errors (e.g., failed assertions, missing setup, DB connection issues).
2. Ensure your integration tests and environment variables (like NODE_ENV: test) are set up correctly in your codebase and workflow.
3. If using a database or external service, verify it is running and accessible during the test step.
4. To debug, run pnpm run test:integration locally or add a debugging step in the workflow to inspect the test environment.

If you share the output of pnpm run test:integration, I can provide targeted code fixes. For now, focus on diagnosing and resolving the integration test issues in your project code, especially those surfaced by pnpm run test:integration during CI.

You can review the workflow definition for the integration-tests job here (ref: 12e13c47e7b47c2b6fd3db627c190e51740ea051):
.github/workflows/tests.yml, lines 64-94
