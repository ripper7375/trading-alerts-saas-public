I'll analyze the failing job logs to identify the issue and provide a solution.The job failed because "One or more test jobs failed", but this appears to be a status aggregation job. Let me look at the workflow file to understand the job dependencies and see which actual test failed.The `test-summary` job is failing because one or more of its dependent test jobs failed. This is a summary job that aggregates results from:

- unit-and-component-tests
- integration-tests
- build-check

The logs show that Integration Tests and Build Check were **skipped**, which means the `unit-and-component-tests` job likely failed, preventing the downstream jobs from running due to the `needs` dependencies.

## Solution

The actual issue is with the **unit-and-component-tests** job, not the summary job itself. You need to check the logs for that job to find the root cause. The summary job is working as intended—it's correctly failing when upstream jobs fail.

However, there's a potential issue with the workflow logic. The current condition at lines 170-173:

```yaml
if: |
  needs.unit-and-component-tests.result == 'failure' ||
  needs.integration-tests.result == 'failure' ||
  needs.build-check.result == 'failure'
```

This only checks for `'failure'` status, but when jobs are skipped due to dependency failures, their result is `'skipped'`. To make the workflow more robust, update the condition to check for non-success states:

```yaml
- name: Fail if any test failed
  if: |
    needs.unit-and-component-tests.result != 'success' ||
    needs.integration-tests.result != 'success' ||
    needs.build-check.result != 'success'
  run: |
    echo "❌ One or more test jobs failed"
    exit 1
```

This ensures the job fails whether tests fail OR get skipped (which usually indicates an upstream failure).

**Next step:** Check the `unit-and-component-tests` job logs in this run to identify the actual test failure that needs to be fixed.
