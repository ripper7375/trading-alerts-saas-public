The job is failing because the **linting check failed**. Let me examine the workflow file and the actual linting errors to provide a specific solution.## Solution

The job is failing because the **ESLint linting check** has errors. The security check summary shows:

- ✅ Dependency audit: PASSED
- ✅ Type check: PASSED
- ❌ **Linting: FAILED**
- ✅ Secrets scan: PASSED
- ✅ Security headers: PASSED

### How to Fix

1. **Run linting locally to see the specific errors:**

```bash
pnpm run lint
```

2. **Try to auto-fix the linting errors:**

```bash
pnpm run lint --fix
```

3. **Commit and push the fixes:**

```bash
git add .
git commit -m "Fix linting errors"
git push
```

If auto-fix doesn't resolve all issues, manually address the remaining linting errors shown in the output of `pnpm run lint`. The errors are likely related to code style, unused variables, or ESLint rule violations in your TypeScript/JavaScript files.
