I'll analyze the failing job logs to identify the issue and provide a solution.The job is failing due to **dependency version mismatch** between your lockfile and package.json. The lockfile has Prisma version `^5.22.0` while the package.json manifest has been updated to `^6.19.2` for both `@prisma/client` and `prisma`.

## Solution

You need to update your lockfile to match the new Prisma version in your package.json. Run the following command:

```bash
npm install
```

Or if you're using pnpm:

```bash
pnpm install
```

Or if you're using yarn:

```bash
yarn install
```

This will update the lockfile to match the versions specified in your package.json (Prisma `^6.19.2`).

**Important:** Make sure to commit the updated lockfile (`package-lock.json`, `pnpm-lock.yaml`, or `yarn.lock`) to your repository. The CI check is specifically designed to catch when dependencies are mismatched between the manifest and lockfile to ensure consistent installations across environments.

If you intentionally updated Prisma to version 6.x, be aware that this is a major version upgrade and you should:

1. Review the [Prisma 6.x migration guide](https://www.prisma.io/docs/guides/upgrade-guides/upgrading-versions)
2. Test your application thoroughly for any breaking changes
3. Update any Prisma-related code if needed
