I'll analyze the failing job logs to identify the issue and provide a solution.The job is failing due to a version mismatch between your lockfile and package.json. Let me check the current package.json to see the exact versions:## Solution

The job is failing due to a **version mismatch** between your lockfile and package manifests for Prisma packages. The lockfile has `^5.22.0` while the manifest expects `^6.19.2`.

### Root Cause

Your `frontend/package.json` still uses older Prisma versions while the root `package.json` has been updated to newer versions:

- **Root package.json**: `@prisma/client: ^6.19.2` and `prisma: ^6.19.2`
- **Frontend package.json**: `@prisma/client: ^5.22.0` and `prisma: ^5.22.0` ❌

### Fix

Update `frontend/package.json` to align with the root package versions:

```json
{
  "dependencies": {
    "@prisma/client": "^6.19.2" // Update from ^5.22.0
    // ... other dependencies
  },
  "devDependencies": {
    "prisma": "^6.19.2" // Update from ^5.22.0
    // ... other devDependencies
  }
}
```

### Steps to Apply

1. **Update frontend/package.json** (lines 29 and 90):
   - Change `"@prisma/client": "^5.22.0"` to `"@prisma/client": "^6.19.2"`
   - Change `"prisma": "^5.22.0"` to `"prisma": "^6.19.2"`

2. **Regenerate the lockfile**:

   ```bash
   pnpm install
   ```

3. **Commit both changes**:
   ```bash
   git add frontend/package.json pnpm-lock.yaml
   git commit -m "fix: sync Prisma versions across package.json files"
   ```

This will ensure all package.json files use consistent Prisma versions and the lockfile matches the manifests.
