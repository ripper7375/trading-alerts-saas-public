I'll analyze the failing job logs to identify the issue and provide a solution.## Solution

The job is failing due to a version mismatch between your package.json manifests and the lockfile for Prisma dependencies. The root `package.json` has been upgraded to Prisma v6.19.2, but `frontend/package.json` still uses v5.22.0.

### Fix

Update `frontend/package.json` to match the root package versions:

**Line 29** - Change:

```json
"@prisma/client": "^5.22.0",
```

To:

```json
"@prisma/client": "^6.19.2",
```

**Line 90** - Change:

```json
"prisma": "^5.22.0",
```

To:

```json
"prisma": "^6.19.2",
```

### After updating the file:

Run `pnpm install` to update the lockfile with the new versions. This will resolve the mismatch and allow the security checks to pass.
