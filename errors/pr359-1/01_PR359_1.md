The failure is due to mismatched dependency versions in your project:

- @prisma/client (lockfile: ^5.22.0, manifest: ^6.19.2)
- prisma (lockfile: ^5.22.0, manifest: ^6.19.2)

Your lockfile (likely package-lock.json or yarn.lock) has version ^5.22.0, but your package.json lists ^6.19.2. To fix:

1. Update dependencies in package.json to match the lockfile, or vice versa:
   - If you want to use the newer versions, run:
     ```bash
     npm install @prisma/client@^6.19.2 prisma@^6.19.2
     ```
     Then commit the updated lockfile.
   - If you want to stick with the old versions, adjust package.json:
     ```json
     "@prisma/client": "^5.22.0",
     "prisma": "^5.22.0"
     ```
     And run:
     ```bash
     npm install
     ```

2. Commit both package.json and lockfile changes.

Choose the version you want to use, make them consistent between package.json and the lockfile, and rerun the workflow. That should resolve this error.
