I'll analyze the failing job logs to identify the issue and provide a solution.The job is failing due to a **dependency version mismatch** between your `package.json` (manifest) and `package-lock.json` (lockfile).

## Problem

Your manifest specifies:

- `@prisma/client: ^6.19.2`
- `prisma: ^6.19.2`

But your lockfile has:

- `@prisma/client: ^5.22.0`
- `prisma: ^5.22.0`

## Solution

You need to update your `package-lock.json` to match the newer Prisma versions in `package.json`. Run this command locally:

```bash
npm install
```

This will regenerate `package-lock.json` with the correct versions (6.19.2) to match your `package.json`.

After running this:

1. Commit the updated `package-lock.json`
2. Push the changes

The CI should pass once the lockfile is synchronized with your manifest versions.
