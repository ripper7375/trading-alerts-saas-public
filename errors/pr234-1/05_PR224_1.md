The job failed for these reasons:

1. **Module Not Found Errors**
   - Both `__tests__/lib/api/mt5-client.test.ts` and `__tests__/lib/api/mt5-transform.test.ts` fail with `Resolver._throwModNotFoundError`.
   - This means the files they are importing—like `@/lib/api/mt5-client` and `@/lib/api/mt5-transform`—are not found by Jest.
   - Typically, this is due to `moduleNameMapper` missing or not matching the project's path aliases in your Jest config.
   - **Solution:** In `jest.config.js`, add or fix a `moduleNameMapper` entry to resolve `@/` to the repository’s source directory. For example:
     ```js
     module.exports = {
       // other config...
       moduleNameMapper: {
         '^@/(.*)$': '<rootDir>/src/$1', // adjust 'src' if your code lives elsewhere
       },
     };
     ```
   - Ensure the referenced files actually exist at the resolved locations.

2. **Test Failure in `alert-checker.test.ts`**
   - The integration test “should fetch price for each unique symbol” failed on a `toHaveBeenCalledWith` check at line 253.
   - This likely means the mocked call(s) to `fetch` are not matching the assertion, possibly due to differences in arguments or call order.
   - **Solution:**
     - Print the received arguments before the assertion to debug difference:
       ```js
       console.log(mockFetch.mock.calls);
       ```
     - Make sure your test is calling `mockFetch` with the expected URL and options for each unique symbol.
     - Review the implementation being tested to ensure only one call per unique symbol, and that your test setup aligns.
     - If matching on deep objects, consider using `expect.objectContaining()` or `expect.anything()` for portions that may vary.

3. **General**
   - The test log reports hasteImpl filename conflicts. If using Jest and encountering duplicated package.json (e.g. in multiple subdirectories), consider excluding those directories or restructuring your project if possible.

Relevant files and test definitions:

- [**tests**/lib/api/mt5-client.test.ts](https://github.com/ripper7375/trading-alerts-saas-public/blob/d813f86a30c5cf673a910d1d7f826e13260135b5/__tests__/lib/api/mt5-client.test.ts)
- [**tests**/lib/api/mt5-transform.test.ts](https://github.com/ripper7375/trading-alerts-saas-public/blob/d813f86a30c5cf673a910d1d7f826e13260135b5/__tests__/lib/api/mt5-transform.test.ts)
- [**tests**/lib/jobs/alert-checker.test.ts](https://github.com/ripper7375/trading-alerts-saas-public/blob/d813f86a30c5cf673a910d1d7f826e13260135b5/__tests__/lib/jobs/alert-checker.test.ts)

**Focus your fixes on:**

- Adding correct alias resolution for `@/` in Jest.
- Verifying all imported files exist at their mapped locations.
- Adjusting fetch mocking and assertions in alert-checker integration tests.

These changes should resolve the current job failure.
