The root cause of your failing job (59792180657) is that your tests are trying to connect to services (notably PostgreSQL at 127.0.0.1:5432 and Redis), but connections are refused. Additionally, two test suites (**tests**/lib/api/mt5-client.test.ts and **tests**/lib/api/mt5-transform.test.ts) fail because required modules can't be resolved. There's also an integration test failure in **tests**/lib/jobs/alert-checker.test.ts.

Here’s how to solve these issues:

---

### 1. Database Connection Errors (ECONNREFUSED 127.0.0.1:5432)

#### Cause

Your test environment lacks a running PostgreSQL instance, so all tests depending on the DB fail.

#### Solution

- **If integration with Postgres is needed for the tests:** Add a Postgres service in your workflow by updating `.github/workflows/ci-nextjs-progressive.yml`:

  ```yaml
  services:
    postgres:
      image: postgres:13
      ports:
        - 5432:5432
      env:
        POSTGRES_DB: your_db
        POSTGRES_USER: your_user
        POSTGRES_PASSWORD: your_password
      options: >-
        --health-cmd pg_isready
        --health-interval 10s
        --health-timeout 5s
        --health-retries 5
  ```

  Make sure your test config uses these credentials from environment variables.

- **If a real database is NOT required:** Mock the database methods in your tests to avoid actual connections.

---

### 2. "[Redis] Invalidate pattern error: Connection is closed."

#### Cause

Redis is either not running or not accessible from the test suite.

#### Solution

- **Use a Redis service in your workflow:**

  ```yaml
  services:
    redis:
      image: redis
      ports:
        - 6379:6379
      options: --health-cmd "redis-cli ping" --health-interval 10s --health-timeout 5s --health-retries 5
  ```

  And ensure application uses the right host and port from env variables.

- **Alternatively, mock Redis calls** in unit/integration tests where the real Redis connection is not essential.

---

### 3. Module Not Found For Tests

Tests in `__tests__/lib/api/mt5-client.test.ts` and `mt5-transform.test.ts` fail with:

```
at Resolver._throwModNotFoundError ...
```

#### Cause

A required module isn't installed or there's a broken import path.

#### Solution

- Check that any required code for `mt5-client` and `mt5-transform` exists and is committed.
- If using a custom module, ensure it’s referenced correctly in `package.json` and resolved properly in import statements.
- Run `pnpm install` (or the relevant install command for your package manager) to ensure dependencies are present before tests in the workflow.

---

### 4. Coverage Reports & Alert Checker Test

`__tests__/lib/jobs/alert-checker.test.ts` reports:

```
Alert Checker Job › checkAlerts integration › should fetch price for each unique symbol
at Object.toHaveBeenCalledWith ...
```

#### Cause

The test likely expects a mocked function to be called with specific parameters, but the call doesn't happen as expected—likely due to setup errors above (broken DB/Redis or missing modules).

#### Solution

Once the service mocks and dependencies are fixed, reevaluate this test. If it still fails, check that your mock and real implementations align in terms of expected calls.

---

## Code Suggestions

**Example: Mocking Redis in a Jest test**

```js
jest.mock('ioredis', () => require('ioredis-mock'));
```

**Example: Use environment variables in your app to configure DB and Redis**

```js
const redis = new Redis(process.env.REDIS_URL || 'redis://localhost:6379');
const db = new Client({
  connectionString:
    process.env.DATABASE_URL || 'postgres://user:pass@localhost:5432/db',
});
```

---

### Summary Checklist

- [ ] Add Postgres & Redis services to CI workflow if tests require real connections.
- [ ] Mock services if real connections are not required (faster tests and more isolation).
- [ ] Ensure all modules required by tests are present and paths are correct.
- [ ] Re-run CI after changes.

For code lines referenced in logs, see:

- [lib/cache/redis.ts:110](https://github.com/ripper7375/trading-alerts-saas-public/blob/d813f86a30c5cf673a910d1d7f826e13260135b5/lib/cache/redis.ts#L110)
- [app/api/indicators/[symbol]/[timeframe]/route.ts:294](https://github.com/ripper7375/trading-alerts-saas-public/blob/d813f86a30c5cf673a910d1d7f826e13260135b5/app/api/indicators/[symbol]/[timeframe]/route.ts#L294)

If you provide your workflow file or further configuration, I can give you exact YAML and code edits customized to your setup.
