The failing job was caused by a high-severity vulnerability detected during the dependency security audit step, which uses pnpm audit. To resolve this:

1. Run pnpm audit locally to identify the package(s) with high vulnerabilities.
2. Upgrade the vulnerable package(s) to a fixed version using:
   pnpm up <package-name>@<safe-version>
3. After upgrading, re-run pnpm audit to confirm the vulnerability is resolved.
4. Commit the updated package.json and pnpm-lock.yaml files.

If the vulnerability cannot be resolved with an upgrade because no patched version exists, consider using the --audit-level=critical option in the workflow to only fail on critical vulnerabilities, but this is not recommended unless absolutely necessary.

Example code change in your workflow to prevent failure on high but not critical vulnerabilities (if you must):

```yaml
- name: Run security audit
  run: |
    echo "🔍 Running pnpm security audit..."
    pnpm audit --audit-level=critical
    echo "✅ No critical vulnerabilities found"
```

The best solution is always to update the vulnerable packages when possible. For more information on the vulnerability, review the details at: https://github.com/advisories/GHSA-36hm-qxxp-pg3m
