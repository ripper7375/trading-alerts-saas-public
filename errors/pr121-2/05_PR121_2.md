The failing test is:
Header Component › tier badge › should have blue styling for PRO badge
at **tests**/components/layout/header.test.tsx line 143.

The test expects the PRO badge to have the CSS class bg-blue-600:

```tsx
const proBadge = screen.getByText(/PRO/);
expect(proBadge).toHaveClass('bg-blue-600');
```

To resolve the failure, check the Header component (app/components/layout/header.tsx) and ensure that when a user's tier is PRO, the badge element includes the class bg-blue-600. For example:

```tsx
<span
  className={
    user.tier === 'PRO'
      ? 'bg-blue-600 text-white ...'
      : 'bg-gray-100 text-gray-800 ...'
  }
>
  {user.tier}
</span>
```

Make sure this mapping matches what the test expects for both FREE and PRO badges, so the styling matches:

- PRO: bg-blue-600
- FREE: bg-gray-100

After updating, rerun the tests to confirm the fix.
