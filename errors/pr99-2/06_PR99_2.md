The failing job is caused by the GET /api/indicators/[symbol]/[timeframe] route not returning the expected error message when an MT5ServiceError is thrown. The test expects the error response to contain error: "MT5 service error", but the actual response contains error: "Internal server error".

Solution:
Update the error handler for the GET /api/indicators/[symbol]/[timeframe] endpoint to return the message "MT5 service error" when handling MT5ServiceError. The test file (**tests**/api/indicators.test.ts, see lines 435–459) verifies this scenario.

Example code fix (in app/api/indicators/[symbol]/[timeframe]/route.ts):

```typescript
try {
  // ...existing logic...
} catch (err) {
  if (err instanceof MT5ServiceError) {
    return NextResponse.json(
      { success: false, error: 'MT5 service error' },
      { status: 500 }
    );
  }
  // ...other error handling...
}
```

This change ensures that when MT5ServiceError is thrown, the API returns the correct error string, passing the test and matching expectations.
