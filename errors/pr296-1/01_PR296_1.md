The main failures in job 61342178922 relate to tests in the following files:

- [lib/tier/**tests**/validator.test.ts (ref: 7091cf6274bdc2712a96d1712b4f4f207d105d89)](https://github.com/ripper7375/trading-alerts-saas-public/blob/7091cf6274bdc2712a96d1712b4f4f207d105d89/lib/tier/__tests__/validator.test.ts)
- [lib/tier/**tests**/constants.test.ts (ref: 7091cf6274bdc2712a96d1712b4f4f207d105d89)](https://github.com/ripper7375/trading-alerts-saas-public/blob/7091cf6274bdc2712a96d1712b4f4f207d105d89/lib/tier/__tests__/constants.test.ts)

#### Problems Detected

1. **Column/Indicator Access Tests**
   - There are failures indicating a mismatch in expected indicator/column counts (e.g., `PRO tier can access all columns`, `body_momentum should have exactly 2 columns`).
   - Tests expect fixed counts: FREE = 2 indicators (16 columns), PRO = 8 indicators (49 columns), etc.
   - If constants or metadata (such as `INDICATOR_METADATA`) have changed or there's an off-by-one error, the expectations in the tests may no longer match the implementation.

2. **Column/Indicator Metadata**
   - Check if `body_momentum` or any other indicator in your metadata is missing columns or has the wrong number of columns.
   - For example, the test expects `body_momentum` to have exactly 2 columns: `'z_score_of_body_size'` and `'candle_classification'`.

#### How to Fix

- In your [indicator `constants` file](https://github.com/ripper7375/trading-alerts-saas-public/blob/7091cf6274bdc2712a96d1712b4f4f207d105d89/lib/tier/constants.ts) (or wherever `INDICATOR_METADATA` is defined), check the column arrays for each indicator, especially `body_momentum`:

  ```ts
  body_momentum: {
    id: 'body_momentum',
    label: ...,
    description: ...,
    category: ...,
    tier: 'PRO',
    columns: ['z_score_of_body_size', 'candle_classification'],
    // ...other properties
  }
  ```

  Make sure the columns array matches the test expectation.

- Verify all indicator/column type counts:
  - FREE tier indicators: 2, columns: 16
  - PRO tier indicators: 6, columns: 33
  - All indicators: 8, columns: 49

- Ensure the sum of columns across all indicators matches the test assertions:

  ```ts
  expect(INDICATOR_METADATA.body_momentum.columns).toHaveLength(2);
  ```

- If the number of system columns changed, update both the `SYSTEM_COLUMNS` constant and the corresponding tests.

#### Code Suggestion

If necessary, update your indicator metadata like this:

```ts
INDICATOR_METADATA.body_momentum = {
  ...INDICATOR_METADATA.body_momentum,
  columns: ['z_score_of_body_size', 'candle_classification'],
};
```

Or correct any mismatched counts in your constants:

```ts
export const FREE_TIER_INDICATORS = ['fractal_diagonal', 'fractal_horizontal'];
export const PRO_ONLY_INDICATORS = [
  'moving_averages',
  'body_momentum',
  'heiken_ashi',
  'keltner_channels',
  'support_resistance',
  'zigzag',
];
```

#### Summary

Review and update your indicator metadata and column arrays to ensure consistency with the test expectations described above. This will address the failed assertions in your test suite.
