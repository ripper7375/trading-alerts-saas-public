I'll analyze the failing job logs to identify the issue and provide a solution.I can see 3 failing tests related to a 57-column schema update. Let me examine the failing test files to understand the issue better:## Solution for Failing Tests

The test failures are related to inconsistencies in the indicator metadata definitions. There are **3 failing tests** with **8 total failures** across the test suite:

### Issues Identified:

1. **`lib/tier/__tests__/constants.test.ts:229`** - `body_momentum` column count mismatch
2. **`lib/tier/__tests__/validator.test.ts:125`** - PRO tier column access validation failing
3. **`__tests__/components/charts/indicator-toggles.test.tsx:116`** - Missing indicator label

### Root Cause:

The tests are failing because the actual `INDICATOR_METADATA` constants don't match what the tests expect. I need to see the constants file to provide the exact fix.
