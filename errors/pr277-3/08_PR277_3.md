The failure is due to a TypeScript error in frontend/jest.setup.ts: "Cannot redeclare block-scoped variable 'jest'." This happens because jest is already defined globally by the Jest environment, and redeclaring it using declare const jest: ... is not needed and causes a conflict.

To fix this, remove or comment out lines 9–11 from frontend/jest.setup.ts:

```typescript
// declare const jest: {
//   setTimeout(timeout: number): void;
// };
```

This will eliminate the redeclaration error and allow the tests to run normally. You can review the file here for reference (ref: 5caa398dea97cc081174437bd42e762c417b9b96):  
https://github.com/ripper7375/trading-alerts-saas-public/blob/5caa398dea97cc081174437bd42e762c417b9b96/frontend/jest.setup.ts
