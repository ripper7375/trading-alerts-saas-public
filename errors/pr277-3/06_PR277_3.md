The error is caused by redeclaring the jest variable in frontend/jest.setup.ts at line 9:

```
9| declare const jest: {
10|   setTimeout(timeout: number): void;
11| };
```

TypeScript already provides type definitions for Jest environments, so this redeclaration leads to TS2451: "Cannot redeclare block-scoped variable 'jest'." To solve the issue, remove or comment out lines 9–11 in frontend/jest.setup.ts. The updated code should look like:

```typescript
// Remove or comment out this block:
// declare const jest: {
//   setTimeout(timeout: number): void;
// };
```

You can see this in the job log and the file contents on ref 5caa398dea97cc081174437bd42e762c417b9b96. After making this change, re-run your workflow and the TS2451 error should be resolved.
