The job failed due to a TypeScript error in frontend/jest.setup.ts: TS2451: Cannot redeclare block-scoped variable 'jest'. This happens because jest is being redeclared where it's already available in the Jest environment.

To fix this, remove or comment out the following block in frontend/jest.setup.ts (lines 9–11):

```typescript
// declare const jest: {
//   setTimeout(timeout: number): void;
// };
```

Jest already provides the jest global, so this declaration is unnecessary and causes conflicts. After making this change, the job should proceed past this error.

You can review the current file state here for validation: frontend/jest.setup.ts (ref 5caa398dea97cc081174437bd42e762c417b9b96).
