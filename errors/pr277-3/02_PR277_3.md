The failing job is due to a TypeScript error in frontend/jest.setup.ts at line 9: Cannot redeclare block-scoped variable 'jest'.

Solution:
Remove or replace the line:

```typescript
declare const jest: {
  setTimeout(timeout: number): void;
};
```

TypeScript’s Jest types already declare the jest global. You do not need to redeclare it. Just remove lines 9-11 from the file:  
https://github.com/ripper7375/trading-alerts-saas-public/blob/5caa398dea97cc081174437bd42e762c417b9b96/frontend/jest.setup.ts#L9-L11

This will resolve the redeclaration error. The rest of your setup file is compatible with the Jest environment.
