The failing job (60727481025) failed because at least one of the individual test jobs ("Unit & Component Tests", "Integration Tests", or "Production Build Check") did not succeed, causing the summary step in .github/workflows/tests.yml (lines 169–176) to exit with code 1.

To fix this, you need to:

1. Identify which dependent job(s) failed: Check each of the test jobs:
   - unit-and-component-tests (lines 10–45)
   - integration-tests (lines 64–91)
   - build-check (lines 95–122)
2. Address their errors: The root cause is always upstream from the test-summary job. Review the logs from those jobs for specifics (failed tests, lint/type errors, or build issues).

A common solution approach:

- For unit/component or integration test failures (step 46, 91), run pnpm run test:ci or pnpm run test:integration locally, debug any errors, and fix failing tests or broken code.
- For build failures (step 122), run pnpm run build locally and resolve TypeScript or Next.js errors.
- For lint/type errors, run pnpm run lint or pnpm run type-check and address issues.

Once underlying issues in those jobs are fixed, the summary job will succeed. Check the logs of the upstream jobs in your workflow run for the specific error details, then fix those spots in your codebase or configuration.
