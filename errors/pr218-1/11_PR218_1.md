I'll analyze the failing job to find a solution for you. ## Solution for Failing Job

The TypeScript compilation is failing with two errors in `lib/db/postgresql.ts`:

### Issues:

1. **Missing type declarations for 'pg' module** (line 9)
2. **Incorrect property access for environment variable** (line 16)

### Fix:

**1. Install the missing type definitions:**

```bash
npm i --save-dev @types/pg
```

**2. Update line 16 in `lib/db/postgresql.ts`:**

Change:

```typescript
connectionString: process.env.POSTGRESQL_URI,
```

To:

```typescript
connectionString: process.env['POSTGRESQL_URI'],
```

### Complete corrected section (lines 15-16):

```typescript
pool = new Pool({
  connectionString: process.env['POSTGRESQL_URI'],
  max: 20,
```

The first error occurs because TypeScript can't find type definitions for the `pg` package. The second error is due to TypeScript's strict index signature rules requiring bracket notation for environment variable access.
