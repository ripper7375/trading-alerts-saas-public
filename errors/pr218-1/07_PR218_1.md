I'll analyze the failing job to find a solution for you. ## Solution for Failing Job

The TypeScript build is failing due to two issues in `lib/db/postgresql.ts`:

### Issue 1: Missing Type Definitions for `pg`

**Error:** Could not find a declaration file for module 'pg'

**Fix:** Install the type definitions for the `pg` package:

```bash
pnpm add -D @types/pg
```

### Issue 2: Index Signature Property Access

**Error:** Property 'POSTGRESQL_URI' must be accessed with bracket notation

**Fix:** Update line 16 in `lib/db/postgresql.ts`:

**Current code:**

```typescript
connectionString: process.env.POSTGRESQL_URI,
```

**Corrected code:**

```typescript
connectionString: process.env['POSTGRESQL_URI'],
```

### Complete Fix

1. Install the missing types:

```bash
pnpm add -D @types/pg
```

2. Update [line 16](https://github.com/ripper7375/trading-alerts-saas-public/blob/09718d3877f2e7a6211ceb4f281093400c83209a/lib/db/postgresql.ts#L16) in `lib/db/postgresql.ts`:

```typescript
function getPool(): Pool {
  if (!pool) {
    pool = new Pool({
      connectionString: process.env['POSTGRESQL_URI'], // Use bracket notation
      max: 20,
      idleTimeoutMillis: 30000,
      connectionTimeoutMillis: 2000,
    });
  }
  return pool;
}
```

These changes will resolve the TypeScript compilation errors and allow the CI build to pass.
