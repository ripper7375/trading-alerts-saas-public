The job failed due to a missing TypeScript type declaration for the pg package, as seen in lib/db/postgresql.ts. The error message suggests:

Type error: Could not find a declaration file for module 'pg'. Try npm i --save-dev @types/pg if it exists or add a new declaration (.d.ts) file containing declare module 'pg';

Solution:  
Run the following command in your project root to install the pg types:

```
npm install --save-dev @types/pg
```

This will resolve the missing type declaration error and allow your build to complete.

Reference:

- TypeScript import in postgresql.ts at ref 09718d3877f2e7a6211ceb4f281093400c83209a:
  import { Pool, PoolClient } from 'pg';
