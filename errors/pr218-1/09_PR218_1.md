I'll analyze the failing job to find a solution. Let me get the logs first. The job is failing because the **Type check** step failed. Let me look at the workflow file and the actual type check errors: ## Solution for Failing Job 59432533722

The **Type Check** step is failing. The logs show:

```
❌ Type check: FAILED
```

All other security checks (dependency audit, linting, secrets scan, security headers) are passing.

### Root Cause

The TypeScript type check (line 65-69 in the workflow) is encountering type errors when running `pnpm run type-check`.

### Solution

You need to fix the TypeScript type errors in your code. To identify the specific errors:

1. **Run the type check locally:**

   ```bash
   pnpm run type-check
   ```

2. **Review the type errors** that are reported. Common issues include:
   - Missing type annotations
   - Incorrect type assignments
   - Missing properties on objects
   - Incompatible function signatures

3. **Fix the type errors** based on what TypeScript reports. Without seeing the actual type check output, the specific errors aren't visible in these logs, but you can reproduce them locally.

### Quick Check

If your `package.json` doesn't have a `type-check` script, add it:

```json
{
  "scripts": {
    "type-check": "tsc --noEmit"
  }
}
```

Once you fix the TypeScript errors and push your changes, the workflow should pass.
