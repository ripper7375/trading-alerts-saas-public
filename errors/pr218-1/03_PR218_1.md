I'll analyze the failing job to find a solution for you. The build is failing because TypeScript cannot find type definitions for the `pg` module.

## Solution

Install the missing type definitions for the `pg` package:

```bash
pnpm add -D @types/pg
```

## Explanation

The error occurs at `./lib/db/postgresql.ts: 9:34` where you're importing from `pg`. TypeScript requires type definitions to properly type-check the code, and the `pg` package doesn't include them by default. The `@types/pg` package provides these TypeScript definitions.

After adding this package to your `devDependencies`, the TypeScript compiler will be able to properly type-check your PostgreSQL database code and the build should succeed.
