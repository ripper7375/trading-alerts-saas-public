The failing job is due to a TypeScript error in app/(dashboard)/dashboard/page.tsx at line 222: 'tierConfig' is possibly 'undefined'. This occurs because TIER_CONFIG[userTier] might return undefined if the userTier value doesn't match a key in TIER_CONFIG.

**Solution:** Add a fallback or guard to ensure tierConfig is defined before using its properties.

**Recommended code change:**

Replace:

```tsx
const tierConfig = TIER_CONFIG[userTier];
```

with:

```tsx
const tierConfig = TIER_CONFIG[userTier] ?? TIER_CONFIG['FREE'];
```

And for additional safety, before rendering the StatsCard components that use tierConfig, add a conditional check:

```tsx
if (!tierConfig) {
  throw new Error('Invalid user tier configuration');
}
```

Or, if you prefer non-fatal handling, render a message or skeleton if tierConfig is undefined.

Apply this change in app/(dashboard)/dashboard/page.tsx (see ref: 1820ccc468bc8c5ef8d4f167fde31cee6b9e957c).

This will resolve the build failure by ensuring that tierConfig is always defined when accessed.
