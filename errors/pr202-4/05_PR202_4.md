I'll analyze the failing job to find a solution for you. Now let me examine the failing test file and the API route it's testing: ## Solution for Failing Test

The test is failing because the API route is not properly catching and handling `MT5ServiceError`. The test expects the error message `"MT5 service error"` but receives `"Internal server error"` instead.

### Root Cause

Looking at lines 305-317 in the route handler, the MT5ServiceError handling checks the error's constructor name:

```typescript
if (error instanceof Error && error.constructor.name === 'MT5ServiceError') {
  return NextResponse.json(
    {
      success: false,
      error: 'MT5 service error',
      message: error.message || 'Failed to fetch data from MT5 service',
    } as ErrorResponse,
    { status: 500 }
  );
}
```

However, this code block is never being reached because the mocked error in the test (line 448) throws an instance of the mocked `MT5ServiceError` class, but the error handling logic may not be recognizing it properly due to how Jest mocks work.

### Fix

Update the error handling in **`app/api/indicators/[symbol]/[timeframe]/route.ts`** to check for the error properties rather than relying solely on constructor name:

```typescript
// Replace lines 304-317 with:
// Handle MT5ServiceError - check by constructor name AND check for statusCode property
if (
  error instanceof Error &&
  (error.constructor.name === 'MT5ServiceError' ||
    ('statusCode' in error && typeof (error as any).statusCode === 'number'))
) {
  return NextResponse.json(
    {
      success: false,
      error: 'MT5 service error',
      message: error.message || 'Failed to fetch data from MT5 service',
    } as ErrorResponse,
    { status: 500 }
  );
}

// Handle MT5AccessDeniedError - check by constructor name AND check for tier property
if (
  error instanceof Error &&
  (error.constructor.name === 'MT5AccessDeniedError' ||
    ('tier' in error && 'accessibleSymbols' in error))
) {
  return NextResponse.json(
    {
      success: false,
      error: 'Access denied',
      message: error.message || 'Access denied to this resource',
      upgradeRequired: true,
    } as ErrorResponse,
    { status: 403 }
  );
}
```

This approach checks for both the constructor name (for production) and specific properties that uniquely identify these error types (for test environments where Jest mocks may not preserve constructor names perfectly).
