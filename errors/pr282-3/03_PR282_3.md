I'll analyze the failing job to find a solution for you. The logs show that this is a summary job that failed because "One or more test jobs failed". Let me get the workflow file to understand the structure and identify which upstream jobs actually failed: ## Solution for Failing Test Summary Job

The **test-summary** job (60830227818) is failing because one or more of its dependent jobs failed or was skipped. The logs indicate that the condition triggered the failure, meaning at least one upstream job has a 'failure' status.

### Root Cause

The test-summary job depends on three jobs:

- `unit-and-component-tests`
- `integration-tests`
- `build-check`

You need to check which of these jobs actually failed to identify the root problem. The logs show jobs were skipped, which happens when an earlier dependency fails.

### Recommended Fix for the Workflow

The current condition (lines 170-173) only checks for `'failure'` status, but doesn't account for `'skipped'` jobs. Update the condition to check for non-success results:

```yaml
- name: Fail if any test failed
  if: |
    needs.unit-and-component-tests.result != 'success' ||
    needs.integration-tests.result != 'success' ||
    needs. build-check.result != 'success'
  run: |
    echo "❌ One or more test jobs failed"
    exit 1
```

This ensures the summary job properly fails when any upstream job is not successful (whether it failed, was cancelled, or skipped).

### Next Steps

1. **Check the actual failing job**: Look at the logs for `unit-and-component-tests` (the first job in the chain) as it likely contains the actual error
2. Apply the fix above to make the workflow more robust
3. Fix the root cause in the failing upstream job

Would you like me to retrieve the logs for the unit-and-component-tests job to identify the actual failure?
