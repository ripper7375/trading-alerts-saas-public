The failing job 60830161125 is caused by errors in tests for Stack B API client methods, specifically failures to resolve the module '@/lib/api' in test files (**tests**/integration/api-client-workflow.test.ts, **tests**/lib/api/stack-a-client.test.ts, and **tests**/lib/api/stack-b-client.test.ts). The error "Cannot find module '../../lib/api' from '**tests**/integration/api-client-workflow.test.ts'" points to one key problem: your test environment cannot resolve the alias @/lib/api because it's not correctly mapped in your Jest configuration.

**How to Fix:**

1. **Update Jest ModuleMapper (jest.config.js or jest.config.ts)**
   Add or update your `moduleNameMapper` to resolve aliases like `@/lib/api`:

   ```js
   module.exports = {
     // ...existing config,
     moduleNameMapper: {
       '^@/(.*)$': '<rootDir>/frontend/$1', // Adjust path if your src root isn't /frontend
     },
   };
   ```

2. **Check Project Structure**
   Make sure the import path matches your file structure. If `lib/api` is under `/frontend`, the above rule works; if it's under `/src`, adjust accordingly.

3. **Verify Your import Statements**
   All imports such as

   ```typescript
   import { api } from '@/lib/api';
   ```

   will resolve correctly as long as alias is correctly mapped.

4. **Re-run Tests**
   After configuring your Jest moduleNameMapper, the module resolution error should disappear and the API client test suites will run as expected.

**Code Suggestion Example** ([jest.config.js]):

```js
module.exports = {
  // ... other Jest config
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/frontend/$1', // or adjust as needed
  },
};
```

**Summary of What Will Improve:**

- StackAClient tests will pass if endpoints are available.
- StackBClient tests will still throw 404s by design until those endpoints are deployed, which is expected per test comments.

For more details, see:

- [**tests**/integration/api-client-workflow.test.ts (ref: b1d80e56316dd85a3c8b31efe21c5887a9109200)](https://github.com/ripper7375/trading-alerts-saas-public/blob/b1d80e56316dd85a3c8b31efe21c5887a9109200/__tests__/integration/api-client-workflow.test.ts)
- [frontend/lib/api/index.ts (ref: b1d80e56316dd85a3c8b31efe21c5887a9109200)](https://github.com/ripper7375/trading-alerts-saas-public/blob/b1d80e56316dd85a3c8b31efe21c5887a9109200/frontend/lib/api/index.ts)

This change directly addresses the job failures and will get your CI back to green for Stack A. Stack B failures are expected until those endpoints are implemented.
