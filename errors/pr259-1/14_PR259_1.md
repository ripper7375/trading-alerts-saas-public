**Failing Job Summary:**
Job 60353173702 in ripper7375/trading-alerts-saas-public failed with:  
`❌ One or more test jobs failed` & exited with code 1. According to the logs, both “Integration Tests” and “Build Check” were skipped, which caused the summary script to announce failure.

**Why Did This Happen?**

1. Either the workflow is misconfigured to skip tests/build steps under current conditions, or required artifacts/environment variables are missing.
2. It may occur if feature branches, environment settings, or PR event triggers are not recognized by the workflow file – typically found in `.github/workflows/tests.yml`.

**Solution Steps:**

1. **Review Workflow `tests.yml` Configuration:**  
   Check for conditional logic that causes test steps to be skipped, e.g., `if:` statements that filter on branch names, PR labels, or environmental variables.
   - Ensure that the trigger events match your PR or branch.
   - Validate any job dependencies or environment requirements.

2. **Make Sure Test/Build Steps Always Run (unless intentionally skipped):**  
   Example fix—remove or update narrow conditional skipping logic:

   ```yaml
   # In .github/workflows/tests.yml
   jobs:
     build-check:
       # Remove unnecessary conditional that may skip
       # if: github.event_name == 'push' && github.ref == 'refs/heads/main'
       ...
     integration-tests:
       # Likewise, revise any 'if:' statements
       ...
   ```

   Or, add fallback behavior so tests always execute for PRs:

   ```yaml
   jobs:
     integration-tests:
       if: github.event_name == 'pull_request' || github.event_name == 'push'
       ...
   ```

3. **Check Required Environment Variables and Artifacts:**  
   The README mentions environment variables (.env.local) for database, MT5, API keys, etc.
   - Confirm secrets are set in GitHub Actions settings for the repo.
   - If the workflow references these (e.g., `DATABASE_URL`, `NEXTAUTH_SECRET`, etc.), missing envs can cause steps to be skipped.
   - Example:
     ```yaml
     env:
       DATABASE_URL: ${{ secrets.DATABASE_URL }}
       NEXTAUTH_SECRET: ${{ secrets.NEXTAUTH_SECRET }}
     ```

4. **Ensure All Test and Build Scripts Exist and Are Invoked:**
   - Typical package scripts are: `npm test`, `npm run build`, etc.
   - The README references:
     ```
     npm run validate
     npm run fix
     npm test
     ```
   - Check the workflow calls these, and the underlying commands are present in `package.json`.

**Code Changes Example:**
Here is how you might revise `.github/workflows/tests.yml` to be more robust:

```yaml
# .github/workflows/tests.yml
name: Tests
on: [push, pull_request]

jobs:
  build-check:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm run build

  integration-tests:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npm install
      - run: npm test
```

**Summary:**

- Check and update skip logic in `.github/workflows/tests.yml`.
- Ensure required environment variables are set.
- Make sure scripts referenced in workflow actually exist.
- Always run validation and test steps explicitly for PRs.

If you need the exact job definition or want me to inspect the workflow file, please provide `.github/workflows/tests.yml` directly.
