I'll analyze the failing job to find a solution for you. I found the test failure. Let me examine the test file to understand the issue better: ## Solution for Failing Tests

The test suite has **9 failing tests**, with 2 main issues in the indicators API test file:

### Issue 1: MT5ServiceError Test Failure (Line 464)

**Location:** `__tests__/api/indicators. test.ts:464`

**Problem:** The test expects the error message to be `"MT5 service error"` but receives `"Internal server error"` instead.

**Root Cause:** The API route is catching `MT5ServiceError` but not returning the expected error message properly.

**Solution:** Update the error handling in the API route to return the correct error message:
