The job failed due to a TypeScript error in ./lib/db/postgresql.ts:

> Could not find a declaration file for module 'pg'. ...node_modules/pg/esm/index.mjs' implicitly has an 'any' type.

### Solution

**Install TypeScript types for the `pg` module:**

Run the following in your project directory:

```bash
npm install --save-dev @types/pg
```

If `@types/pg` does not exist, add a custom module declaration. Create a file (e.g., `pg.d.ts`) in your project (commonly inside a `types/` folder or root):

```typescript
// types/pg.d.ts
declare module 'pg';
```

### Code Reference

- The error occurs at line 10 in [`lib/db/postgresql.ts`](https://github.com/ripper7375/trading-alerts-saas-public/blob/dd4f8261991b268afb77c9ed98d1d478107a1324/lib/db/postgresql.ts).

#### Summary

1. Try: `npm install --save-dev @types/pg`
2. If not available, add `declare module 'pg';` to a .d.ts file

This should resolve the type error and allow `next build` to succeed. Let me know if you hit another error.
