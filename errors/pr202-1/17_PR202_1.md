I'll analyze the failing job to find a solution for you. ## Solution for Failing Job 59404703208

The build is failing due to **TypeScript errors** in `lib/db/postgresql.ts`. There are two issues:

### Issue 1: Missing Type Definitions for `pg`

**Error:** `Could not find a declaration file for module 'pg'`

**Solution:** Install the type definitions for the `pg` package:

```bash
pnpm add -D @types/pg
```

### Issue 2: TypeScript Index Signature Access (TS4111)

**Errors on line 23:**

- `Property 'POSTGRESQL_URI' comes from an index signature, so it must be accessed with ['POSTGRESQL_URI']`
- `Property 'DATABASE_URL' comes from an index signature, so it must be accessed with ['DATABASE_URL']`

**Solution:** Update line 23 to use bracket notation for accessing environment variables:

```typescript
const poolConfig: PoolConfig = {
  connectionString:
    process.env['POSTGRESQL_URI'] || process.env['DATABASE_URL'],
  max: 20,
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 2000,
};
```

### Complete Fix

1. **Install the missing types:**

   ```bash
   pnpm add -D @types/pg
   ```

2. **Update `lib/db/postgresql.ts` line 23:**
   ```typescript
   const poolConfig: PoolConfig = {
     connectionString:
       process.env['POSTGRESQL_URI'] || process.env['DATABASE_URL'],
     max: 20,
     idleTimeoutMillis: 30000,
     connectionTimeoutMillis: 2000,
   };
   ```

These changes will resolve all TypeScript compilation errors and allow the build to pass.
