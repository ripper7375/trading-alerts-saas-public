I'll analyze the failing job to find a solution for you. Let me check the problematic file to understand the issues better: ## Solution for Failing Job

The TypeScript build is failing due to two issues in `lib/db/postgresql.ts`:

### Issue 1: Missing Type Definitions for 'pg'

**Error:** Could not find a declaration file for module 'pg'

**Solution:** Install the type definitions for the `pg` package:

```bash
pnpm add -D @types/pg
```

### Issue 2: Index Signature Access for Environment Variables

**Errors on line 23:**

- Property 'POSTGRESQL_URI' must be accessed with ['POSTGRESQL_URI']
- Property 'DATABASE_URL' must be accessed with ['DATABASE_URL']

**Solution:** Update line 23 to use bracket notation for accessing `process.env`:

```typescript
const poolConfig: PoolConfig = {
  connectionString:
    process.env['POSTGRESQL_URI'] || process.env['DATABASE_URL'],
  max: 20,
  idleTimeoutMillis: 30000,
  connectionTimeoutMillis: 2000,
};
```

### Complete Fix

1. Add the type definitions to your `package.json`:

   ```bash
   pnpm add -D @types/pg
   ```

2. Update [line 23](https://github.com/ripper7375/trading-alerts-saas-public/blob/dd4f8261991b268afb77c9ed98d1d478107a1324/lib/db/postgresql.ts#L23) in `lib/db/postgresql.ts`:
   ```typescript
   connectionString: process.env['POSTGRESQL_URI'] || process. env['DATABASE_URL'],
   ```

These changes will resolve the TypeScript compilation errors and allow the CI build to pass.
