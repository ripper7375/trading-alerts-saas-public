I'll analyze the failing job to find a solution for you. The build is failing due to a TypeScript error in `lib/indicators/timeframe-filter. ts` at line 201. The error states that `divisor` is possibly `undefined`. Let me check the file to see the exact issue: ## Solution

The TypeScript compilation is failing at line 201 in `lib/indicators/timeframe-filter. ts` because the `divisor` variable is possibly `undefined`. The function `getNextTimeframeBoundary` retrieves the divisor but doesn't check if it exists before using it.

**The Issue:**

```typescript
export function getNextTimeframeBoundary(
  timeframe: string,
  referenceTime?: Date
): number {
  const current = getLatestTimeframeTimestamp(timeframe, referenceTime);
  const divisor = TIMEFRAME_DIVISORS[timeframe.toUpperCase()];

  // Add the divisor in seconds
  return current + divisor * 60; // ❌ divisor might be undefined
}
```

**Fix:**
Add a check to handle the case where the divisor is undefined, similar to how other functions in the file handle it:

```typescript
export function getNextTimeframeBoundary(
  timeframe: string,
  referenceTime?: Date
): number {
  const current = getLatestTimeframeTimestamp(timeframe, referenceTime);
  const upperTimeframe = timeframe.toUpperCase();
  const divisor = TIMEFRAME_DIVISORS[upperTimeframe];

  if (divisor === undefined) {
    throw new Error(`Unknown timeframe: ${timeframe}`);
  }

  // Add the divisor in seconds
  return current + divisor * 60;
}
```

This matches the error handling pattern used in `getLatestTimeframeTimestamp` (line 156-158) and ensures TypeScript knows the divisor is defined before using it.
