I'll analyze the failing job to identify the issue and provide a solution. Now let me examine the failing file: ## Solution

The build is failing due to a TypeScript error on line 201 in `lib/indicators/timeframe-filter. ts`. The issue is that `divisor` could be `undefined`, but the code doesn't handle this case before using it in the calculation.

### Problem

At line 198, `divisor` is retrieved from `TIMEFRAME_DIVISORS`, which can return `undefined` if the timeframe is invalid. However, line 201 uses `divisor` in a calculation without checking if it exists first.

### Fix

Add a check for `undefined` before using the divisor. Here's the corrected code for the `getNextTimeframeBoundary` function:

```typescript
export function getNextTimeframeBoundary(
  timeframe: string,
  referenceTime?: Date
): number {
  const current = getLatestTimeframeTimestamp(timeframe, referenceTime);
  const divisor = TIMEFRAME_DIVISORS[timeframe.toUpperCase()];

  if (divisor === undefined) {
    throw new Error(`Unknown timeframe: ${timeframe}`);
  }

  // Add the divisor in seconds
  return current + divisor * 60;
}
```

This matches the error handling pattern already used in other functions in the same file (like `getLatestTimeframeTimestamp` on line 156-158), ensuring consistency and proper type safety.
