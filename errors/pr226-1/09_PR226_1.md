I'll analyze the failing job to find a solution. Let me get the logs first. ## Solution

The TypeScript compiler is failing at **line 201** in `lib/indicators/timeframe-filter. ts` because the variable `divisor` is possibly `undefined`.

### The Problem

In the `getNextTimeframeBoundary` function (lines 193-202), you're accessing `TIMEFRAME_DIVISORS[timeframe.toUpperCase()]` on line 198, which can return `undefined` for invalid timeframes. However, you're then using `divisor` on line 201 without checking if it's defined first.

### The Fix

Add a check for `undefined` after retrieving the divisor. Here's the corrected code:

```typescript
export function getNextTimeframeBoundary(
  timeframe: string,
  referenceTime?: Date
): number {
  const current = getLatestTimeframeTimestamp(timeframe, referenceTime);
  const divisor = TIMEFRAME_DIVISORS[timeframe.toUpperCase()];

  if (divisor === undefined) {
    throw new Error(`Unknown timeframe: ${timeframe}`);
  }

  // Add the divisor in seconds
  return current + divisor * 60;
}
```

This matches the pattern you're already using in other functions like `getLatestTimeframeTimestamp` (line 156) and `filterByTimeframe` (line 84), making the codebase consistent and type-safe.
