I'll analyze the failing job to find a solution. Let me get the logs first. The job failed due to a TypeScript error. Let me fetch the file to see the issue: ## Solution

The TypeScript compiler is failing because the `divisor` variable on **line 201** is possibly `undefined`, which occurs when an invalid timeframe is passed to `getNextTimeframeBoundary()`.

### The Issue

In the `getNextTimeframeBoundary` function (lines 193-202), the code retrieves `divisor` from `TIMEFRAME_DIVISORS` but doesn't check if it's undefined before using it in the calculation on line 201.

### Fix

Add a validation check after retrieving the divisor:

```typescript
export function getNextTimeframeBoundary(
  timeframe: string,
  referenceTime?: Date
): number {
  const current = getLatestTimeframeTimestamp(timeframe, referenceTime);
  const upperTimeframe = timeframe.toUpperCase();
  const divisor = TIMEFRAME_DIVISORS[upperTimeframe];

  if (divisor === undefined) {
    throw new Error(`Unknown timeframe: ${timeframe}`);
  }

  // Add the divisor in seconds
  return current + divisor * 60;
}
```

**Changes:**

1. Store `timeframe. toUpperCase()` in a variable (line 197)
2. Use the uppercase version when accessing `TIMEFRAME_DIVISORS` (line 198)
3. Add an undefined check similar to the pattern used in `getLatestTimeframeTimestamp` (lines 199-201)

This ensures TypeScript knows that `divisor` is a number when it reaches line 201, and provides a clear error message if an invalid timeframe is provided.
