The failure is due to TypeScript errors in app/api/test/seed/route.ts:

1. Line 70: 
   - Error TS4111: Property 'TEST_SECRET' comes from an index signature, so it must be accessed with ['TEST_SECRET'].
   - Solution: Replace object.TEST_SECRET with object['TEST_SECRET'] on line 70.

   Example fix:
   ```ts
   // Before
   const secret = env.TEST_SECRET;

   // After
   const secret = env['TEST_SECRET'];
   ```

2. Line 173:
   - Error TS6133: 'request' is declared but its value is never read.
   - Solution: Remove the unused 'request' parameter or prefix it with an underscore.

   Example fix:
   ```ts
   // Before
   async function handler(request: Request) {

   // After
   async function handler(_request: Request) {
   // or just remove 'request' entirely if not needed
   }
   ```

Apply these changes in app/api/test/seed/route.ts to resolve the type checking errors and fix the CI failure.