The job failure is primarily due to two issues:

1. **React Testing Error:**
   - The logs repeatedly state:  
     `An update to PlanSelector inside a test was not wrapped in act(...).`
   - This means your React test code is missing `act()` when updating components asynchronously during tests. Jest and React Testing Library require state updates (e.g., when using SWR or async effects) to be wrapped in `act()` to ensure results are reliable.

**Solution for #1:**

- Find your test files that use `<PlanSelector />`.
- Whenever you trigger an update in your test (e.g., click, change state, async fetch), wrap it like this:

  ```js
  import { act } from 'react-dom/test-utils';

  await act(async () => {
    // trigger updates (e.g., render, fire events)
    render(<PlanSelector />);
    // or userEvent.click(...), etc.
  });
  ```

- For component tests using `@testing-library/react`, also ensure async updates are awaited:
  ```js
  await act(async () => {
    // await fireEvent or userEvent actions, then assertions
  });
  ```

2. **Environment Variable Issue (`CRON_SECRET not configured`):**
   - The test fails at this code line:
     ```
     if (!cronSecret) {
       console.error('CRON_SECRET not configured');
       return NextResponse.json(
         { error: 'Cron job not configured' },
         { status: 500 }
       );
     }
     ```
   - This means the required environment variable (`CRON_SECRET`) is missing during test execution.

**Solution for #2:**

- In your test setup or CI configuration, provide the required environment variable.
- For local testing, add this to `.env.local`:
  ```env
  CRON_SECRET=your-secret-value
  ```
- For GitHub Actions:
  ```yaml
  env:
    CRON_SECRET: ${{ secrets.CRON_SECRET }}
  ```
- If you need to pass a header in tests, double-check your test stubs/mock for the secret header.

---

**Summary of Actions:**

1. Update your React test code to wrap state updates in `act()`, especially for `<PlanSelector />`.
2. Set the `CRON_SECRET` environment variable for tests and CI.
3. Rerun the job after making these fixes.

Let me know if you want the exact test fixture or action file for these changes.
