The job is failing due to **schema/count mismatches in `lib/tier/constants.ts` and its downstream access-control helpers**. Your unit tests expect the **“61-column schema”** (9 system + 52 indicator columns), but the current backend constants file is defining a **63-column schema** (it even says “63-Column Database Schema” and “54 indicator columns”), which makes:

- `getAccessibleColumns('PRO')` return **63**, not **61** → breaks `lib/tier/__tests__/validator.test.ts` (expects 61, and no locked columns for PRO).
- `INDICATOR_METADATA.body_momentum.columns` have **3** columns, but the test expects **2** (`body_size`, `body_direction`) → breaks `lib/tier/__tests__/constants.test.ts`.

## Fix: align `lib/tier/constants.ts` to the test’s 61-column contract

### 1) Make `body_momentum` exactly 2 columns

Update `INDICATOR_METADATA.body_momentum.columns` to remove `body_classification`.

**File:** `lib/tier/constants.ts` @ `a4f3fb979fe172de4104fe8a5a41f6293efdd73d`  
https://github.com/ripper7375/trading-alerts-saas-public/blob/a4f3fb979fe172de4104fe8a5a41f6293efdd73d/lib/tier/constants.ts

Change:

```ts
body_momentum: {
  // ...
  columns: ['body_size', 'body_direction', 'body_classification'],
  // ...
},
```

to:

```ts
body_momentum: {
  // ...
  columns: ['body_size', 'body_direction'],
  // ...
},
```

This directly satisfies the failing assertion in:

- `lib/tier/__tests__/constants.test.ts` (expects exactly 2)

### 2) Reduce PRO indicator column total from 38 → 36 (and overall from 54 → 52)

Your tests expect:

- PRO-only indicator columns total = **36** (`constants.test.ts` line ~183-189)
- All indicator columns total = **52**
- PRO tier total columns = **61** (9 system + 52 indicators)

Right now `lib/tier/constants.ts` defines extra columns in multiple places (comment + metadata indicate expanded schemas):

- `body_momentum` currently adds +1 extra (fixed above)
- `heiken_ashi` is currently **8** columns in backend constants, but the test expects **7**
  - Test expects: “heiken_ashi should have exactly 7 columns” (`constants.test.ts` line ~131-136)
- There may also be expanded keltner naming in backend (it’s already 10 which matches tests, but ensure names match what tests expect)

So also update `heiken_ashi` metadata to 7 columns (matching the test suite’s contract).

Change:

```ts
heiken_ashi: {
  // ...
  columns: [
    'ha_open',
    'ha_high',
    'ha_low',
    'ha_close',
    'ha_color',
    'ha_trend',
    'ha_body_size',
    'ha_body_zscore',
  ],
  // ...
},
```

to (example that matches test’s “7 columns”; pick the correct 7 your API/db actually uses for the 61-column schema):

```ts
heiken_ashi: {
  // ...
  columns: [
    'ha_open',
    'ha_high',
    'ha_low',
    'ha_close',
    'ha_color',
    'ha_trend',
    'ha_strength', // or whatever the 7th column is in your 61-col schema
  ],
  // ...
},
```

Important: the test doesn’t currently assert exact names for all 7, only that it contains `ha_open` and `ha_color`, but your code _does_ depend on column names for access control. So make sure the 7th entry is the canonical one for the backend schema.

### 3) Update the header comments + tier count helper to match 61-column expectations

Your file header and `getTierColumnCount()` currently describe 63 columns:

```ts
// PRO: 9 system + 54 indicator = 63 columns
```

After removing the extra columns, ensure `getTierColumnCount()` computes 61 and the comments match. The function already sums dynamically, so once `INDICATOR_METADATA` is corrected, it will return the correct number—but update the comments to avoid future confusion.

## Why this fixes the failing validator test

`lib/tier/validator.ts` builds accessible columns by concatenating `SYSTEM_COLUMNS` + indicator columns from `INDICATOR_METADATA`:

- `getAccessibleColumns('PRO')` iterates `ALL_INDICATORS` and pushes `INDICATOR_METADATA[indicator].columns`  
  https://github.com/ripper7375/trading-alerts-saas-public/blob/a4f3fb979fe172de4104fe8a5a41f6293efdd73d/lib/tier/validator.ts#L137-L153

Once `INDICATOR_METADATA` totals 52 indicator columns (and system stays 9), `getAccessibleColumns('PRO')` will return **61**, matching:

- `lib/tier/__tests__/validator.test.ts` expectation: PRO tier should get 61 columns (and locked columns list should be empty).

## Note (non-blocking for this job)

The Codecov step is hitting a 429 rate limit but is configured to exit 0 (“Codecov will exit with status code 0…”). It’s noisy but **not the cause of the job failure**—the failing Jest tests are.
