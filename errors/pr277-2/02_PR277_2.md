The failing job is caused by a type error in frontend/jest.setup.ts on line 11, where the polyfill for clearImmediate is defined as:

```typescript
global.clearImmediate = function (id: NodeJS.Timeout) {
  return clearTimeout(id);
};
```

The error message indicates that the function signature for clearImmediate is expected to be (immediate: Immediate | undefined) => void, not (id: Timeout) => void.

To fix this, update the signature and usage as follows:

```typescript
global.clearImmediate = function (immediate?: NodeJS.Immediate) {
  return clearTimeout(immediate as unknown as number);
};
```

Or, more accurately, for compatibility with both Timeout and Immediate types, you might want:

```typescript
global.clearImmediate = function (immediate?: any) {
  return clearTimeout(immediate);
};
```

Modify the definition of clearImmediate in frontend/jest.setup.ts to match the expected types. This will resolve the type error and allow the build to complete successfully.

Relevant file: frontend/jest.setup.ts (ref: f7f4baac2e7ae9c880a1b2a76b1957fc4957d092)
