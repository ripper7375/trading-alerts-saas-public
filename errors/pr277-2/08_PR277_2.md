The failing job is caused by TypeScript type errors in frontend/jest.setup.ts:

1. The polyfilled clearImmediate expects a NodeJS.Timeout, but setImmediate expects Immediate. Their types are incompatible:

   ```typescript
   global.clearImmediate = function (id: NodeJS.Timeout) { ... }
   ```

   Should be:

   ```typescript
   global.clearImmediate = function (immediate: NodeJS.Immediate | undefined) {
     if (immediate) clearTimeout(immediate as unknown as number);
   };
   ```

   Or you may need to align types using:

   ```typescript
   global.clearImmediate = function (immediate: any) {
     if (immediate) clearTimeout(immediate);
   };
   ```

2. The setImmediate polyfill is missing the **promisify** property required by Node.js definitions. Update it as follows:

   ```typescript
   global.setImmediate = Object.assign(
     function (callback: (...args: any[]) => void, ...args: any[]) {
       return setTimeout(callback, 0, ...args);
     },
     { __promisify__: undefined }
   );
   ```

3. performance.markResourceTiming does not exist. If you need a strict mock, the TypeScript type for performance may require extension via declaration merging:
   ```typescript
   declare global {
     interface Performance {
       markResourceTiming?: (...args: any[]) => void;
     }
   }
   global.performance.markResourceTiming = function () {};
   ```

Apply these changes to frontend/jest.setup.ts to resolve the TypeScript errors. You can review and edit the file here:  
[frontend/jest.setup.ts at f7f4baac2e7ae9c880a1b2a76b1957fc4957d092](https://github.com/ripper7375/trading-alerts-saas-public/blob/f7f4baac2e7ae9c880a1b2a76b1957fc4957d092/frontend/jest.setup.ts)
