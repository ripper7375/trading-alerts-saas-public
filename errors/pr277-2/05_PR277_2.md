The failing job 60726506211 in your workflow is due to the "TypeScript Type Check" step failing. According to the job logs, the summary step reports:

❌ Type check: FAILED

Solution:

- Check the output of pnpm run type-check locally or in the logs for details on the type errors causing the failure.
- Resolve all reported type errors in your TypeScript codebase.

Here’s how to identify and fix:

1. Run pnpm run type-check on your development machine.
2. Review the errors displayed.
3. Correct the type errors in your .ts or .tsx files.
4. Commit and push your changes.

Relevant workflow step:

```yaml
- name: Run TypeScript type check
  run: |
    echo "🔍 Running TypeScript type check..."
    pnpm run type-check
    echo "✅ No type errors found"
```

Make sure the type check passes locally before pushing. Once you fix all TypeScript errors, your job should succeed. For more details, view the workflow definition: .github/workflows/security-checks.yml (ref: f7f4baac2e7ae9c880a1b2a76b1957fc4957d092).
