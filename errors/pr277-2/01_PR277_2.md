The job failed due to a type incompatibility in frontend/jest.setup.ts at line 11:

Type error: Type '(id: Timeout) => void' is not assignable to type '(immediate: Immediate | undefined) => void'.

Solution:
The Node.js globals setImmediate/clearImmediate handle Immediate objects, not Timeout. Your polyfill uses NodeJS.Timeout, which causes the type error. To resolve this, adjust the clearImmediate polyfill to accept the correct type:

```typescript
if (typeof global.clearImmediate === 'undefined') {
  global.clearImmediate = function (immediate?: NodeJS.Immediate) {
    return clearTimeout(immediate as any);
  };
}
```

Or, if you're in a browser-like (jsdom) environment, you may want to use setTimeout/clearTimeout but with the correct typing to match Immediate | undefined.

You can update the code to:

```typescript
if (typeof global.clearImmediate === 'undefined') {
  global.clearImmediate = function (immediate?: any) {
    return clearTimeout(immediate);
  };
}
```

This resolves the type mismatch in the polyfill. See the affected file here.
