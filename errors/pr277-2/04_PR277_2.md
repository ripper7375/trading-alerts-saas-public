The failing job is caused by TypeScript type mismatches and missing properties in your polyfills in frontend/jest.setup.ts:

1. clearImmediate polyfill (line 11):  
   The function signature should match the Node.js global, which expects an argument of type Immediate | undefined, not Timeout.  
   Update the polyfill to:
   ```typescript
   global.clearImmediate = function (immediate?: NodeJS.Immediate) {
     clearTimeout(immediate as any);
   };
   ```
2. setImmediate polyfill (line 18):  
   The polyfill is missing the required **promisify** property.  
   Update to:
   ```typescript
   const setImmediatePolyfill = function (
     callback: (...args: any[]) => void,
     ...args: any[]
   ) {
     return setTimeout(callback, 0, ...args);
   };
   (setImmediatePolyfill as any).__promisify__ = true;
   global.setImmediate = setImmediatePolyfill;
   ```
3. performance.markResourceTiming mock (lines 25-26):  
   The correct property might not be available; ensure you’re targeting the right global (jsdom’s globalThis or window.performance). If TypeScript still complains, cast as any:
   ```typescript
   (global.performance as any).markResourceTiming = function () {};
   ```

Apply these changes to frontend/jest.setup.ts on the f7f4baac2e7ae9c880a1b2a76b1957fc4957d092 ref to resolve the build errors. See source file: frontend/jest.setup.ts.
