The failing job is caused by TypeScript/ESLint errors due to unused variables in your codebase. Here are the issues and their solutions:

**1. app/(dashboard)/alerts/new/create-alert-client.tsx**

- Error: 'ConditionType' is defined but never used.
- Solution: Remove or comment out the `type ConditionType = (typeof CONDITION_TYPES)[number]['value'];` line if it's not referenced elsewhere.

**2. app/(dashboard)/settings/account/page.tsx**

- Error: 'Laptop' and 'Smartphone' are defined but never used.
- Solution: Remove these imports from the lucide-react import:
  ```tsx
  // change this:
  import {
    Eye,
    EyeOff,
    Lock,
    Laptop, // Remove this
    Smartphone, // Remove this
    Monitor,
    AlertTriangle,
    Loader2,
    Check,
    Shield,
  } from 'lucide-react';
  ```

**3. app/api/indicators/[symbol]/[timeframe]/route.ts**

- Errors: 'z', 'isValidSymbol', 'isValidTimeframe' are defined but never used.
- Solution: Remove these unused function and import:
  - Remove the `import { z } from 'zod';` line if only used for the unused variables.
  - Remove `function isValidSymbol(symbol: string)`, and `function isValidTimeframe(timeframe: string)` if not referenced.
- Errors: 'upperSymbol' and 'upperTimeframe' are never reassigned. Use 'const' instead of 'let'.
  - Solution: Change variable declarations:
    ```typescript
    const upperSymbol = symbolResult.data;
    const upperTimeframe = timeframeResult.data;
    ```

**4. components/ui/pagination.tsx**

- Error: 'showEndButtons' is assigned a value but never used in StandardPagination.
- Solution: Remove the `showEndButtons` prop if it’s not implemented, or use it as intended.

**Summary of Fixes**

- Remove unused variable definitions/imports.
- Change `let` to `const` for variables never reassigned.
- Remove or use any unused props in component files.

After making these changes, re-run your build and linting. These adjustments will resolve the current CI failure.

You can see the affected files on ref b9087f06ef5cb5e8136dc9335657399736f5aa54.
