The job failed due to TypeScript errors caused by declared variables that are never used. Here’s how to fix each error:

1. Remove or use the declared but unused identifiers. Specifically, update the following files:

- app/(dashboard)/alerts/new/create-alert-client.tsx: Remove or use 'ConditionType' (line 56).
- app/(dashboard)/settings/account/page.tsx: Remove or use 'Laptop' (line 12) and 'Smartphone' (line 13).
- app/api/indicators/[symbol]/[timeframe]/route.ts: Remove or use 'z' (line 19), 'isValidSymbol' (line 95), and 'isValidTimeframe' (line 102).
- components/ui/pagination.tsx: Remove or use 'showEndButtons' (line 225).
- hooks/use-unsaved-changes.ts: Remove or use 'router' (line 64).

Sample code suggestions:

- Remove unused import or variable, e.g.:

```ts
// Before:
import { ConditionType } from '...';
// After (if unused):
// import { ConditionType } from "...";
```

- Or, if it’s a variable:

```ts
// Before:
const Laptop = ...;
// After (if unused):
// const Laptop = ...;
```

Alternatively, use them if needed.

After making these changes, commit and push. The TypeScript compile step should then pass, resolving the job failure.

Relevant lines from the logs:

- Error TS6196: 'ConditionType' is declared but never used.
- Error TS6133: '[var]' is declared but its value is never read (var = Laptop, Smartphone, z, isValidSymbol, isValidTimeframe, showEndButtons, router).

Let me know if you want exact code lines.
