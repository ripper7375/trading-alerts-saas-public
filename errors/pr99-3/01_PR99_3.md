The failing test is test_free_tier_denied_timeframe. The error is in this assertion:

```python
assert 'requires PRO tier' in error
```

But the actual error message is:

```
'Timeframe M5 requires PRO. Upgrade for all timeframes.'
```

To fix this, update the assertion in your test to match the actual message. Change this line (in mt5-service/tests/test_models.py):

```python
assert 'requires PRO tier' in error
```

to

```python
assert 'requires PRO' in error
```

Or, if you want to keep a stricter assertion, match the new string:

```python
assert 'requires PRO. Upgrade for all timeframes.' in error
```

This will resolve the failing job. You can view the test file here:  
https://github.com/ripper7375/trading-alerts-saas-public/blob/d977f40e80fa062ff6d3c8ace1dcf1e61aaab5e3/mt5-service/tests/test_models.py
