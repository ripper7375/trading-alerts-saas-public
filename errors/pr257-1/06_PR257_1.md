The job failed due to several issues in lib/websocket/use-mt5-websocket.ts:

1. Cannot find module 'socket.io-client' or its corresponding type declarations.
2. Cannot find module '@/types/indicators' or its corresponding type declarations.
3. TypeScript parameters implicitly have 'any' type in several places (lines 125, 142, 161, 165).

Here's how to resolve them:

**1. Install missing dependencies and types**

- Install socket.io-client and its types:
  ```sh
  npm install socket.io-client
  npm install --save-dev @types/socket.io-client
  ```
- Ensure that '@types/indicators' exists at the expected path (src/types/indicators.ts or lib/types/indicators.ts). If this is an alias, verify your tsconfig.json has:
  ```json
  {
    "compilerOptions": {
      "baseUrl": ".",
      "paths": {
        "@/*": ["./*"]
      }
    }
  }
  ```

**2. Add explicit types for parameters:**

Edit lib/websocket/use-mt5-websocket.ts (ref: 4fd50f10cdc3ddc18c666b459150313194917897):

```typescript
socket.on('disconnect', (reason: string) => { ... })  // Line 125
socket.on('connect_error', (error: Error) => { ... }) // Line 142
socket.on('subscribed', (data: any) => { ... })       // Line 161
socket.on('unsubscribed', (data: any) => { ... })     // Line 165
```

Or, for stricter typing, define custom types if possible.

**Summary of actions:**

- Install missing packages and type definitions.
- Add or fix your `@/types/indicators` import or path alias.
- Add missing parameter types on indicated lines to resolve implicit 'any' errors.

After applying these changes, commit and push your updates, and rerun the workflow to verify the fix.
