The job failed due to missing dependencies and TypeScript errors in lib/websocket/use-mt5-websocket.ts:

1. Module not found: socket.io-client and @/types/indicators
   - Solution: Install socket.io-client and ensure types are available. Run:
     ```
     pnpm add socket.io-client
     pnpm add -D @types/socket.io-client
     ```
     For @/types/indicators, verify this alias and file exist in your codebase. If it’s missing, create the file or fix the import path.

2. TypeScript errors (implicit 'any' for parameters: reason, error, data):
   - Solution: Add explicit type annotations. For example:
     ```typescript
     socket.on('disconnect', (reason: string) => { ... });
     socket.on('connect_error', (error: Error) => { ... });
     socket.on('subscribed', (data: any) => { ... });
     socket.on('unsubscribed', (data: any) => { ... });
     ```
     Adjust the types based on your actual data structure.

After applying these fixes, rerun your tests. You can review the problematic file here (ref 4fd50f10cdc3ddc18c666b459150313194917897):  
https://github.com/ripper7375/trading-alerts-saas-public/blob/4fd50f10cdc3ddc18c666b459150313194917897/lib/websocket/use-mt5-websocket.ts
