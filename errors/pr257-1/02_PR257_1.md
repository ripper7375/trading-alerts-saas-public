The job failed due to a missing dependency: "Cannot find module 'socket.io-client' or its corresponding type declarations" in lib/websocket/use-mt5-websocket.ts (line 23). To resolve this:

1. Install socket.io-client and its TypeScript types:
   ```bash
   npm install socket.io-client
   npm install --save-dev @types/socket.io-client
   ```
2. Commit and push the changes, then re-run the workflow.

This will provide the required module and type declarations, allowing your build to succeed. The relevant import is:

```typescript
import { io, Socket } from 'socket.io-client';
```
