The failure occurred because one or more test jobs did not pass in your GitHub Actions workflow. The log shows this summary step exited with code 1, triggering a job failure: "❌ One or more test jobs failed".

To resolve this:

1. Identify and fix the root failing job from these three in .github/workflows/tests.yml (ref: 4fd50f10cdc3ddc18c666b459150313194917897):
   - unit-and-component-tests
   - integration-tests
   - build-check

Check their detailed logs to see which step or command failed.

2. Typical issues and solutions:
   - For test failures (pnpm run test:ci or pnpm run test:integration): Run these locally to see specific test errors. Fix failing tests or update test data/code as needed.
   - For build problems (pnpm run build or bundle size check): Ensure your project builds without errors; reduce the .next/ output (bundle) size if it exceeds limits (see steps 127-150).

Code suggestion (for debugging): Run the following locally and check for errors, then resolve:

```bash
pnpm install --frozen-lockfile
pnpm run type-check
pnpm run lint
pnpm run test:ci
pnpm run test:integration
pnpm run build
```

After fixing identified errors, push your changes to trigger the workflow again. Review the individual job logs for more specific issues if failures persist.
