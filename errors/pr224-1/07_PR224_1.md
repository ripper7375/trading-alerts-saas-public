I'll analyze the failing job to find a solution for you. Now let me examine the failing test files to understand the issues better: ## Solution for Failing Tests

The job is failing due to issues in two test files. Here are the specific problems and fixes:

### 1. **indicator-cache.test.ts** - Line 221 & 412

**Problem:** `getCacheSize()` is returning an object instead of a number, causing type errors in assertions.

**Fix:** The `getCacheSize()` function needs to return a number. Check the implementation in `@/lib/cache/indicator-cache`:

```typescript
// Current (incorrect) - likely returning an object
export function getCacheSize() {
  return cache; // or cache.size object
}

// Should be:
export function getCacheSize(): number {
  return cache.size; // or cache.length depending on implementation
}
```

### 2. **indicator-cache. test.ts** - Line 523

**Problem:** Test expects different data when using optional `bars` parameter, but the cache key might not differentiate between calls with and without the parameter.

**Fix:** Ensure the cache key generation includes the `bars` parameter properly:

```typescript
// In indicator-cache.ts
function getCacheKey(symbol: string, timeframe: string, bars?: number): string {
  const normalizedSymbol = symbol.toUpperCase();
  const normalizedTimeframe = timeframe.toUpperCase();
  // Make sure to include bars in the key, use a default or "default" string when undefined
  const barsKey = bars !== undefined ? bars.toString() : 'default';
  return `${normalizedSymbol}: ${normalizedTimeframe}:${barsKey}`;
}
```

### 3. **indicators.test.ts** - Line 464

**Problem:** Test expects error message `"MT5 service error"` but receives `"Internal server error"`.

**Fix:** Update the error handling in `@/app/api/indicators/[symbol]/[timeframe]/route.ts` to properly catch and handle `MT5ServiceError`:

```typescript
// In the route handler's catch block
import { MT5ServiceError, MT5AccessDeniedError } from '@/lib/api/mt5-client';

try {
  // ... existing code
} catch (error) {
  if (error instanceof MT5ServiceError) {
    return NextResponse.json(
      { success: false, error: 'MT5 service error' },
      { status: 500 }
    );
  }

  if (error instanceof MT5AccessDeniedError) {
    // ... existing MT5AccessDeniedError handling
  }

  // Generic error handling
  return NextResponse.json(
    { success: false, error: 'Internal server error' },
    { status: 500 }
  );
}
```

These fixes should resolve all 39 failing tests in the test suite.
