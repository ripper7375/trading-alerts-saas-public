I'll analyze the failing job to find a solution for you. The job is failing because both the **Type check** and **Linting** steps are failing. Let me get more detailed logs from the actual type check and linting steps to see the specific errors: The job is failing because **two security checks are failing**: Type check and Linting.

## Root Cause

Based on the logs, the security checks workflow shows:

- ✅ Dependency audit: PASSED
- ❌ Type check: FAILED
- ❌ Linting: FAILED
- ✅ Secrets scan: PASSED
- ✅ Security headers: PASSED

## Solution

You need to fix the type checking and linting errors in your code. Here's how to proceed:

### 1. Run Type Check Locally

```bash
npm run type-check
# or
yarn type-check
# or
pnpm type-check
```

This will show you the specific TypeScript errors that need to be fixed. Common issues include:

- Missing type annotations
- Type mismatches
- Incorrect imports
- Using `any` types where specific types are expected

### 2. Run Linting Locally

```bash
npm run lint
# or
yarn lint
# or
pnpm lint
```

This will show you the linting violations. You can often auto-fix many of them with:

```bash
npm run lint -- --fix
```

### 3. Fix the Issues

Address each error reported by the type checker and linter. Once both commands run successfully locally, commit and push your changes.

The workflow will pass once both the type check and linting steps return success.
