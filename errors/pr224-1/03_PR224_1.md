I'll analyze the failing job to find a solution for you. ## Solution

The build is failing due to ESLint errors for unused variables. The `bars` parameter is defined but never used in two deprecated functions at lines 266 and 281.

**Fix:** Prefix the unused `bars` parameters with an underscore to indicate they're intentionally unused:

```typescript
// Line 263-270
export async function getCachedIndicatorData(
  symbol: string,
  timeframe: string,
  _bars?: number
): Promise<unknown> {
  const cacheKey = getCacheKey(symbol, timeframe);
  return redis.get(cacheKey);
}

// Line 277-286
export async function setCachedIndicatorData(
  symbol: string,
  timeframe: string,
  data: unknown,
  _bars?: number
): Promise<void> {
  const cacheKey = getCacheKey(symbol, timeframe);
  await redis.set(cacheKey, data, INDICATOR_CACHE_TTL);
  statsCounters.sets++;
}
```

This follows the ESLint rule requirement that unused arguments must match the pattern `/^_/u`. Since these are deprecated backward-compatibility wrappers, prefixing with underscore is the cleanest solution without breaking the function signatures.
