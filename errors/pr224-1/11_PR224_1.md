I'll analyze the failing job to find a solution for you. ## Solution

The ESLint failure is due to unused function parameters `bars` on lines 266 and 281 in `lib/cache/indicator-cache. ts`. The linter requires unused parameters to be prefixed with an underscore.

### Fix

Update the parameter names from `bars` to `_bars` on both lines:

**Line 266:**

```typescript
export async function getCachedIndicatorData(
  symbol: string,
  timeframe: string,
  _bars?: number
): Promise<unknown> {
  const cacheKey = getCacheKey(symbol, timeframe);
  return redis.get(cacheKey);
}
```

**Line 281:**

```typescript
export async function setCachedIndicatorData(
  symbol: string,
  timeframe: string,
  data: unknown,
  _bars?: number
): Promise<void> {
  const cacheKey = getCacheKey(symbol, timeframe);
  await redis.set(cacheKey, data, INDICATOR_CACHE_TTL);
  statsCounters.sets++;
}
```

These are deprecated backward-compatibility functions, so the `bars` parameter is intentionally unused. Prefixing with underscore follows the ESLint rule while maintaining the function signature for backward compatibility.
