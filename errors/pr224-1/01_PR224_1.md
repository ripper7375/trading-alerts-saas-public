The failing job is caused by ESLint errors in lib/cache/indicator-cache.ts:

- Line 266: 'bars' is defined but never used.
- Line 281: 'bars' is defined but never used.

To resolve this, prefix the unused bars parameter with an underscore \_bars in both functions (getCachedIndicatorData and setCachedIndicatorData):

```typescript
export async function getCachedIndicatorData(
  symbol: string,
  timeframe: string,
  bars?: number // Change to _bars
): Promise<unknown> {
  const cacheKey = getCacheKey(symbol, timeframe);
  return redis.get(cacheKey);
}

export async function setCachedIndicatorData(
  symbol: string,
  timeframe: string,
  data: unknown,
  bars?: number // Change to _bars
): Promise<void> {
  const cacheKey = getCacheKey(symbol, timeframe);
  await redis.set(cacheKey, data, INDICATOR_CACHE_TTL);
  statsCounters.sets++;
}
```

Change the function parameter from bars?: number to \_bars?: number in both functions, or remove it entirely if not needed.

See lines 263–267 and 277–282 in indicator-cache.ts (ref: 55314c59ea403bd8d5ae23187f05bcda5f062ec6). This should resolve the lint error and allow the job to pass.
