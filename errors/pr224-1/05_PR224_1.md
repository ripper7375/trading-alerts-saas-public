I'll analyze the failing job to find a solution for you. Now let me check the file to see the issue: ## Solution

The build is failing due to ESLint errors for unused variables. The `bars` parameter is defined but never used in two deprecated functions (lines 266 and 281).

### Fix

Prefix the unused `bars` parameters with an underscore to indicate they are intentionally unused:

**Line 266** - Change:

```typescript
export async function getCachedIndicatorData(
  symbol: string,
  timeframe: string,
  bars?: number
): Promise<unknown> {
```

To:

```typescript
export async function getCachedIndicatorData(
  symbol: string,
  timeframe: string,
  _bars?: number
): Promise<unknown> {
```

**Line 281** - Change:

```typescript
export async function setCachedIndicatorData(
  symbol: string,
  timeframe: string,
  data: unknown,
  bars?: number
): Promise<void> {
```

To:

```typescript
export async function setCachedIndicatorData(
  symbol: string,
  timeframe: string,
  data: unknown,
  _bars?:  number
): Promise<void> {
```

These are backward compatibility wrappers that accept the `bars` parameter for API compatibility but don't use it internally. Prefixing with underscore follows the ESLint rule pattern `/^_/u` for allowed unused arguments.
