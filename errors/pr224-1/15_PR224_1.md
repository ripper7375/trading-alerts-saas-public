I'll analyze the failing job to find a solution for you. ## Solution

The build is failing due to **TypeScript unused variable errors** at lines 266 and 281 in `lib/cache/indicator-cache.ts`. The parameter `bars` is declared but never used in two deprecated functions.

### Fix

Remove the unused `bars` parameter from both functions:

**Lines 263-270** - Update `getCachedIndicatorData`:

```typescript
export async function getCachedIndicatorData(
  symbol: string,
  timeframe: string
): Promise<unknown> {
  const cacheKey = getCacheKey(symbol, timeframe);
  return redis.get(cacheKey);
}
```

**Lines 277-286** - Update `setCachedIndicatorData`:

```typescript
export async function setCachedIndicatorData(
  symbol: string,
  timeframe: string,
  data: unknown
): Promise<void> {
  const cacheKey = getCacheKey(symbol, timeframe);
  await redis.set(cacheKey, data, INDICATOR_CACHE_TTL);
  statsCounters.sets++;
}
```

These are deprecated backward compatibility wrappers, so removing the unused `bars` parameter won't affect functionality and will resolve the TypeScript compilation errors.
