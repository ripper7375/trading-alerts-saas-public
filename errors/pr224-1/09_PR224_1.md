I'll analyze the failing job to find a solution for you. ## Solution

The TypeScript build is failing because of unused variable declarations on lines 266 and 281. The `bars` parameter is declared but never used in the deprecated backward compatibility functions.

### Fix

Remove the unused `bars` parameter from both functions:

**Lines 263-270** - Update `getCachedIndicatorData`:

```typescript
export async function getCachedIndicatorData(
  symbol: string,
  timeframe: string
): Promise<unknown> {
  const cacheKey = getCacheKey(symbol, timeframe);
  return redis.get(cacheKey);
}
```

**Lines 277-286** - Update `setCachedIndicatorData`:

```typescript
export async function setCachedIndicatorData(
  symbol: string,
  timeframe: string,
  data: unknown
): Promise<void> {
  const cacheKey = getCacheKey(symbol, timeframe);
  await redis.set(cacheKey, data, INDICATOR_CACHE_TTL);
  statsCounters.sets++;
}
```

These are deprecated functions that don't use the `bars` parameter, so removing it will resolve the TypeScript errors without affecting functionality.
