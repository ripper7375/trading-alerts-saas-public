The job failed because the version specifier for the newman package in package-lock.json (^6.2.0) does not match the version specified in package.json (^6.2.1). To resolve this, update your lockfile so both files agree on the newman version.

**Solution Steps:**

1. Run `pnpm install` (or `npm install` if you use npm) locally to update package-lock.json to match `package.json`.
2. Commit and push the updated lockfile.

This brings the lockfile and manifest into sync and allows the job to pass.

**Relevant lines:**

- In [package.json](https://github.com/ripper7375/trading-alerts-saas-public/blob/31e29c76d20ad560c763f76f1f96b22691195bfd/package.json):  
  `"newman": "^6.2.1"`
- In [package-lock.json](https://github.com/ripper7375/trading-alerts-saas-public/blob/31e29c76d20ad560c763f76f1f96b22691195bfd/package-lock.json):  
  `"newman": "^6.2.0"`

**Command example:**

```sh
pnpm install
# or
npm install
```

Commit and push the resulting lockfile changes.
