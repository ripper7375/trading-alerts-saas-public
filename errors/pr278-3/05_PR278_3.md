The logs indicate that the failing job encountered multiple test failures caused by "Module not found" (ModNotFoundError) errors in several test files. The root problem is that Jest cannot resolve certain imports—commonly paths like @/lib/affiliate/constants or @/lib/cron, and even relative paths (e.g., '../../setup') in your test cases.

To resolve this:

1. **Check Path Aliases in tsconfig.json / jest.config.js**
   - Ensure that any path aliases used (such as `@/lib/...`) are properly mapped in both your `tsconfig.json` ("paths" option) and Jest's configuration (often `moduleNameMapper`).
   - Example for `jest.config.js`:
     ```js
     module.exports = {
       // ... other config ...
       moduleNameMapper: {
         '^@/(.*)$': '<rootDir>/src/$1',
       },
     };
     ```
   - And in `tsconfig.json`:
     ```json
     {
       "compilerOptions": {
         "baseUrl": ".",
         "paths": {
           "@/*": ["src/*"]
         }
       }
     }
     ```

2. **Ensure Modules and Files Exist**
   - Double-check that all files being imported (especially those referenced in failing tests, like `@/lib/affiliate/constants`, `@/lib/cron/...`, etc.) exist at the correct paths and are committed.

3. **Verify Relative Imports**
   - For relative imports like `'../../setup'`, make sure the file exists at the specified location relative to the test file.

4. **Reinstall Dependencies**
   - Sometimes the error can be caused by a corrupted or mismatched `node_modules`. Run:
     ```
     rm -rf node_modules/ && npm install
     ```
     or if using pnpm/yarn:
     ```
     pnpm install
     # or
     yarn install
     ```

5. **Example Code Fix for jest.config.js:**
   If your code lives in `src/`, update or add `moduleNameMapper`:

   ```js
   module.exports = {
     // ... other config ...
     moduleNameMapper: {
       '^@/(.*)$': '<rootDir>/src/$1',
     },
   };
   ```

   Adjust `<rootDir>/src/$1` as needed to match your project structure.

6. **Commit All Referenced Files**
   - If you created or moved any files as part of your PR, ensure they are staged and committed.

By ensuring module paths are correctly mapped and all files exist, these resolving errors and subsequent test failures should be fixed. If you need more precise edits, let me know which files or aliases are not resolving, and I can suggest exact path corrections.
