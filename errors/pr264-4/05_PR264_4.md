The failing job's problem is with the test: IndicatorToggles Component › rendering › should render PRO indicators, specifically at:

**tests**/components/charts/indicator-toggles.test.tsx line 293:

```ts
expect(screen.getByText('Momentum Candles')).toBeVisible();
```

This (and similar tests) expect that PRO indicators like "Momentum Candles" are rendered.

To resolve this:

- Verify that the IndicatorToggles component actually renders the text for all PRO indicators, e.g., "Momentum Candles", "Keltner Channels", "TEMA", "HRMA", "SMMA", "ZigZag" as expected by the test.
- Ensure the logic for rendering these items is not conditional on tier or collapsed state within the initial render.

Code suggestion for IndicatorToggles (in components/charts/IndicatorToggles.tsx):

- Confirm that your component includes something like:

```tsx
{
  proIndicators.map((indicator) => (
    <label key={indicator}>
      <input
        type="checkbox"
        name={indicator}
        disabled={userTier !== 'PRO'}
        // other props
      />
      {indicatorNameMap[indicator]} {/* e.g., "Momentum Candles", etc. */}
    </label>
  ));
}
```

- Double-check the section/header for "Pro Indicators" and make sure it’s not hidden or gated in a way the test doesn’t account for.

If after this review the indicators still do not render or the test fails, add a console.log or debug output in the component to verify what is actually being rendered during the test. This will confirm whether the test or the component logic should be adjusted.
