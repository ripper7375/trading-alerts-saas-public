The failure is due to the job reporting "❌ One or more test jobs failed" and explicitly exiting with code 1. This is triggered by the Test Summary job when any of the following jobs fail: unit-and-component-tests, integration-tests, or build-check. The logs also confirm that both "Integration Tests" and "Build Check" steps were skipped, which suggests that their dependencies did not run or completed unsuccessfully.

To solve this:

1. Ensure that the jobs unit-and-component-tests, integration-tests, and build-check are configured to always run (remove conditions that could cause them to be skipped).
2. If you only intend to skip a job under certain conditions, explicitly set those with the if: directive, and check that the conditional logic for skipping is correct.
3. Verify that matrix or needs dependencies are correct. If a prior job fails or is skipped, dependent jobs will not run; this can cause summary/final jobs to trigger failure logic.
4. In the .github/workflows/tests.yml file (ref: 813161680a84e20a89fc60892a832669a1ce68a6), review the needs and if: always() fields to ensure jobs are executed as expected.

Example code fix: To ensure summary steps correctly reflect the test status, you may need to update your if logic or dependencies.

For example, change:

```yaml
needs: [unit-and-component-tests, integration-tests, build-check]
if: always()
```

Make sure all upstream jobs are configured to run and not to be skipped due to prior jobs' failures unless intended.

Also, verify that your test and build scripts (such as pnpm run test:ci and pnpm run build) do not have silent failures or conditional early exits.

Carefully check and, if needed, reset or clarify your use of if: and needs in your workflow configuration. You can review the full configuration here.
