The failure is caused by the step in the test-summary job that runs:

```bash
echo "❌ One or more test jobs failed"
exit 1
```

This occurs when one or more of the previous jobs (unit-and-component-tests, integration-tests, build-check) fail.

In your logs, both "Integration Tests" and "Build Check" are marked as skipped, which triggers this failure. This typically means that a required dependency job did not succeed or was skipped (possibly due to a previous job failure, cancellation, or an unmet condition).

To fix this:

1. Ensure the dependency jobs required by integration-tests and build-check (i.e., unit-and-component-tests) run and complete successfully.
2. Double-check that the conditions and matrix configurations for triggering these jobs are correct in your workflow file.
3. If jobs are being skipped unintentionally, review trigger branches and any conditional statements (such as branch filters in the workflow) to ensure jobs always run as expected for your PR.

If you continue having skipped jobs, try running the workflow locally or manually trigger the workflow from the Actions tab for your branch to identify upstream job failures or misconfigurations.
