The job failed due to a TypeScript error in scripts/test-mt5-deployment.ts at line 11:

error TS6133: 'Prisma' is declared but its value is never read.

You can fix this by removing Prisma from the import statement since it's not used in the file. Change:

```typescript
import { PrismaClient, Prisma } from '@prisma/client';
```

to:

```typescript
import { PrismaClient } from '@prisma/client';
```

Make this change and re-run the job. The TypeScript build should proceed without this error.  
See the code: scripts/test-mt5-deployment.ts (ref: 59be0f2c3c55b5008d37aa5dce81045f76655298).
