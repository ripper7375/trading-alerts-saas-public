The job failed due to a TypeScript error in scripts/test-mt5-deployment.ts on line 11: 'Prisma' is declared but its value is never read. This means the import { PrismaClient, Prisma } from '@prisma/client' statement is bringing in both PrismaClient and Prisma, but Prisma is not used anywhere in the file.

Solution: Remove the unused Prisma import from line 11. Update:

```typescript
import { PrismaClient, Prisma } from '@prisma/client';
```

to:

```typescript
import { PrismaClient } from '@prisma/client';
```

This will resolve the type error and allow the build to complete successfully.

You can view the file here: scripts/test-mt5-deployment.ts (ref: 59be0f2c3c55b5008d37aa5dce81045f76655298)
