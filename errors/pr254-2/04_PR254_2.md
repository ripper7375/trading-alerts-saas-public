The failure was caused by a TypeScript error in scripts/test-mt5-deployment.ts at line 11:

Type error: 'Prisma' is declared but its value is never read.

To fix this, remove Prisma from the import statement. Change:

```typescript
import { PrismaClient, Prisma } from '@prisma/client';
```

to:

```typescript
import { PrismaClient } from '@prisma/client';
```

This will resolve the unused import error and allow the job to proceed successfully. See the code at ref 59be0f2c3c55b5008d37aa5dce81045f76655298 for reference.
