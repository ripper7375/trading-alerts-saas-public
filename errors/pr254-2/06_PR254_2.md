The failing job is due to a TypeScript error in scripts/test-mt5-deployment.ts at line 11:

error TS6133: 'Prisma' is declared but its value is never read.

In your import statement:

```typescript
import { PrismaClient, Prisma } from '@prisma/client';
```

You only use PrismaClient throughout the script; Prisma is never referenced. To resolve this, modify the import as follows:

```typescript
import { PrismaClient } from '@prisma/client';
```

This will fix the job failure caused by the unused Prisma import. See the full file for reference: scripts/test-mt5-deployment.ts (ref: 59be0f2c3c55b5008d37aa5dce81045f76655298).
