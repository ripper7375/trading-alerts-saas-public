I'll analyze the failing job to find a solution for you. The job is failing due to two issues:

## Issue 1: Lockfile out of sync (Critical)

The primary failure is that the lockfile doesn't match `package.json`. The dependency `@types/pg@^8.11.10` was added to `package.json` but the lockfile wasn't updated.

**Solution:**
Run the following command locally and commit the changes:

```bash
pnpm install
```

Then commit the updated lockfile:

```bash
git add pnpm-lock.yaml
git commit -m "Update lockfile for @types/pg dependency"
git push
```

## Issue 2: Missing coverage reports (Secondary)

The codecov step is failing because no coverage files are being generated. This happens after the lockfile failure, so it may succeed once the first issue is resolved.

If it continues failing after fixing the lockfile, you'll need to:

1. Ensure your test command generates coverage (e.g., `pnpm test --coverage`)
2. Verify the coverage output path matches `coverage/lcov.info`
3. Or add the `fail_ci_if_error:  false` flag to the codecov action if coverage is optional

**Priority:** Fix the lockfile issue first, as it's causing the build to fail before coverage can be generated.
