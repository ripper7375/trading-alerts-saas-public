I'll analyze the failing job to find a solution for you. The job is failing because the lockfile is out of sync with `package.json`. The error indicates that `@types/pg@^8.11.10` was added to dependencies but the lockfile wasn't updated.

## Solution

Run the following command locally to update the lockfile:

```bash
pnpm install
```

Then commit the updated `pnpm-lock.yaml`:

```bash
git add pnpm-lock.yaml
git commit -m "Update lockfile for @types/pg dependency"
git push
```

This will regenerate the lockfile to match the dependencies specified in `package.json`, which should resolve the CI failure.
