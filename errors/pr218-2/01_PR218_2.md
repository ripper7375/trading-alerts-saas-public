The job failed because the specifiers in the lockfile do not match those in package.json. Specifically, the dependency @types/pg@^8.11.10 was added to package.json but not synchronized in package-lock.json. Your workflow uses pnpm with --frozen-lockfile, so this mismatch will always fail CI.

**Solution:**

1. Run the following commands locally to update your lockfile:

   ```bash
   pnpm install
   pnpm install --fix-lockfile
   git add package-lock.json
   git commit -m "fix: synchronize package-lock.json with package.json"
   ```

2. Push the updated lockfile to your branch.

This will resolve the lockfile mismatch and allow the CI workflow to pass. No changes are needed to your workflow or dependency definitions, just ensure the lockfile is in sync after dependency updates.
