I'll analyze the failing job to find a solution. Let me get the job logs first. The job is failing because three security checks failed:

1. **Dependency audit: FAILED**
2. **Type check: FAILED**
3. **Linting: FAILED**

Let me get more details from the workflow file and look at the earlier parts of the logs: The security checks job failed because three underlying checks are failing:

1. **Dependency audit: FAILED**
2. **Type check: FAILED**
3. **Linting: FAILED**

The summary job (which is what failed) aggregates results from all security checks and fails if any upstream check fails. To fix this, you need to address each of the three failing checks:

## Solution

### 1. Fix Dependency Audit

Run locally and address vulnerabilities:

```bash
pnpm audit --audit-level=high
```

Then either update vulnerable packages or add acceptable vulnerabilities to an audit ignore list if they're false positives or have no fix available.

### 2. Fix Type Check

Run the type checker locally to see the errors:

```bash
pnpm run type-check
```

Fix all TypeScript errors that are reported. Common issues include:

- Missing type annotations
- Type mismatches
- Incorrect property access
- Missing null/undefined checks

### 3. Fix Linting

Run ESLint locally to see the errors:

```bash
pnpm run lint
```

Fix all linting errors. You can also try:

```bash
pnpm run lint --fix
```

to auto-fix formatting issues.

Once you fix these three checks locally and push your changes, the Security Check Summary job will pass. The other two checks (secrets scan and security headers) are already passing.
