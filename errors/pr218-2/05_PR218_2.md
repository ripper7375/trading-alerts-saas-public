I'll analyze the failing job to find a solution for you. Let me check the workflow file and the package.json to understand the issue better: ## Solution

The job is failing because the `pnpm-lock.yaml` file is out of sync with `package.json`. Specifically, the dependency `@types/pg@^8.11.10` was added to `package.json` but the lockfile wasn't updated.

### Fix

You need to regenerate the lockfile to match the current `package.json`. Run this command locally:

```bash
pnpm install
```

This will update `pnpm-lock.yaml` to include the new `@types/pg` dependency. Then commit and push the updated lockfile:

```bash
git add pnpm-lock.yaml
git commit -m "chore: update pnpm-lock.yaml for @types/pg"
git push
```

### Root Cause

The workflow uses `pnpm install --frozen-lockfile` (line 56 of the workflow file), which intentionally fails if there's a mismatch between `package.json` and `pnpm-lock.yaml`. This is a safety mechanism to ensure reproducible builds.

When you added `@types/pg@^8.11.10` to the `devDependencies` in `package.json`, the lockfile wasn't regenerated, causing the CI check to fail.
