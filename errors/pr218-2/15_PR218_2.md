I'll analyze the failing job to find a solution. Let me get the logs first. ## Solution

The job is failing because the lockfile (pnpm-lock.yaml) is out of sync with your package.json. The error indicates that `@types/pg@^8.11.10` was added to the dependencies but the lockfile wasn't updated.

**Issue:** The `@types/pg` package is listed in `devDependencies` (line 136 of package.json), but the lockfile check is detecting it as a new addition that hasn't been properly synchronized.

**Fix:** Update your lockfile by running:

```bash
pnpm install
```

Then commit the updated `pnpm-lock.yaml` file:

```bash
git add pnpm-lock.yaml
git commit -m "chore: update lockfile for @types/pg"
git push
```

This will synchronize the lockfile with your package. json and resolve the security check failure. The issue occurs because the workflow is running `pnpm install --frozen-lockfile`, which requires an exact match between package.json and the lockfile.
