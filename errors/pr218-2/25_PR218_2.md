I'll analyze the failing job to find a solution for you. The job is failing due to a **lockfile mismatch**. The error indicates that `@types/pg@^8.11.10` was added to `package.json` but the lockfile wasn't updated.

## Solution

Run the following command locally to update your lockfile:

```bash
pnpm install
```

Then commit and push the updated `pnpm-lock.yaml` file:

```bash
git add pnpm-lock.yaml
git commit -m "Update lockfile for @types/pg dependency"
git push
```

This will synchronize the lockfile with the dependencies in `package.json`, which should resolve the CI failure.

---

**Note:** The Codecov error that follows is a consequence of the build failing before tests could run—once the lockfile issue is fixed, coverage files should be generated properly.
