I'll analyze the failing job to find a solution for you. The job is failing because of a lockfile mismatch. The error indicates that `@types/pg@^8.11.10` was added as a dependency in `package.json` but the lockfile hasn't been updated.

## Solution

You need to update your lockfile to include the new dependency. Run the following command locally:

```bash
pnpm install
```

This will update your `pnpm-lock.yaml` file to include the new `@types/pg` dependency. Then commit and push the updated lockfile:

```bash
git add pnpm-lock.yaml
git commit -m "Update lockfile for @types/pg"
git push
```

Alternatively, if you have write access to the repository and want to ensure lockfile consistency is maintained, you could update the CI workflow to automatically fail with clearer instructions, but the immediate fix is simply regenerating the lockfile with the updated dependencies.
