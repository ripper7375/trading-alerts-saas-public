I'll analyze the failing job to find a solution for you. ## Solution

The job is failing because the pnpm lockfile is out of sync with `package.json`. The dependency `@types/pg@^8.11.10` was added to the project but the lockfile wasn't updated.

### Fix

You need to regenerate the lockfile by running:

```bash
pnpm install
```

This will sync the lockfile with the `package.json` changes and resolve the error. After running this command, commit the updated `pnpm-lock.yaml` file.

### Root Cause

Looking at your `package.json` (line 136), `@types/pg` version `^8.11.10` is listed in `devDependencies`, but the lockfile doesn't reflect this addition. The CI workflow is using `pnpm install --frozen-lockfile` which fails when there's a mismatch between the lockfile and package.json.

### Verification

After updating the lockfile, you can verify it locally with:

```bash
pnpm install --frozen-lockfile
```

This should complete successfully before pushing the changes.
