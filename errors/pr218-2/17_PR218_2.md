I'll analyze the failing job to find a solution for you. The job is failing because the lockfile is out of sync with `package.json`. Specifically, `@types/pg@^8.11.10` was added to the dependencies but the lockfile wasn't updated.

## Solution

Run the following command to update the lockfile:

```bash
pnpm install
```

Then commit the updated lockfile:

```bash
git add pnpm-lock.yaml
git commit -m "Update lockfile with @types/pg dependency"
git push
```

This will synchronize the lockfile with the package. json changes and resolve the CI failure.
