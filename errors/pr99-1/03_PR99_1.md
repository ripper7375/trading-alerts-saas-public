The failing job (ID: 59015768229) was caused by Python style errors in the file mt5-service/app/models/requests.py, specifically exceeding the maximum line length (E501: line too long) at the following lines:

- Line 269: 92 characters (limit 88)
- Line 273: 104 characters (limit 88)
- Line 277: 93 characters (limit 88)
- Line 280: 101 characters (limit 88)

To resolve this, break up or wrap each of these lines so that no line exceeds 88 characters in length. Here is how you can modify each offending line:

**Before:**

```python
269|         return False, f"Invalid symbol: {symbol}. Valid symbols: {', '.join(VALID_SYMBOLS)}"
273|         return False, f"Invalid timeframe: {timeframe}. Valid timeframes: {', '.join(VALID_TIMEFRAMES)}"
277|         return False, f"Symbol {symbol} requires PRO tier. Upgrade to access all 15 symbols."
280|         return False, f"Timeframe {timeframe} requires PRO tier. Upgrade to access all 9 timeframes."
```

**After (suggestion):**

```python
269|         return False, (
270|             f"Invalid symbol: {symbol}. Valid symbols: {', '.join(VALID_SYMBOLS)}"
271|         )
273|         return False, (
274|             f"Invalid timeframe: {timeframe}. Valid timeframes: {', '.join(VALID_TIMEFRAMES)}"
275|         )
277|         return False, (
278|             f"Symbol {symbol} requires PRO tier. Upgrade to access all 15 symbols."
279|         )
280|         return False, (
281|             f"Timeframe {timeframe} requires PRO tier. Upgrade to access all 9 timeframes."
282|         )
```

Each line is split for readability and compliance with the line length requirement.

Apply these changes to mt5-service/app/models/requests.py (ref: 86c1bb6134a8c69868f479fe02489322833dc365) to fix the failing job.
