The failing job (59015770563) error is caused by a TypeScript type mismatch at line 383 in app/api/indicators/[symbol]/[timeframe]/route.ts:

> Argument of type 'CachedData' is not assignable to parameter of type 'Record<string, unknown>'. Index signature for type 'string' is missing in type 'CachedData'.

This occurs when calling a function (likely filterIndicatorData) that expects its first argument to have a string index signature. The CachedData type is defined as:

```typescript
type CachedData = MT5IndicatorData & {
  proIndicatorsTransformed: ProIndicatorData;
};
```

but does not explicitly have an index signature.

**Solution:**
Add an index signature to CachedData so TypeScript recognizes it as compatible:

```typescript
type CachedData = MT5IndicatorData & {
  proIndicatorsTransformed: ProIndicatorData;
  [key: string]: unknown; // <- Add this line
};
```

Apply this change to the CachedData type definition (line 372 in route.ts).

Alternatively, if filterIndicatorData does not require the index signature and a more precise type is preferable, update the filterIndicatorData function’s parameter typing.

**Summary of changes:**

- Add `[key: string]: unknown;` to CachedData to resolve the type error.

For reference, see the code at commit 86c1bb6134a8c69868f479fe02489322833dc365. This will allow type compatibility and prevent compilation failure.
