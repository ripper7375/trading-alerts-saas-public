I'll analyze the failing job to find a solution for you. The job is failing because the **Type check** step failed. Let me get more details from the workflow file and the actual type check output: ## Solution

The job failed because the **TypeScript type check** step failed. The summary shows:

- ❌ Type check: FAILED

All other security checks passed (dependency audit, linting, secrets scan, and security headers).

### Fix

You need to resolve TypeScript type errors in your code. Run the type check locally to see the specific errors:

```bash
pnpm run type-check
```

This will show you the exact TypeScript errors that need to be fixed. Common issues to look for:

1. **Missing type definitions** - Ensure all variables, function parameters, and return types are properly typed
2. **Type mismatches** - Check for incorrect types being assigned or passed to functions
3. **Null/undefined handling** - Add proper null checks or optional chaining where needed
4. **Import errors** - Verify all imports are correct and types are exported properly

Once you identify the specific errors from running `pnpm run type-check`, fix them in your code and push the changes. The workflow will automatically re-run and should pass once all TypeScript errors are resolved.
