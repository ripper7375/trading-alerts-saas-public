I'll analyze the failing job to identify the issue and provide a solution. I can see there are TypeScript compilation errors. Let me examine the failing files to understand the issues: ## Solution for Failing TypeScript Build

The job is failing due to **TypeScript type errors** in two files. Here are the fixes:

### 1. Fix `lib/confluence/calculator.ts` (Line 106)

**Problem:** Empty object `{}` doesn't satisfy the `Record<Timeframe, TimeframeSignal>` type requirement.

**Solution:** Initialize `breakdown` with the correct type assertion:

```typescript
// Line 106: Change from
breakdown: {},

// To:
breakdown: {} as Record<string, TimeframeSignal>,
```

### 2. Fix `lib/db/multi-timeframe-query.ts` (Lines 124-128, 143)

**Problem:** Type assertions using `as unknown` are incomplete and don't properly cast to the target types.

**Solution:** Update the type assertions to be more explicit:

```typescript
// Lines 124-126:  Change the type assertions
fractals: row.fractals as Fractals | null,
horizontal_trendlines: row.horizontal_trendlines as HorizontalTrendlines | null,
diagonal_trendlines: row.diagonal_trendlines as DiagonalTrendlines | null,

// Line 127: Fix momentum_candles
momentum_candles: (row. momentum_candles || []) as MomentumCandle[],

// Line 128-139: Fix keltner_channels
keltner_channels: (row.keltner_channels || {
  ultra_extreme_upper: [],
  extreme_upper: [],
  upper_most: [],
  upper: [],
  upper_middle: [],
  lower_middle:  [],
  lower: [],
  lower_most: [],
  extreme_lower: [],
  ultra_extreme_lower: [],
}) as KeltnerChannels,

// Line 143: Fix zigzag
zigzag: (row.zigzag || { peaks: [], bottoms: [] }) as ZigZag,
```

**Complete replacement for lines 124-143:**

```typescript
fractals: row.fractals as Fractals | null,
horizontal_trendlines: row.horizontal_trendlines as HorizontalTrendlines | null,
diagonal_trendlines: row.diagonal_trendlines as DiagonalTrendlines | null,
momentum_candles: (row.momentum_candles || []) as MomentumCandle[],
keltner_channels: (row. keltner_channels || {
  ultra_extreme_upper: [],
  extreme_upper: [],
  upper_most: [],
  upper: [],
  upper_middle: [],
  lower_middle:  [],
  lower: [],
  lower_most: [],
  extreme_lower: [],
  ultra_extreme_lower: [],
}) as KeltnerChannels,
tema: row.tema,
hrma: row.hrma,
smma: row.smma,
zigzag: (row.zigzag || { peaks: [], bottoms: [] }) as ZigZag,
```

These changes will resolve all TypeScript compilation errors and allow the build to pass.
