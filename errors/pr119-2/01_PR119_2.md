The failing job was caused by a TypeScript error in app/(auth)/verify-email/pending/page.tsx: "Not all code paths return a value" in the useEffect at line 19. In your code:

```tsx
useEffect(() => {
  if (countdown > 0) {
    const timer = setTimeout(() => setCountdown(countdown - 1), 1000);
    return () => clearTimeout(timer);
  } else if (countdown === 0 && resendStatus === 'rate_limited') {
    setResendStatus('idle');
  }
}, [countdown, resendStatus]);
```

Not all branches of the useEffect callback return a function (some just fall through), but React expects either nothing or a cleanup function returned explicitly.

**Solution:** Make sure you always return either a cleanup function or undefined:

```tsx
useEffect(() => {
  if (countdown > 0) {
    const timer = setTimeout(() => setCountdown(countdown - 1), 1000);
    return () => clearTimeout(timer);
  } else if (countdown === 0 && resendStatus === 'rate_limited') {
    setResendStatus('idle');
  }
  return undefined; // Explicitly return undefined if no cleanup
}, [countdown, resendStatus]);
```

Update the useEffect in app/(auth)/verify-email/pending/page.tsx at ref 8a4190ab6d8a4b951a1e41f2137bbfa12c123471 with the fix above. This will resolve the build failure.
