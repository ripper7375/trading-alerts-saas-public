The failing job is due to a TypeScript error in lib/candle-data-helpers.ts at line 221:

Type 'Candle | undefined' is not assignable to type 'Candle | null'. Type 'undefined' is not assignable to type 'Candle | null'.

This occurs in the getLatestCandle function:

```typescript
export async function getLatestCandle(symbol: string): Promise<Candle | null> {
  const candles = await getFromRedis(symbol, 1);
  return candles.length > 0 ? candles[0] : null;
}
```

The type returned by candles[0] is Candle | undefined, but the function expects Candle | null.

Solution: Change undefined to null with a nullish coalescing operator.

Replace line 221 with:

```typescript
return candles[0] ?? null;
```

This safely converts undefined to null and resolves the type error. See the relevant code: [lib/candle-data-helpers.ts#L221](https://github.com/ripper7375/trading-alerts-saas-public/blob/ce1b95c1749c4d62eb595a03688caac7d72c7901/lib/candle-data-helpers.ts#L221).
