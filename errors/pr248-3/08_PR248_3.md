The TypeScript type check failed in your workflow (see the summary and log under the job for details). To fix this, review errors from pnpm run type-check. Common solutions are:

- Check your codebase for TypeScript errors: run pnpm run type-check locally and resolve any errors shown.
- If you need stricter type coverage, ensure all files have correct types and typings are up to date.
- If you use custom types or third-party types, make sure they’re installed and imported correctly.

If you're unsure which files are causing the issue, look for the TypeScript error output in your CI log after the pnpm run type-check step. Fix those errors in the relevant files, rerun the workflow, and the job should pass.
