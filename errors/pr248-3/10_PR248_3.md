The job failed due to a TypeScript type error in lib/candle-data-helpers.ts at line 221:

Type 'Candle | undefined' is not assignable to type 'Candle | null'.
Type 'undefined' is not assignable to type 'Candle | null'.

Here is the relevant code from getLatestCandle:

```typescript
221|   return candles.length > 0 ? candles[0] : null;
```

candles[0] has type Candle | undefined, but the function returns Candle | null.

Solution: Explicitly handle the case where candles[0] might be undefined. You can fix this by changing line 221 to:

```typescript
return candles.length > 0 && candles[0] ? candles[0] : null;
```

or simply:

```typescript
return candles[0] ?? null;
```

This ensures the function always returns either a Candle or null, matching the declared return type.

Reference: lib/candle-data-helpers.ts (ref: ce1b95c1749c4d62eb595a03688caac7d72c7901)
