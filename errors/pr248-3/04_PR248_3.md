The job failed due to a type error in lib/candle-data-helpers.ts at line 221:

Type error: Type 'Candle | undefined' is not assignable to type 'Candle | null'. Type 'undefined' is not assignable to type 'Candle | null'.

The problematic code:

```typescript
return candles.length > 0 ? candles[0] : null;
```

Here, candles[0] may be undefined, not null. To fix this, explicitly handle the undefined case so the function always returns Candle or null.

Replace line 221 in getLatestCandle with:

```typescript
return candles[0] ?? null;
```

Or, for compatibility:

```typescript
return candles.length > 0 && candles[0] ? candles[0] : null;
```

This change will resolve the type error and allow the build to succeed. See the code context here.
