The failure is due to a TypeScript type mismatch in lib/candle-data-helpers.ts at line 221:
Type error: Type 'Candle | undefined' is not assignable to type 'Candle | null'. Type 'undefined' is not assignable to type 'Candle | null'.

To fix this, update the getLatestCandle function (lines 219–222) as follows:

Replace:

```typescript
return candles.length > 0 ? candles[0] : null;
```

With:

```typescript
return candles.length > 0 && candles[0] ? candles[0] : null;
```

Or, more idiomatically:

```typescript
return candles.at(0) ?? null;
```

Both solutions ensure that the function returns either a Candle or null, never undefined, satisfying the declared return type.

See the relevant code here:  
https://github.com/ripper7375/trading-alerts-saas-public/blob/ce1b95c1749c4d62eb595a03688caac7d72c7901/lib/candle-data-helpers.ts
