I'll analyze the failing job to find a solution for you. I can see there are 5 failing tests in this job. Let me examine the relevant files to provide solutions: ## Solution for Failing Tests

There are **5 failing tests** in the test suite. Here's the breakdown and solutions:

---

### **1. Confluence Calculator Test Failure**

**File**: `__tests__/unit/confluence-calculator.test.ts:396`

**Issue**: The test expects a confluence score less than 7 for mixed signals, but it's receiving a higher value.

**Line 396**:

```typescript
expect(result.confluence_score).toBeLessThan(7);
```

**Solution**: The confluence calculation algorithm needs adjustment to reduce scores when signals are conflicting. Check the `calculateConfluenceScore` function in `lib/confluence/calculator.ts` to ensure:

```typescript
// When calculating mixed signals (4 bullish, 4 bearish, 1 neutral)
// The score should be reduced more aggressively
// Suggested formula adjustment:
const conflictPenalty = Math.abs(bullishWeight - bearishWeight) / totalWeight;
const adjustedScore = baseScore * conflictPenalty;
```

---

### **2. Jest Parse Errors (2 files)**

**Files**:

- `__tests__/part20/api/indicators. test.ts`
- `__tests__/part20/api/confluence.test.ts`

**Issue**: Jest encounters an unexpected token when parsing `next-auth` module at runtime. The error traces to:

```
node_modules/. pnpm/next-auth@4.24.13. ../next-auth/index.js:25:37
```

**Solution**: Update the Jest mock configuration for `next-auth` to prevent it from loading the actual module:

```typescript
// In jest.config.js or jest.setup.js
moduleNameMapper: {
  '^next-auth$': '<rootDir>/__mocks__/next-auth.ts',
  '^next-auth/(. *)$': '<rootDir>/__mocks__/next-auth. ts',
}
```

Or update the test files to mock `next-auth` before any imports:

```typescript
// Move this to the very top of both test files (before line 9)
jest.mock('next-auth', () => ({
  __esModule: true,
  default: jest.fn(),
  getServerSession: jest.fn(async () => mockSessionData),
}));
```

---

### **3. Database Query Errors (2 files)**

**Files**:

- `__tests__/part20/integration/cache-integration.test.ts:259`
- `__tests__/part20/integration/db-queries.test.ts:226`

**Issue**: `getIndicatorDataFromDb` in `lib/db/queries.ts: 37` is throwing an error. The query uses dynamic table names without proper SQL injection protection.

**Problem in `lib/db/queries.ts` line 37-44**:

```typescript
const rows = await query<RawRow>(
  `SELECT timestamp, open, high, low, close,
          fractals, horizontal_trendlines, diagonal_trendlines,
          momentum_candles, keltner_channels, tema, hrma, smma, zigzag
   FROM ${tableName}  // ⚠️ SQL injection risk
   ORDER BY timestamp DESC
   LIMIT $1`,
  [limit]
);
```

**Solution**: Use proper identifier escaping for PostgreSQL:

```typescript
export async function getIndicatorDataFromDb(
  symbol: string,
  timeframe: string,
  limit: number = 1000
): Promise<IndicatorData> {
  // Validate inputs
  const validSymbols = [
    'EURUSD',
    'GBPUSD',
    'USDJPY',
    'AUDJPY' /* add all valid symbols */,
  ];
  const validTimeframes = [
    'M5',
    'M15',
    'M30',
    'H1',
    'H2',
    'H4',
    'H8',
    'H12',
    'D1',
  ];

  if (!validSymbols.includes(symbol.toUpperCase())) {
    throw new Error(`Invalid symbol: ${symbol}`);
  }
  if (!validTimeframes.includes(timeframe.toUpperCase())) {
    throw new Error(`Invalid timeframe: ${timeframe}`);
  }

  const tableName = `${symbol.toLowerCase()}_${timeframe.toLowerCase()}`;

  // Use pg-format or identifier escaping
  const rows = await query<RawRow>(
    `SELECT timestamp, open, high, low, close,
            fractals, horizontal_trendlines, diagonal_trendlines,
            momentum_candles, keltner_channels, tema, hrma, smma, zigzag
     FROM "${tableName}"
     ORDER BY timestamp DESC
     LIMIT $1`,
    [limit]
  );

  // ...  rest of the function
}
```

**For tests**: Mock the `query` function properly:

```typescript
jest.mock('@/lib/db/postgresql', () => ({
  query: jest.fn(async () => [
    // Return mock data matching RawRow interface
  ]),
}));
```

---

### **Summary**

1. **Confluence Calculator**: Adjust scoring algorithm to better handle mixed signals
2. **Jest Parse Errors**: Fix `next-auth` mocking strategy to prevent module loading
3. **Database Queries**: Add input validation and proper SQL identifier escaping, or mock the query function in tests

All fixes should be applied to ensure the test suite passes completely.
