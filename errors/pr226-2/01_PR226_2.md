The failing job 59529932172 has multiple issues; here are the main errors and their solutions:

---

### 1. "An update to PlanSelector inside a test was not wrapped in act(...)"

- **Solution:** In your tests for the PlanSelector component, wrap any code causing React state updates in await act(...) from 'react-dom/test-utils' (or equivalent from your test library). This suppresses warnings and ensures the UI is updated before assertions.
- **Example fix:**
  ```ts
  import { act } from 'react-dom/test-utils';
  // ...inside the test
  await act(async () => {
    // trigger updates
  });
  ```

---

### 2. "CRON_SECRET not configured"

- **Solution:** The environment variable CRON_SECRET is missing during tests. Set process.env.CRON_SECRET to a dummy value in your test setup.
- **Example fix:**
  ```ts
  // Add in your jest.setup.js or at the top of tests touching cron API:
  process.env.CRON_SECRET = 'test-secret';
  ```

---

### 3. "Jest encountered an unexpected token" (related to next-auth/Next.js imports)

- **Solution:** Jest needs Babel configuration to handle modern JS/TypeScript/Next.js features. Ensure your jest.config.js includes proper transformations.
- **Example fix—jest.config.js:**
  ```js
  transform: {
    '^.+\\.(ts|tsx|js|jsx)$': 'babel-jest',
  },
  transformIgnorePatterns: [
    '/node_modules/(?!next-auth|other-esm-module-to-transpile)'
  ],
  ```
  Also, ensure a .babelrc or babel.config.js is present with next/babel preset.

---

### 4. Failing test in confluence-calculator.test.ts

- Failure at `.toBeLessThan` on line 396.
- **Solution:** Check expected/actual values in that specific test case. If testing lower scores but actual is not, inspect logic/calculation or fix the test expectation.

---

### 5. Database errors in admin tests

- Failure: "Admin users error: Error: Database error"
- **Solution:** Mock database functions used in tests to avoid actual DB calls, or ensure test DB is configured correctly.

---

### Next Steps:

- Implement the above code changes depending on the specific file reporting errors.
- After these fixes, rerun your workflow.

If you need the exact test or component source code, let me know the filename or area you'd like to review, and I can provide code-level suggestions for that section.
