The job failed due to a TypeScript error in lib/auth/auth-options.ts on line 21: Parameter 'data' implicitly has an 'any' type. This occurs in your CustomPrismaAdapter's createUser function. TypeScript strict mode requires all function parameters to have explicit types.

To fix this, explicitly type the data parameter. Since NextAuth adapters use AdapterUser or a compatible interface, update your function as follows:

```typescript
import type { AdapterUser } from 'next-auth/adapters';
// ... other imports

function CustomPrismaAdapter(): Adapter {
  const baseAdapter = PrismaAdapter(prisma);

  return {
    ...baseAdapter,
    createUser: async (data: AdapterUser) => {
      const user = await prisma.user.create({
        data: {
          email: data.email,
          name: data.name,
          image: data.image,
          emailVerified: data.emailVerified ?? new Date(),
          tier: 'FREE',
          role: 'USER',
          isAffiliate: false,
        },
      });
      return user as AdapterUser;
    },
  };
}
```

This will resolve the type error. See the line in context here:  
[lib/auth/auth-options.ts#L21](https://github.com/ripper7375/trading-alerts-saas-public/blob/df4e155a02e10d7841a623d9c960e1856a5e074f/lib/auth/auth-options.ts#L21)
