The failing job is caused by a TypeScript error in frontend/app/api-test/page.tsx:

Type error: Cannot find module '@/lib/api-client' or its corresponding type declarations.

Solution:

1. Ensure that the file frontend/lib/api-client.ts (or .js/.ts/.d.ts) exists in your repo. If it doesn’t, add it or restore it from version control.
2. If the file exists, check that your tsconfig.json (or jsconfig.json) includes the correct path alias configuration:

```json
"compilerOptions": {
  "baseUrl": ".",
  "paths": {
    "@/*": ["./frontend/*"]
  }
}
```

3. If your alias is correct and the file is present, ensure that there are no typos in the import or filename.
4. After correcting the above, commit and push your changes.

If the file was intentionally removed, update the import in frontend/app/api-test/page.tsx to use the correct module or remove related code.
