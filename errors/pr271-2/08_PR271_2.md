The failing job 60685724236 failed at the "TypeScript Type Check" step. According to the logs, the type check reported as "FAILED", while all other checks passed.

Solution:

- Inspect the output of pnpm run type-check locally to see the TypeScript errors.
- Fix the reported type errors in your codebase. These often involve missing types, incorrect interfaces, or invalid assignments.
- After resolving type errors, commit your changes and push to re-run the workflow.

If you need to see the specific type errors, run the following command locally:

```bash
pnpm install --frozen-lockfile
pnpm run type-check
```

This will display the exact issues TypeScript is encountering. Once they are fixed, the security check summary will pass.

Relevant workflow definition: [.github/workflows/security-checks.yml#L44-L70 (ref: b544401bf1155e0a90038bd160fd7c644d3ccbb5)](https://github.com/ripper7375/trading-alerts-saas-public/blob/b544401bf1155e0a90038bd160fd7c644d3ccbb5/.github/workflows/security-checks.yml#L44-L70).
