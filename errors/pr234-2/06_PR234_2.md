The failing job (ID: 59844664049) shows that both "Integration Tests" and "Build Check" steps were skipped. As a result, the workflow ended with the message: ❌ One or more test jobs failed and exited with code 1.

**Solution:**

1. **Check why tests/builds were skipped:**  
   The workflow is designed to skip key steps ("Integration Tests" and "Build Check").  
   This can happen if:
   - Conditional checks in your workflow file (`.github/workflows/tests.yml`) are causing steps to be skipped.
   - The branches, tags, or event triggers are not set correctly.
   - Required environment variables, dependencies, or configuration files are missing.

2. **Code suggestions:**
   - Open `.github/workflows/tests.yml` and review the conditions (`if:`, `env:`, or `jobs.<job>.steps.<step>.if`) for the skipped steps.
   - Ensure they do not unintentionally skip on Pull Request events or your current branch.
   - Example fix: If you find this in your workflow step:
     ```yaml
     if: github.event_name == 'push'
     ```
     Consider changing to:
     ```yaml
     if: github.event_name == 'push' || github.event_name == 'pull_request'
     ```
   - Make sure all required dependencies and configuration files are present and available to the workflow.

3. **Next steps:**
   - Edit `.github/workflows/tests.yml` to fix any incorrect conditional statements.
   - Push these changes and rerun the workflow.

Would you like me to fetch the contents of `.github/workflows/tests.yml` to provide more targeted code suggestions?
