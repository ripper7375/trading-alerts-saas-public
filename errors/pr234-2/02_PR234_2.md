The failing job encountered a TypeScript error during compile:

- In lib/api/mt5-client.ts, line 11, there is an import from @/types/tier that attempts to use UserTier.
- Error: Module '@/types/tier' has no exported member 'UserTier'.

**Solution:**

1. Open lib/api/mt5-client.ts and review the import statement at line 11.
2. Check the file types/tier (should be located at src/types/tier.ts or similar):
   - Ensure UserTier is either defined and exported in types/tier.
   - If missing, export it like:
     ```ts
     export type UserTier = ... // your definition
     ```
   - If UserTier was renamed or removed, update the import/use in mt5-client.ts to the correct type name that exists.
3. If you have recently renamed the type or removed it, review the changes and either restore it or update code using UserTier accordingly.

**Example fix in mt5-client.ts:**

```ts
// OLD
import { UserTier } from '@/types/tier';

// NEW (if renamed to Tier)
import { Tier } from '@/types/tier';
```

Or, add UserTier to types/tier.ts:

```ts
export type UserTier = {
  /* your type definition */
};
```

**Action:**

- Correct the import or type definition, then commit and push to re-run the workflow.

If you want the exact code for the files referenced, let me know the correct location of types/tier or your intended type structure.
