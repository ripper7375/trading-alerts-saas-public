The job failed due to a TypeScript error in ./lib/api/mt5-client.ts at line 11:

```
Type error: Module '"@/types/tier"' has no exported member 'UserTier'.
```

**Solution Steps:**

1. Open `@/types/tier` and verify the export. Make sure `UserTier` is correctly exported, for example:

   ```ts
   // In types/tier.ts
   export type UserTier = ...;
   ```

   If `UserTier` does not exist, either:
   - Export it if it’s defined, or
   - Refactor the import in `mt5-client.ts` to use the correct type name.

2. In `./lib/api/mt5-client.ts`, ensure the import matches what’s exported. For example:

   ```ts
   import { UserTier } from '@/types/tier';
   ```

   If the type is named differently, update both the import and usage accordingly.

**Code Suggestion:**

- Add this to `@/types/tier.ts` if missing:

  ```ts
  export type UserTier = /* your type definition here */;
  ```

- Or, fix the import in `mt5-client.ts`, matching the actual exported member.

**Summary:**  
Verify `UserTier` exists and is exported from `@/types/tier`, or update your import and usage to match the correct type name. This will resolve the build error and allow your workflow to complete.
