The failing job 60728720941 failed because one or more dependent test jobs did not pass. Based on the workflow configuration (.github/workflows/tests.yml, ref: 275c428df0915f918d6b796682d7ebb30a890d5e), the final test-summary job checks if unit-and-component-tests, integration-tests, or build-check jobs failed, and exits with code 1 if any did.

To solve this:

1. Identify and fix the root cause in the dependent jobs:
   - Review the outputs and logs for unit-and-component-tests, integration-tests, and build-check jobs to locate the specific failure.
   - Typical issues in this workflow are:
     - Failing or skipped unit/component/integration tests (`pnpm run test:ci` or `pnpm run test:integration`)
     - Failed code checks (type-check with TypeScript, ESLint, or Next.js build issues)
     - Dependency installation failures
     - Bundle size exceeding defined thresholds (see the build-check step)
2. After identifying the specific failure (e.g., a failing test, dependency error, or bundle size issue), update the corresponding code or configuration:
   - Fix failing tests or update test scripts.
   - Address lint/type errors.
   - Reduce bundle size if it exceeds the thresholds in the build-check step.
   - Ensure the pnpm lockfile is correct if dependency installation fails.

Sample actionable fix (for a failing test):

- Edit the failing test file and correct the logic or mocks.
- Commit and push the changes to retrigger the workflow.

Review the detailed logs of each test/build job to pinpoint the specific reason for the initial failure, then apply the relevant code or config changes.
