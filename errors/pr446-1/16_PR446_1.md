The failing job **65817970904** is failing during the **TypeScript check** step (`pnpm run type-check`) due to a strict TS rule about accessing `process.env` keys.

### Root cause

From the logs:

- **`hooks/use-ohlcv-socket.ts(76,19): error TS4111`**
- Message: `Property 'NEXT_PUBLIC_MT5_WS_URL' comes from an index signature, so it must be accessed with ['NEXT_PUBLIC_MT5_WS_URL'].`

In `hooks/use-ohlcv-socket.ts` at ref `d09f59ad41f5d801c5e38f4900dee0d4d2513c78`, you currently have:

```ts
const wsUrl = process.env.NEXT_PUBLIC_MT5_WS_URL ?? 'http://localhost:5001';
```

With your current TS configuration/types, `process.env` is treated as an index-signature object, so dot-access is rejected.

### Fix (recommended)

Change dot access to bracket access:

**`hooks/use-ohlcv-socket.ts`** (around line 76)

```ts
const wsUrl = process.env['NEXT_PUBLIC_MT5_WS_URL'] ?? 'http://localhost:5001';
```

That resolves **TS4111** and should allow the `Run TypeScript check` step in `.github/workflows/tests.yml` to pass.

### Optional hardening (runtime safety)

If you want to avoid empty-string env values:

```ts
const wsUrl =
  process.env['NEXT_PUBLIC_MT5_WS_URL']?.trim() || 'http://localhost:5001';
```

### Note about the Codecov messages

After the TypeScript failure, Codecov reports missing `./coverage/lcov.info`, but it also states it **exits with status code 0** (so it’s not causing the job failure). Once the TypeScript check passes, tests should run and generate coverage as usual (assuming your `test:ci` script outputs `coverage/lcov.info`).
