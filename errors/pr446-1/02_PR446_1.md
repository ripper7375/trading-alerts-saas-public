The failing job `65817970870` is failing during `pnpm run build` (Next.js type-check), not during the bundle-size step. The error is:

- **File:** `hooks/use-ohlcv-socket.ts` (ref `d09f59ad41f5d801c5e38f4900dee0d4d2513c78`)
- **Line:** 76
- **Error:** `Property 'NEXT_PUBLIC_MT5_WS_URL' comes from an index signature, so it must be accessed with ['NEXT_PUBLIC_MT5_WS_URL'].`

## Fix (code change)

Update the `process.env` access to bracket notation so TypeScript is satisfied.

**Current code (fails):**

```ts
const wsUrl = process.env.NEXT_PUBLIC_MT5_WS_URL ?? 'http://localhost:5001';
```

**Replace with:**

```ts
const wsUrl = process.env['NEXT_PUBLIC_MT5_WS_URL'] ?? 'http://localhost:5001';
```

That’s the minimal change needed to make the build/type-check pass.

## Optional improvement (avoid invalid URLs)

If you want to be extra defensive (e.g., empty string), use:

```ts
const wsUrl =
  process.env['NEXT_PUBLIC_MT5_WS_URL']?.trim() || 'http://localhost:5001';
```

## Related warning (not the failure, but should be fixed)

The logs also show:
`⚠ Mismatching @next/swc version, detected: 15.5.7 while Next.js is on 15.5.11.`

Align versions by ensuring `next` and `@next/swc-*` resolve to the same version (typically by reinstalling after lockfile update, or adjusting any overrides/resolutions if you have them). This warning won’t fail the job here, but it can cause inconsistent builds.
