The job failed due to a TypeScript compile error in hooks/use-ohlcv-socket.ts at line 76:

Property 'NEXT_PUBLIC_MT5_WS_URL' comes from an index signature, so it must be accessed with ['NEXT_PUBLIC_MT5_WS_URL'].

To fix this, update line 76 from:

```typescript
const wsUrl = process.env.NEXT_PUBLIC_MT5_WS_URL ?? 'http://localhost:5001';
```

to:

```typescript
const wsUrl = process.env['NEXT_PUBLIC_MT5_WS_URL'] ?? 'http://localhost:5001';
```

This change correctly accesses environment variables defined by an index signature, resolving the compile error.

Additionally, the logs mention a mismatching @next/swc version; synchronize its version with Next.js in your package.json to avoid future warnings.

See failing code: [hooks/use-ohlcv-socket.ts#L76](https://github.com/ripper7375/trading-alerts-saas-public/blob/d09f59ad41f5d801c5e38f4900dee0d4d2513c78/hooks/use-ohlcv-socket.ts)
