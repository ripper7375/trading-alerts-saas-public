The job failed due to ESLint errors where variables were defined but never used. To fix this, you need to adjust your code to comply with the @typescript-eslint/no-unused-vars rule. The errors occurred in these files:

1. app/api/disbursement/config/route.ts (line 48): The request parameter is unused in the GET handler.
   Solution: Prefix unused parameters with an underscore (\_), or remove them if not needed.

   Example:

   ```typescript
   export async function GET(_request: NextRequest): Promise<NextResponse> {
     // ...rest of the code...
   }
   ```

   See the code here (ref: 5176e02f9ef44eba3b2ea25f76b06d05cc49a668).

2. app/api/disbursement/health/route.ts (line 39): The request parameter is unused in the GET handler.
   Solution: Same as above.

   ```typescript
   export async function GET(_request: NextRequest): Promise<NextResponse> {
     // ...rest of the code...
   }
   ```

   See the code here (ref: 5176e02f9ef44eba3b2ea25f76b06d05cc49a668).

3. lib/disbursement/cron/disbursement-processor.ts (line 15): The DisbursementProvider type is imported but not used.
   Solution: Remove the import if it is not needed.

   Example:

   ```typescript
   // Remove this line if not used elsewhere:
   // import type { DisbursementProvider } from '@/types/disbursement';
   ```

   See the code here (ref: 5176e02f9ef44eba3b2ea25f76b06d05cc49a668).

After making these changes, your build should succeed.
