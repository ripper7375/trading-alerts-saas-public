I'll analyze the failing job to find a solution. Let me get the logs first. The TypeScript type-check is failing with several errors. Let me examine the problematic files: ## Solution for TypeScript Compilation Errors

The job is failing due to 5 TypeScript errors. Here are the fixes:

### 1. **Unused `request` parameters** (3 errors)

Remove the `request` parameter prefix or use it:

**app/api/disbursement/config/route.ts:48**

```typescript
// Change from:
export async function GET(request: NextRequest): Promise<NextResponse> {

// To:
export async function GET(): Promise<NextResponse> {
```

**app/api/disbursement/health/route.ts:39**

```typescript
// Change from:
export async function GET(request: NextRequest): Promise<NextResponse> {

// To:
export async function GET(): Promise<NextResponse> {
```

**app/api/disbursement/reports/affiliate/[affiliateId]/route.ts:52**

```typescript
// Prefix with underscore if might be used later:
export async function GET(_request: NextRequest, ... ): Promise<NextResponse> {
```

### 2. **Type errors in app/api/webhooks/riseworks/route.ts** (2 errors)

**Line 73 & 115** - The `payload` field expects `InputJsonValue` but is receiving `unknown` and incompatible objects. Cast the values explicitly:

```typescript
// Line 69-73, change:
await prisma.riseWorksWebhookEvent.create({
  data: {
    eventType: 'unknown',
    provider: 'RISE',
    payload: parsedPayload,  // ❌ Type error

// To:
await prisma. riseWorksWebhookEvent.create({
  data: {
    eventType: 'unknown',
    provider: 'RISE',
    payload: parsedPayload as Record<string, unknown>,  // ✅ Fixed

// Line 111-115, change:
await prisma.riseWorksWebhookEvent.create({
  data: {
    eventType,
    provider: 'RISE',
    payload: webhookData,  // ❌ Type error

// To:
await prisma.riseWorksWebhookEvent.create({
  data: {
    eventType,
    provider: 'RISE',
    payload: webhookData as Record<string, unknown>,  // ✅ Fixed
```

### 3. **Unused import in lib/disbursement/cron/disbursement-processor.ts:15**

Remove the unused type import:

```typescript
// Remove line 15:
import type { DisbursementProvider } from '@/types/disbursement';
```

### 4. **Type error at line 214** - `null` not assignable to filter type

```typescript
// Line 212-214, change:
const accounts = await this.prisma.affiliateRiseAccount.findMany({
  where: {
    riseId: { not: null },  // ❌ Type error

// To:
const accounts = await this.prisma.affiliateRiseAccount.findMany({
  where: {
    riseId: { not: null as unknown as undefined },  // ✅ Fixed
// Or simply:
  where: {
    NOT: { riseId: null },  // ✅ Alternative fix
```

Apply these changes and the type-check should pass.
