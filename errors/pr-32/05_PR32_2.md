I'll help you find a solution for the failing job. Let me first get the logs to see what's causing the failure. The type-check job is failing with two TypeScript errors. Let me examine the failing files: ## Solution for Failing Job

The type-check job is failing due to two TypeScript errors:

### Error 1: Unused Variable (Line 52)

**File:** `app/api/disbursement/reports/affiliate/[affiliateId]/route.ts`

The `request` parameter is declared but never used.

**Fix:** Remove the unused parameter or prefix it with an underscore:

```typescript
export async function GET(
  _request: NextRequest,
  context: RouteParams
): Promise<NextResponse> {
```

### Error 2: Type Incompatibility (Line 213)

**File:** `lib/disbursement/cron/disbursement-processor.ts`

The query filter `{ riseId: null }` is invalid. TypeScript expects `riseId` to be a string or StringFilter, not null.

**Fix:** Use `isNot` instead of `NOT` with the proper filter:

```typescript
// Replace line 211-214 with:
const accounts = await this.prisma.affiliateRiseAccount.findMany({
  where: {
    riseId: { not: null },
  },
});
```

These two changes will resolve all TypeScript errors and allow the CI pipeline to pass.
