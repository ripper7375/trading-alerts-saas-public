I'll analyze the failing job to find a solution for you. I can see the TypeScript type checking is failing with several errors. Let me retrieve the files to understand the issues: ## Solution for TypeScript Type Check Failures

The build is failing due to TypeScript errors. Here are the fixes needed:

### 1. **app/api/webhooks/riseworks/route.ts** - Lines 73 & 115

**Problem**: Type `unknown` and complex objects aren't assignable to Prisma's `InputJsonValue`.

**Solution**: Cast the values to `InputJsonValue`:

```typescript
// Line 73 - Cast parsedPayload
payload:  parsedPayload as Prisma.InputJsonValue,

// Line 115 - Cast webhookData
payload: webhookData as Prisma.InputJsonValue,
```

Add this import at the top of the file:

```typescript
import { Prisma } from '@prisma/client';
```

### 2. **lib/disbursement/cron/disbursement-processor.ts** - Lines 15 & 214

**Problem**:

- Line 15: `DisbursementProvider` type is imported but never used
- Line 214: `null` is not assignable to the filter type

**Solution**:

Remove the unused import on line 15:

```typescript
// Remove this line:
import type { DisbursementProvider } from '@/types/disbursement';
```

For line 214, change the filter to handle null properly:

```typescript
// Change from:
riseId: { not: null },

// To:
riseId: { not: null as string | null },
```

Or use a more explicit filter:

```typescript
NOT: { riseId: null },
```

### 3. **Unused Request Parameters** - Lines 48, 39, 52 in various files

**Problem**: The `request` parameter is declared but never used in GET handlers.

**Solution**: Prefix with underscore to indicate intentionally unused:

```typescript
// app/api/disbursement/config/route.ts line 48:
export async function GET(_request: NextRequest): Promise<NextResponse> {

// app/api/disbursement/health/route.ts line 39:
export async function GET(_request:  NextRequest): Promise<NextResponse> {

// app/api/disbursement/reports/affiliate/[affiliateId]/route.ts line 52:
export async function GET(
  _request: NextRequest,
  // ... rest of parameters
```

These changes will resolve all TypeScript errors and allow the build to pass.
