The primary failure in job 59546586039 is due to two main issues:

1. **React Testing Issue:**
   - The error messages indicate that "An update to PlanSelector inside a test was not wrapped in act(...)."
   - To fix this, you must wrap any state updates or effects inside your test files for components like `PlanSelector` using React’s `act()` helper from the testing library:

   ```js
   import { act } from 'react-dom/test-utils'; // or from '@testing-library/react'

   act(() => {
     // Any code that triggers updates
     fireEvent.click(screen.getByText('Choose Plan'));
   });
   ```

   Update your tests (usually in files like `__tests__/components/PlanSelector.test.tsx`) to ensure all updates are inside `act()`.

2. **Environment Variable Not Set:**
   - The logs show `CRON_SECRET not configured` and related 500/401 Unauthorized errors in `app/api/cron/process-pending-disbursements/route.ts`.
   - Solution: Configure the `CRON_SECRET` environment variable in your GitHub Actions workflow or test setup so your API endpoints that rely on it can authorize requests.
     - In your workflow file (`.github/workflows/ci-nextjs-progressive.yml`), add:
       ```yaml
       env:
         CRON_SECRET: your-cron-secret-here
       ```
       Or set it in the test runner environment.

Additionally, some failing tests show the error “Jest encountered an unexpected token,” which indicates your Babel/Jest configuration might not be correctly handling all project syntax (especially with dependencies like `next-auth`). Ensure your Babel config includes necessary presets:

```json
{
  "presets": ["next/babel"]
}
```

And check that your Jest config transforms your source files:

```js
transform: {
  "^.+\\.(js|jsx|ts|tsx)$": "babel-jest"
}
```

**Summary of Solutions:**

- Wrap test updates on React components in `act()`.
- Ensure `CRON_SECRET` is set and available to your tests and CI jobs.
- Update Babel/Jest config to support your syntax/dependencies.

If you need code suggestions for a specific file, let me know which test or API file you want a fix for.
