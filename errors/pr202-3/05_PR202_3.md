I'll analyze the failing job to find a solution for you. Let me examine the test file and the API route to understand the issue better. ## Solution for Failing Test

The test is failing because the API route is returning `"Internal server error"` instead of `"MT5 service error"` when handling an `MT5ServiceError`.

**Issue**: The error handler in the API route isn't properly catching and returning the expected error message for `MT5ServiceError`.

Let me check the actual API route implementation to provide the exact fix:
