The job failed due to a TypeScript compilation error in scripts/monitor-mt5-pipeline.ts on line 42:

Property 'REDIS_URL' comes from an index signature, so it must be accessed with ['REDIS_URL'].

To fix the error, change:

```typescript
this.redisClient = createClient({ url: process.env.REDIS_URL });
```

to:

```typescript
this.redisClient = createClient({ url: process.env['REDIS_URL'] });
```

This adjustment ensures proper access of environment variables when TypeScript index signatures are in play. Make this change in scripts/monitor-mt5-pipeline.ts (see ref a3069b086b57321a72d82478444477ea425b4c6e), then re-run your workflow to resolve the failure.
