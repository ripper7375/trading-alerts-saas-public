The job failed due to a TypeScript error in scripts/monitor-mt5-pipeline.ts at line 42. The error message is:

Property 'REDIS_URL' comes from an index signature, so it must be accessed with ['REDIS_URL'].

To fix this, update the way you access the environment variable in your code. Replace:

```typescript
this.redisClient = createClient({ url: process.env.REDIS_URL });
```

with:

```typescript
this.redisClient = createClient({ url: process.env['REDIS_URL'] });
```

This change ensures compatibility with strict typing when accessing environment variables in TypeScript. The corrected code will resolve the compile error and allow your workflow to complete. See the code in context: scripts/monitor-mt5-pipeline.ts (ref: a3069b086b57321a72d82478444477ea425b4c6e).
