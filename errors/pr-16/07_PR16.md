I'll analyze the failing job to find a solution. Let me get the logs first. Let me fetch the files with the TypeScript errors: ## TypeScript Errors Fix

The build is failing due to several TypeScript strict type checking errors. Here are the solutions:

### 1. **app/(dashboard)/admin/fraud-alerts/[id]/page.tsx**

**Line 101**: Remove unused `router` variable:

```tsx
// Remove this line:
const router = useRouter();
```

**Line 122**: Use bracket notation to access `params.id`:

```tsx
// Change:
fetchAlert();
}, [params.id]);

// To:
fetchAlert();
}, [params['id']]);
```

### 2. **app/(dashboard)/admin/fraud-alerts/page.tsx**

**Lines 17, 20**: Remove unused imports:

```tsx
// Change:
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { FraudAlertCard } from '@/components/admin/FraudAlertCard';
import { FraudPatternBadge } from '@/components/admin/FraudPatternBadge';

// To:
import { Card, CardContent } from '@/components/ui/card';
import { Button } from '@/components/ui/button';
import { FraudAlertCard } from '@/components/admin/FraudAlertCard';
```

### 3. **app/api/payments/dlocal/check-three-day-eligibility/route.ts**

**Line 24**: Remove unused `request` parameter:

```typescript
// Change:
export async function GET(request: NextRequest): Promise<NextResponse> {

// To:
export async function GET(): Promise<NextResponse> {
```

### 4. **app/api/payments/dlocal/validate-discount/route.ts**

**Lines 83-104**: Fix the Prisma include statement. Remove the `affiliate` property from the include object:

```typescript
// Change:
const affiliateCode = await prisma.affiliateCode.findFirst({
  where: {
    code: normalizedCode,
    status: 'ACTIVE',
  },
  include: {
    affiliate: {
      select: {
        id: true,
        status: true,
      },
    },
  },
});

// Then accessing:
if (affiliateCode. affiliate?. status !== 'ACTIVE') { ... }
affiliateCode.affiliateId

// To:
const affiliateCode = await prisma.affiliateCode.findFirst({
  where: {
    code: normalizedCode,
    status: 'ACTIVE',
  },
  include: {
    affiliateProfile: true,
  },
});

// Then access:
if (affiliateCode. affiliateProfile?.status !== 'ACTIVE') { ... }
affiliateCode.affiliateProfileId
```

### 5. **components/payments/PaymentMethodSelector.tsx**

**Lines 227 & 237**: Add null checks for `fallbackMethods[0]`:

```tsx
// Change both occurrences:
onChange(fallbackMethods[0].id);

// To:
onChange(fallbackMethods[0]?.id);
```

Apply these fixes and the TypeScript type checking should pass.
