The CI job failed due to TypeScript/ESLint errors that must be fixed for a successful build:

1. **Unused Variables**
   - In `app/(dashboard)/admin/fraud-alerts/[id]/page.tsx`:  
     `'router' is assigned a value but never used.`  
     Solution: Remove the line `const router = useRouter();` if you do not use `router` anywhere in the file (see [file](<https://github.com/ripper7375/trading-alerts-saas-public/blob/22c36d75fb5d8e89b02fe35748e156a9722d3927/app/(dashboard)/admin/fraud-alerts/%5Bid%5D/page.tsx#L101>)).
   - In `app/(dashboard)/admin/fraud-alerts/page.tsx`:  
     `'CardHeader'`, `'CardTitle'`, and `'FraudPatternBadge'` are defined/imported but never used.  
     Solution: Remove these imports if they are unused (see [file](<https://github.com/ripper7375/trading-alerts-saas-public/blob/22c36d75fb5d8e89b02fe35748e156a9722d3927/app/(dashboard)/admin/fraud-alerts/page.tsx#L17-L20>)).
   - In `app/api/payments/dlocal/check-three-day-eligibility/route.ts`:  
     `'request'` is defined but never used in the function signature.  
     Solution: Replace `request` with `_request` if it is not used, or remove it from the function parameters (see [file](https://github.com/ripper7375/trading-alerts-saas-public/blob/22c36d75fb5d8e89b02fe35748e156a9722d3927/app/api/payments/dlocal/check-three-day-eligibility/route.ts#L24)).

2. **React Hook Dependency Warning**
   - In `components/payments/PriceDisplay.tsx`:  
     `useEffect` is missing `fetchConversion` in its dependency array.  
     Solution:  
     Update this `useEffect` so its dependency list includes `fetchConversion`, or move `fetchConversion` into the effect or wrap it with `useCallback` if you want to avoid lint warnings.  
     Example fix (if `fetchConversion` does not depend on props/state):
     ```tsx
     useEffect(() => {
       setLoading(true);
       fetchConversion();
       // eslint-disable-next-line react-hooks/exhaustive-deps
     }, [usdAmount, currency]);
     ```
     Alternatively, define `fetchConversion` using `useCallback(...)`.

**After making these changes, commit and push again. The job should pass these checks.**
