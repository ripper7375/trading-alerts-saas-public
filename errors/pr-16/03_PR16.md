I'll help you find a solution for the failing job. Let me first get the job logs to understand what went wrong. Let me retrieve the failing files to provide specific fixes: ## Solution for Failing Job 58611674530

The build is failing due to **ESLint errors** for unused variables. Here are the specific issues and fixes:

### 1. **app/(dashboard)/admin/fraud-alerts/[id]/page.tsx** (Line 101)

**Error:** `'router' is assigned a value but never used`

**Fix:** Remove the unused `router` variable:

```tsx
// Line 101 - Remove this line:
const router = useRouter();
```

Also remove the import if it's no longer needed:

```tsx
// Line 15 - Change from:
import { useParams, useRouter } from 'next/navigation';
// To:
import { useParams } from 'next/navigation';
```

---

### 2. **app/(dashboard)/admin/fraud-alerts/page.tsx** (Lines 17, 20)

**Error:** Unused imports `CardHeader`, `CardTitle`, and `FraudPatternBadge`

**Fix:** Remove these unused imports:

```tsx
// Line 17 - Change from:
import { Card, CardContent, CardHeader, CardTitle } from '@/components/ui/card';
// To:
import { Card, CardContent } from '@/components/ui/card';

// Line 20 - Remove this line entirely:
import { FraudPatternBadge } from '@/components/admin/FraudPatternBadge';
```

---

### 3. **app/api/payments/dlocal/check-three-day-eligibility/route.ts** (Line 24)

**Error:** `'request' is defined but never used`

**Fix:** Prefix the parameter with underscore to indicate it's intentionally unused:

```typescript
// Line 24 - Change from:
export async function GET(request: NextRequest): Promise<NextResponse> {
// To:
export async function GET(_request: NextRequest): Promise<NextResponse> {
```

---

### 4. **components/payments/PriceDisplay. tsx** (Line 158)

**Warning:** Missing dependency `fetchConversion` in useEffect

**Fix:** Wrap `fetchConversion` in useCallback to stabilize the reference:

```tsx
// Add useCallback to imports (line 18):
import { useState, useEffect, useCallback } from 'react';

// Wrap fetchConversion with useCallback (around line 120):
const fetchConversion = useCallback(async (): Promise<void> => {
  try {
    setError(null);
    // ... rest of the function
  } finally {
    setLoading(false);
    setRefreshing(false);
  }
}, [usdAmount, currency]); // Add dependencies

// Update useEffect (line 155):
useEffect(() => {
  setLoading(true);
  fetchConversion();
}, [fetchConversion]); // Now include fetchConversion
```

---

After making these changes, the build should pass successfully. All issues are related to code quality enforcement rather than functionality problems.
