The failing job 59795506051 is caused by the "Dependency Security Audit" step in your workflow. The summary output is:

❌ Dependency audit: FAILED  
✅ Type check: PASSED  
✅ Linting: PASSED  
✅ Secrets scan: PASSED  
✅ Security headers: PASSED  
❌ Some security checks failed

Your workflow runs pnpm audit with --audit-level=high as the critical command:

```yaml
- name: Run security audit
  run: |
    echo "🔍 Running pnpm security audit..."
    pnpm audit --audit-level=high
    echo "✅ No high/critical vulnerabilities found"
```

This means at least one dependency is reported as having a "high" or greater vulnerability.

Solution:

1. Run pnpm audit locally to see the details of the reported vulnerabilities.
2. Upgrade the affected dependency/dependencies to a safe version:
   ```
   pnpm update <package-name>
   ```
   or adjust your package.json and lockfile to the required secure versions.
3. If the vulnerability cannot be easily fixed, and you have assessed it as a false positive or non-exploitable, use a .auditrc or ignore mechanism (see pnpm audit docs), but document your reasoning in the repo.
4. Re-run the workflow to confirm the audit passes.

Review the dependencies and ensure that no critical/high issues remain unresolved before merging. The job definition and logic can be found in the Security Checks workflow: [.github/workflows/security-checks.yml](https://github.com/ripper7375/trading-alerts-saas-public/blob/faa6773a87220ab9c823fb3186ef3de75b3f5a85/.github/workflows/security-checks.yml).
