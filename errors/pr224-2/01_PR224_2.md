The failing job encountered two main issues:

1. Unhandled React state updates in tests:
   - Error: "An update to PlanSelector inside a test was not wrapped in act(...)."
   - Solution: In your tests that interact with components triggering state updates (especially using SWR, React hooks, or async handlers), wrap interactions and assertions in React's act() utility. For example:

   ```javascript
   import { act } from 'react-dom/test-utils';

   await act(async () => {
     // your code that triggers state updates
   });
   ```

   This applies to any tests involving PlanSelector or other components that rely on async fetching or state changes.

2. Cron job test failures due to missing or incorrect CRON_SECRET:
   - Error: "CRON_SECRET not configured" and test expects status 401 for missing/misconfigured secrets.
   - Solution: Ensure the CRON_SECRET environment variable is set in your CI environment. If you use dotenv or a CI secrets manager, make sure CRON_SECRET is present.

   Example in GitHub Actions:

   ```yaml
   env:
     CRON_SECRET: ${{ secrets.CRON_SECRET }}
   ```

   Also, your test file (**tests**/api/cron/process-pending.test.ts) resets modules and expects specific statuses for missing/malformed secrets.

Additional notes:

- One test in **tests**/lib/cache/indicator-cache.test.ts failed (“should store data in cache”). If this is persistent, check your cache implementation and ensure test isolation in beforeEach/afterEach blocks.
- For coverage uploads, ensure the report file (e.g., coverage/lcov.info) is generated. Install coverage tools if missing.

Links to relevant files:

- [**tests**/lib/cache/indicator-cache.test.ts (ref 3a3ff7905273eca8508ed066cbc8746028b62f7f)](https://github.com/ripper7375/trading-alerts-saas-public/blob/3a3ff7905273eca8508ed066cbc8746028b62f7f/__tests__/lib/cache/indicator-cache.test.ts)
- [app/api/cron/process-pending-disbursements/route.ts (ref 3a3ff7905273eca8508ed066cbc8746028b62f7f)](https://github.com/ripper7375/trading-alerts-saas-public/blob/3a3ff7905273eca8508ed066cbc8746028b62f7f/app/api/cron/process-pending-disbursements/route.ts)
- [**tests**/api/cron/process-pending.test.ts (ref 3a3ff7905273eca8508ed066cbc8746028b62f7f)](https://github.com/ripper7375/trading-alerts-saas-public/blob/3a3ff7905273eca8508ed066cbc8746028b62f7f/__tests__/api/cron/process-pending.test.ts)

Prioritize fixing the unwrapped act() calls and establishing correct environment variables. If you need targeted code updates, share the file and line where PlanSelector is tested for more specific guidance.
