I'll help you find a solution for the failing job. Let me first get the logs to understand what went wrong. Now let me check the files to understand the context: ## Solution for Failing Job

The build is failing due to **ESLint errors** for unused variables. Here's how to fix each issue:

### File: `app/api/payments/dlocal/create/route.ts`

**Line 27** - Remove unused import:

```typescript
// Change this:
import {
  PRICING,
  isDLocalCountry,
  getPlanDuration,
} from '@/lib/dlocal/constants';

// To this (remove isDLocalCountry):
import { PRICING, getPlanDuration } from '@/lib/dlocal/constants';
```

**Lines 134-135** - Change `let` to `const` for variables that aren't reassigned:

```typescript
// Change this:
let finalAmount = localAmount;
let discountAmount = 0;

// To this:
const finalAmount = localAmount;
const discountAmount = 0;
```

**Line 147** - Prefix unused variable with underscore:

```typescript
// Change this:
const orderId = `order-${userId}-${Date.now()}`;

// To this:
const _orderId = `order-${userId}-${Date.now()}`;
```

### File: `app/api/webhooks/dlocal/route.ts`

**Line 21** - Remove unused import:

```typescript
// Change this:
import {
  verifyWebhookSignature,
  mapDLocalStatus,
  extractUserIdFromOrderId,
} from '@/lib/dlocal/dlocal-payment.service';

// To this (remove extractUserIdFromOrderId):
import {
  verifyWebhookSignature,
  mapDLocalStatus,
} from '@/lib/dlocal/dlocal-payment.service';
```

These changes will resolve all ESLint errors and allow the build to pass.
