The failure in job 59545440171 is caused by the "Test Summary" step detecting that one or more of the dependent jobs (unit-and-component-tests, integration-tests, build-check) did not succeed. The logs show:

❌ One or more test jobs failed

To fix this, you should review the actual test jobs (unit-and-component-tests, integration-tests, build-check) for their specific failure details. However, the provided log shows:

- Integration Tests: skipped
- Build Check: skipped

This means these jobs likely did not run because their dependencies were not satisfied (possibly due to failures or skips upstream).

Solution:

- Ensure unit-and-component-tests completes successfully, as integration-tests and build-check depend on it.
- Confirm none of the jobs are skipped due to matrix exclusion, conditional logic, or workflow misconfiguration.
- Check for failures in the unit-and-component-tests step (especially pnpm install, type checks, linting, or test:ci script).
- If the jobs are being skipped unintentionally, verify your branch filters and workflow triggers in .github/workflows/tests.yml, especially lines 5 and 7, to confirm your PR is targeting main or develop.

If you find an error in any test, address that error directly. If jobs are skipped due to workflow conditions, adjust your triggers or matrix setup to ensure all required jobs run.

Reference: [.github/workflows/tests.yml (ref: 26872ef0)](https://github.com/ripper7375/trading-alerts-saas-public/blob/26872ef08103ac5b1f87331f1534cbb9389d7ce4/.github/workflows/tests.yml)
