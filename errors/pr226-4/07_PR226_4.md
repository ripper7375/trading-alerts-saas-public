I'll analyze the failing job to identify the issue and provide a solution. Let me examine the test files and the route files that are failing. ## Solution for Failing Tests

The job is failing due to **Jest parsing errors** when trying to import `next-auth`. The error indicates that Jest encounters an unexpected token when parsing the `next-auth` module, which is ESM (ECMAScript Module) code that Jest isn't properly transforming.

### Root Cause

The test files are importing from `app/api/indicators/[symbol]/[timeframe]/route.ts` which imports `next-auth` at line 12. Jest cannot parse this ESM module because it's not properly configured to transform `next-auth` and its dependencies.

### Solution

Update your Jest configuration to properly handle `next-auth` and other ESM modules. Modify your `jest.config.js` (or create/update it):

```javascript
const nextJest = require('next/jest');

const createJestConfig = nextJest({
  dir: './',
});

const customJestConfig = {
  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
  testEnvironment: 'jest-environment-jsdom',
  moduleNameMapper: {
    '^@/(. *)$': '<rootDir>/$1',
  },
  // Add this to transform next-auth and other problematic ESM modules
  transformIgnorePatterns: [
    'node_modules/(? !(next-auth|@auth|@panva|uuid|nanoid)/)',
  ],
  testMatch: ['**/__tests__/**/*.test. ts', '**/__tests__/**/*.test.tsx'],
};

module.exports = createJestConfig(customJestConfig);
```

### Key Changes:

1. **`transformIgnorePatterns`**: Configure Jest to transform `next-auth` and its dependencies (`@auth`, `@panva`, `uuid`, `nanoid`) instead of ignoring them.

### Alternative Quick Fix

If you want to keep the mocking approach but avoid the import issue, modify the test file to mock the route handler before importing it:

```typescript
// At the top of __tests__/part20/api/indicators. test.ts
jest.mock('next-auth', () => ({
  __esModule: true,
  getServerSession: jest.fn(async () => mockSessionData),
}));

jest.mock('next-auth/next', () => ({
  __esModule: true,
  default: jest.fn(),
}));

// Mock the route module entirely if needed
jest.mock('@/app/api/indicators/[symbol]/[timeframe]/route', () => {
  const actual = jest.requireActual(
    '@/app/api/indicators/[symbol]/[timeframe]/route'
  );
  return actual;
});
```

The Jest configuration change is the recommended approach as it will fix the parsing issue across all test files that import modules using `next-auth`.
