I'll analyze the failing job to find a solution. Let me get the logs first. Let me check the file where the error is occurring: ## Solution

The build is failing due to a TypeScript type error on **line 24** of `lib/db/queries.ts`. The issue is that `VALID_SYMBOLS. includes()` expects a specific union type, but `normalizedSymbol` (which is `string`) is too broad.

### The Problem

```typescript
const isValidSymbol = VALID_SYMBOLS.includes(normalizedSymbol);
```

TypeScript's `includes()` method for arrays expects the parameter to match the array's element type exactly. Since `normalizedSymbol` is typed as `string`, it doesn't match the literal union type that `VALID_SYMBOLS` contains.

### The Fix

Update lines 24-25 in `lib/db/queries.ts` to use type assertions:

```typescript
const isValidSymbol = VALID_SYMBOLS.includes(
  normalizedSymbol as (typeof VALID_SYMBOLS)[number]
);
const isValidTimeframe = VALID_TIMEFRAMES.includes(
  normalizedTimeframe as (typeof VALID_TIMEFRAMES)[number]
);
```

This tells TypeScript that we're asserting the normalized strings conform to the valid types, which is safe because the `includes()` check validates this at runtime.

### Alternative Solution

If you want a more type-safe approach without assertions, you can use type guards:

```typescript
const isValidSymbol = (VALID_SYMBOLS as readonly string[]).includes(
  normalizedSymbol
);
const isValidTimeframe = (VALID_TIMEFRAMES as readonly string[]).includes(
  normalizedTimeframe
);
```

Either approach will resolve the compilation error and allow your build to succeed.
