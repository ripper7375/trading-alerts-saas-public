I'll analyze the failing job to identify the issue and provide a solution. The job is failing due to a **type check failure**. Let me examine the workflow file and then look at the type check errors: ## Solution

The job is failing because the **TypeScript type check** step failed. The logs show:

```
❌ Type check: FAILED
```

### Root Cause

The `type-check` job (lines 44-69 in the workflow) is running `pnpm run type-check` and encountering TypeScript type errors in your code.

### Fix

You need to resolve the TypeScript type errors. Run the type check locally to see the specific errors:

```bash
pnpm run type-check
```

This will show you exactly which files have type errors and what needs to be fixed. Common issues include:

1. **Missing type definitions** - Variables or parameters without proper types
2. **Type mismatches** - Assigning incompatible types
3. **Missing properties** - Objects missing required properties
4. **Incorrect function signatures** - Parameters or return types not matching

Once you identify the errors, fix them by:

- Adding proper type annotations
- Correcting type mismatches
- Ensuring all required properties are present
- Updating function signatures to match their usage

After fixing the type errors locally and confirming `pnpm run type-check` passes, commit and push your changes. The workflow will re-run and should pass all security checks.
