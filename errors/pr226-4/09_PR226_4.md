I'll analyze the failing job to find a solution for you. ## Solution

The TypeScript build is failing because of type mismatches on lines 24-25 in `lib/db/queries.ts`. The `VALID_SYMBOLS. includes()` and `VALID_TIMEFRAMES.includes()` methods expect literal union types, but they're receiving `string` types.

**The Issue:**

```typescript
const normalizedSymbol = symbol.toUpperCase();
const normalizedTimeframe = timeframe.toUpperCase();

const isValidSymbol = VALID_SYMBOLS.includes(normalizedSymbol); // Error: string not assignable
const isValidTimeframe = VALID_TIMEFRAMES.includes(normalizedTimeframe); // Error: string not assignable
```

**The Fix:**

Update the type assertions in the `validateTableName` function:

```typescript
function validateTableName(
  symbol: string,
  timeframe: string
): { isValid: boolean; tableName: string } {
  const normalizedSymbol = symbol.toUpperCase();
  const normalizedTimeframe = timeframe.toUpperCase();

  const isValidSymbol = VALID_SYMBOLS.includes(
    normalizedSymbol as (typeof VALID_SYMBOLS)[number]
  );
  const isValidTimeframe = VALID_TIMEFRAMES.includes(
    normalizedTimeframe as (typeof VALID_TIMEFRAMES)[number]
  );

  if (!isValidSymbol || !isValidTimeframe) {
    return { isValid: false, tableName: '' };
  }

  const tableName = `${normalizedSymbol.toLowerCase()}_${normalizedTimeframe.toLowerCase()}`;
  return { isValid: true, tableName };
}
```

This casts the strings to the specific union types that TypeScript expects, allowing the includes check to work properly while maintaining type safety.
