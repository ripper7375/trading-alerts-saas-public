I'll analyze the failing job to find a solution for you. Now let me get the failing test file to understand the issue: The test is failing at line 239 where it expects `data.sent` to be `2`, but the actual value is different. Looking at the test, it mocks the database to return 2 affiliates but the actual API route is not returning the expected count.

Let check the actual route implementation to see what's wrong:
