I'll analyze the failing job to find a solution for you. Now let me examine the failing test file to understand what's going wrong: ## Solution for Failing Tests

The test failures in `__tests__/lib/cache/indicator-cache.test.ts` are occurring at lines 416 and 523. Based on the test file, there are two failing tests:

1. **Line 416**: "should store data in cache" - expects `getCacheSize()` to be `1` after storing data
2. **Line 523**: "should handle optional bars parameter" - expects cached data to match what was stored

### Root Cause

The issue appears to be with the cache implementation not properly storing or retrieving data. The tests are failing because:

1. The cache size is not being tracked correctly after `setCachedIndicatorData` is called
2. Data retrieval for entries with/without the optional `bars` parameter is not working as expected

### Recommended Fix

You need to check the `lib/cache/indicator-cache.ts` implementation file. Here's what to verify:

**For the cache size issue (line 416):**

```typescript
// In lib/cache/indicator-cache. ts, ensure setCachedIndicatorData increments the cache size
export async function setCachedIndicatorData(
  symbol: string,
  timeframe: string,
  data: any,
  bars?: number
): Promise<void> {
  const key = generateCacheKey(symbol, timeframe, bars);
  // Make sure you're actually storing in the cache Map/Store
  cache.set(key, data);
  stats.sets++;
  // Ensure the cache size reflects the new entry
}
```

**For the optional bars parameter issue (line 523):**

```typescript
// Ensure the cache key generation handles optional bars consistently
function generateCacheKey(
  symbol: string,
  timeframe: string,
  bars?: number
): string {
  const normalizedSymbol = symbol.toUpperCase();
  const normalizedTimeframe = timeframe.toUpperCase();
  // When bars is undefined, don't include it in the key
  return bars !== undefined
    ? `${normalizedSymbol}:${normalizedTimeframe}:${bars}`
    : `${normalizedSymbol}:${normalizedTimeframe}`;
}
```

### Next Steps

1. Review the `lib/cache/indicator-cache.ts` file to verify the cache storage mechanism
2. Ensure `getCacheSize()` returns the actual size of the cache Map/Store
3. Verify that `generateCacheKey()` properly handles the optional `bars` parameter
4. Make sure data serialization/deserialization is working correctly (the test expects objects to be preserved, with `undefined` values stripped during JSON serialization)

Would you like me to examine the implementation file to provide more specific fixes?
