The failing job 59548315382 simply outputs: "❌ One or more test jobs failed" and exits with code 1. This indicates one or more dependency jobs (unit-and-component-tests, integration-tests, or build-check) failed.

To find and fix the root cause, focus on the following steps based on your workflow:

1. Inspect the output of the "unit-and-component-tests," "integration-tests," and "build-check" for specific errors (for example, test failures, lint/type-check errors, or bundle size violations).
2. In most failures, you'll get more details by re-running the workflow and inspecting logs for those jobs directly—look for explicit error messages or failed test outputs.
3. Common issues and solutions:
   - If you find TypeScript or ESLint errors, run pnpm run type-check or pnpm run lint locally and fix reported issues.
   - If there are test failures in pnpm run test:ci or pnpm run test:integration, review and fix the failing tests.
   - If the bundle size exceeds specified thresholds (see the build check script), optimize your code or assets to reduce the .next/ build directory size.

You can see and edit the job definition in .github/workflows/tests.yml at ref b02dea25a5c367d0250e3093b48600dbf3a0c7ca for more details.

If you provide logs or errors from one of the actual test/build jobs, I can help with a targeted code-level fix. For now, start with checking those earlier job outputs for actionable error messages.
