I'll analyze the failing job to find a solution for you. The job is failing due to TypeScript type errors in two files. Let me examine the files to provide specific solutions: ## Solution

You have two TypeScript type errors that need to be fixed:

### 1. **transaction-logger.ts (Line 37)** - JSON type incompatibility

The `details` field type doesn't match Prisma's expected `InputJsonValue`.

**Fix:**

```typescript
details: entry.details ? (entry.details as any) : {},
```

Or more properly, cast it at line 37:

```typescript
details: (entry.details || {}) as any,
```

### 2. **transaction-service.ts (Line 175)** - Status type mismatch

The `status` field in `UpdateData` is typed as `string` but Prisma expects the specific enum type `DisbursementTransactionStatus`.

**Fix at line 150-151:**

```typescript
import {
  PrismaClient,
  DisbursementProvider,
  DisbursementTransactionStatus,
} from '@prisma/client';

// Then update the interface:
interface UpdateData {
  status: DisbursementTransactionStatus;
  completedAt?: Date;
  failedAt?: Date;
  errorMessage?: string;
  providerTxId?: string;
}
```

And update the function parameter at line 144:

```typescript
async updateTransactionStatus(
  id: string,
  status: DisbursementTransactionStatus,
  options?: {
    providerTxId?: string;
    errorMessage?: string;
  }
): Promise<void> {
```

These changes will resolve the type checking errors and allow your build to pass.
