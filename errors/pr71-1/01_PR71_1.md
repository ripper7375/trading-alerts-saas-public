The job failed due to a TypeScript type error in app/api/admin/users/route.ts on line 172: 'user.sessions' is possibly 'undefined'. This occurs when mapping users, specifically when accessing user.sessions[0]. To fix this, ensure that user.sessions exists before accessing its length or elements. Adjust the code in the map callback as follows:

```typescript
if (user.sessions && user.sessions.length > 0 && user.sessions[0]) {
  const sessionExpiry = new Date(user.sessions[0].expires);
  lastLoginAt = new Date(
    sessionExpiry.getTime() - SESSION_DURATION_DAYS * 24 * 60 * 60 * 1000
  );
}
```

This update checks that user.sessions is defined before accessing its length or the first element. See the relevant code in route.ts at this ref.
