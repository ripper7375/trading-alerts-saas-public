The job failed because the migration 20260214000000_rag_dual_memory tried to run an ALTER TABLE command on the "trades" table, which does not exist yet in your database. Specifically:

Error:

> ERROR: relation "trades" does not exist
> CONTEXT: SQL statement "ALTER TABLE "trades" ADD COLUMN "advice_session_id" TEXT"

Solution:

- Ensure the migration for creating the "trades" table runs before any migration that alters it.
- Check the migration folder (prisma/migrations) and verify that the "init" or prior migration actually creates the "trades" table, and is listed before the "rag_dual_memory" migration.
- If "trades" is created in a different migration, move the ALTER TABLE statement for "trades" to a migration after the table is created.
- If "trades" should be present before this migration, confirm your development and CI database is up to date with all migrations applied in the correct order.

Example fix for migration ordering (in prisma/migrations):

1. Migration N: create_trades_table
2. Migration N+1: rag_dual_memory (contains ALTER TABLE "trades"...)

If fixing locally:

- Revert the failed migration with `npx prisma migrate resolve --rolled-back 20260214000000_rag_dual_memory`
- Fix your migration order or SQL.
- Re-run: `npx prisma migrate dev`

If your migration SQL for "rag_dual_memory" looks like this:

```sql
ALTER TABLE "trades"
    ADD COLUMN "advice_session_id" TEXT;
```

Make sure there is a migration before it with:

```sql
CREATE TABLE "trades" ( ... );
```

If "trades" should already exist, you may need to reset your database during CI setup.

Summary: Move or adjust your ALTER TABLE statement so it cannot run before "trades" exists. This will resolve your job failure.
