The failing job summary shows that "One or more test jobs failed" but marks both "Integration Tests" and "Build Check" as skipped. This means the underlying issue likely comes from the jobs these depend on—specifically, the "unit-and-component-tests" job.

Steps to resolve:

1. Check the "unit-and-component-tests" job for failures, focusing on these main steps:
   - pnpm install --frozen-lockfile
   - pnpm exec prisma generate
   - pnpm run type-check
   - pnpm run lint
   - pnpm run test:ci

2. Run these commands locally to reproduce the issue. The most common sources of problems are:
   - Outdated dependencies (run pnpm install – check your lockfile)
   - TypeScript or ESLint errors (run pnpm run type-check and pnpm run lint)
   - Failing or outdated tests (run pnpm run test:ci)

3. After fixing any issues found, push your changes and re-run the workflow.

Code suggestion (example for fixing a possible test error):
If a specific test is failing, edit the file and fix the logic or update the test expectation. Then, stage and push the fix.

You can view and debug the source workflow here:
.github/workflows/tests.yml (ref: 875b4f7e77a0e50bcaf338a585191281a2064215)
