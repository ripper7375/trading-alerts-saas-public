I'll analyze the failing job to identify the issue and provide a solution. I found the TypeScript type-check failures. Let me retrieve the files to provide specific fixes: ## Solution for Failing Job

The TypeScript type-check is failing due to two errors that need to be fixed:

### Error 1: Unused parameter in route. ts (Line 52)

The `request` parameter is declared but never used in the GET function.

**Fix:** Remove the unused parameter since it's not needed:

```typescript
export async function GET(
  _request: NextRequest,
  context: RouteParams
): Promise<NextResponse> {
```

Or simply remove it entirely and adjust the function signature:

```typescript
export async function GET(
  context: RouteParams
): Promise<NextResponse> {
```

However, since Next.js route handlers expect the first parameter to be the request, prefix it with underscore to indicate it's intentionally unused.

### Error 2: Type mismatch in disbursement-processor.ts (Line 213)

The `NOT` clause is using `{ riseId: null }` which TypeScript doesn't accept because `null` isn't compatible with the expected `StringFilter` type.

**Fix:** Change line 211-214 from:

```typescript
const accounts = await this.prisma.affiliateRiseAccount.findMany({
  where: {
    NOT: { riseId: null },
  },
});
```

To use the correct Prisma filter syntax:

```typescript
const accounts = await this.prisma.affiliateRiseAccount.findMany({
  where: {
    riseId: { not: null },
  },
});
```

Or alternatively:

```typescript
const accounts = await this.prisma.affiliateRiseAccount.findMany({
  where: {
    riseId: { isSet: true },
  },
});
```

These changes will resolve both TypeScript errors and allow your build to pass.
