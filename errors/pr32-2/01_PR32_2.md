The build failed because in app/api/disbursement/reports/affiliate/[affiliateId]/route.ts, the GET function's request parameter is declared but never used:

```typescript
export async function GET(
  request: NextRequest, // <--- unused parameter
  context: RouteParams
): Promise<NextResponse> {
  // ...
}
```

To resolve this, remove the unused request parameter:

```typescript
export async function GET(context: RouteParams): Promise<NextResponse> {
  // ...existing code...
}
```

Alternatively, if you plan to use the request parameter in the future, you can prefix it with an underscore to silence the unused variable error:

```typescript
export async function GET(
  _request: NextRequest,
  context: RouteParams
): Promise<NextResponse> {
  // ...existing code...
}
```

See the code at this location: [route.ts (ref: 4fecf9e5f70051cc7cb641650e7aa3f60de14223)](https://github.com/ripper7375/trading-alerts-saas-public/blob/4fecf9e5f70051cc7cb641650e7aa3f60de14223/app/api/disbursement/reports/affiliate/%5BaffiliateId%5D/route.ts).

This change will resolve the type error and fix the failing build.
