The failure is due to the "TypeScript Type Check" job failing in your workflow. According to the workflow definition, this step runs pnpm run type-check. The key error appears in the summary log:

❌ Type check: FAILED

To resolve this:

1. Run pnpm run type-check locally to see the exact TypeScript errors.
2. Fix any reported type errors in your codebase.
3. Commit and push the fixes.

If the type-check script in package.json is missing or incorrect, ensure it includes the correct TypeScript command, such as:

```json
"scripts": {
  "type-check": "tsc --noEmit"
}
```

After making these corrections, re-run the checks. You can view your workflow definition here for details: .github/workflows/security-checks.yml (ref: 57182ffe27ab5be48a633fe04fea31f4ef628069).
