Your failing job is caused by missing or incorrect module paths and type issues:

**1. Module Not Found Errors**

- `error TS2307: Cannot find module '@/components/ui/checkbox'`
- `error TS2307: Cannot find module '@/lib/auth'`
- `error TS2305: Module '"@/components/ui/button"' has no exported member 'ButtonProps'.`

**Solutions:**

**A. For `@/components/ui/checkbox`:**

- Make sure the file `components/ui/checkbox.tsx` exists and is exported.
- If missing, create a `checkbox.tsx` with a compatible Checkbox component, or adjust your import to the correct path.

**B. For `@/lib/auth`:**

- Ensure `lib/auth.ts` exists and exports `authOptions`.
- If it has moved, update your import statements in `app/api/admin/fraud-alerts/bulk/route.ts` and `app/api/user/sessions/route.ts` to the right path.

**C. For `ButtonProps` export:**

- In `components/ui/button.tsx`, there is no explicit export of `ButtonProps`. To fix:
  - Add this to `button.tsx`:

    ```tsx
    import { VariantProps } from 'class-variance-authority';
    // At end of file
    export type ButtonProps = React.ComponentProps<'button'> &
      VariantProps<typeof buttonVariants> & { asChild?: boolean };
    ```

  - This will expose `ButtonProps` for importing in `components/ui/pagination.tsx`.

**2. Extra/Unused Declarations**

- Errors such as `'ConditionType' is declared but never used` and declared variables never read (`'Laptop'`, `'Smartphone'`): These are just TypeScript warnings. You can remove the unused imports or prefix them with `_` to avoid warnings:

  ```tsx
  // If unused:
  // Remove these lines
  ```

**3. Pagination Component Issues**

- Duplicate `size` prop and missing `size` prop errors in `components/ui/pagination.tsx`. Review how `ButtonProps` and button components are used; ensure that when spreading props, you're not including `size` multiple times or omitting required properties. After addressing the export for `ButtonProps`, these should be resolved.

**Summary of Code Changes**

1. Verify or create `components/ui/checkbox.tsx`.
2. Verify or correct the `@/lib/auth` import path.
3. Export `ButtonProps` in `components/ui/button.tsx` as shown above.

Once you apply these fixes, your failing job should be resolved. See the files involved for reference (ref: bdbde959a0e232cef7788b311f3acfa506281623).
