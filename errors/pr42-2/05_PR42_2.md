The job failed because prisma.systemConfig and prisma.systemConfigHistory do not exist on type PrismaClient. This indicates your Prisma schema is missing models for systemConfig and systemConfigHistory.

To fix the issue:

1. Add the required models to your Prisma schema (usually schema.prisma):

```prisma
model systemConfig {
  key        String   @id
  value      String
  valueType  String
  category   String?
  description String?
  updatedBy   String?
  updatedAt   DateTime @updatedAt
}

model systemConfigHistory {
  id         Int      @id @default(autoincrement())
  configKey  String
  oldValue   String
  newValue   String
  changedBy  String
  reason     String?
  changedAt  DateTime @default(now())
}
```

2. Run:

```
npx prisma generate
npx prisma migrate dev --name add-system-config
```

3. Ensure usage in files such as app/api/admin/settings/affiliate/route.ts (ref: b6932abf38f80655e948a94f71eb52cdfb7fad97) and app/api/config/affiliate/route.ts matches the newly created model names (systemConfig, systemConfigHistory—note case-sensitivity).

This will solve the TS2339 errors and allow correct access to those tables with Prisma.
