Your job is failing mainly due to two types of issues:

**1. Not wrapping React state updates in act(...) during tests (PlanSelector):**  
To fix this, ensure that any state updates or async data fetching in your test interacting with <PlanSelector /> are wrapped in React's act function. For example, if you're using React Testing Library, you might need:

```js
import { act } from '@testing-library/react';
// ...

await act(async () => {
  // your code that triggers updates (like render, fireEvent, waitFor, etc)
});
```

Edit your test(s) involving PlanSelector to ensure all side effects and updates are wrapped accordingly.

---

**2. CRON_SECRET not configured in tests for process-pending-disbursements:**  
The logs show this error:

```
console.error('CRON_SECRET not configured')
```

This means your test for `process-pending-disbursements` API is not setting the required environment variable.  
**Solution:** In your test setup file or before running the test, set this variable:

```js
process.env.CRON_SECRET = 'your_test_secret';
```

Do this at the start of your test file or in a global test setup if used in multiple tests.

---

**3. Indicator API test error:**

```
Expected: "MT5 service error"
Received: "Internal server error"
```

When MT5ServiceError is thrown, the error message is being replaced by a generic error. Update your API route handler to catch MT5ServiceError and return the expected message:

```js
try {
  // ...
} catch (error) {
  if (error instanceof MT5ServiceError) {
    return NextResponse.json(
      { success: false, error: 'MT5 service error' },
      { status: 500 }
    );
  }
  // fallback
  return NextResponse.json(
    { success: false, error: 'Internal server error' },
    { status: 500 }
  );
}
```

Patch your route handler to match test expectations.

---

**Apply these fixes in their respective places to resolve the job failure.** If you need file names or code locations for any of these, let me know!
