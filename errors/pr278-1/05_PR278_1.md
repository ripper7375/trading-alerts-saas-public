The main cause of the failing job (60738893158) is that several test suites are unable to resolve certain modules, resulting in errors like:

- Could not locate module @/lib/cache/indicator-cache
- Could not locate module @/lib/db/postgresql
- Could not locate module @/lib/affiliate/constants

These errors typically indicate issues with path aliasing (@/...) in your Jest/test setup. This may be due to missing or misconfigured path mapping in your Jest configuration, usually in your jest.config.js/ts and/or the tsconfig.json.

**Solution:**

1. **Check your Jest config for moduleNameMapper**
   Add/verify the following section in your `jest.config.js` or `jest.config.ts`:

   ```js
   module.exports = {
     // ...your other config
     moduleNameMapper: {
       '^@/(.*)$': '<rootDir>/src/$1',
     },
   };
   ```

   Adjust `<rootDir>/src/$1` to the correct base path if your code is not in `src/`.

2. **Ensure tsconfig has correct paths**
   In your `tsconfig.json` (or `tsconfig.base.json`), make sure your paths are correctly mapped:

   ```json
   {
     "compilerOptions": {
       // ...other options,
       "baseUrl": ".",
       "paths": {
         "@/*": ["src/*"]
       }
     }
   }
   ```

   Again, adjust `"src/*"` to match your source code location.

3. **Install dependencies and clean up**
   - Run `pnpm install` (or npm/yarn as appropriate) to ensure dependencies are up-to-date.
   - If you changed tsconfig or Jest config, restart your test runner/CI job.

**Code Suggestions Recap:**

- Add/verify `moduleNameMapper` in Jest config.
- Ensure path alias in tsconfig matches your project structure.
- Re-install dependencies and re-run tests.

Fixing these config issues should resolve module resolution errors and allow the test files to run as expected.
