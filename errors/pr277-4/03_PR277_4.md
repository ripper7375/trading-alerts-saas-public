The failures in job 60728295100 are due to the following issues:

1. **Module Not Found**: The logs show errors like:

   ```
   at Resolver._throwModNotFoundError ... (frontend/__tests__/integration/stack-b.test.ts:20:21)
   ```

   This means that a module imported at or near line 20 is missing from your environment. In all failed integration tests, the import statement is:

   ```typescript
   import { api } from '@/lib/api-clients';
   ```

   The `'@/lib/api-clients'` alias likely isn't resolved correctly in your test environment. This typically happens if your test runner (e.g. Jest) isn't configured to recognize your project's custom import path aliases.

   **Solution:**
   - Ensure your `jest.config.js` or `jest.config.ts` correctly maps the `@` alias to the project's root/src directory, for example:
     ```js
     module.exports = {
       // ...other config...
       moduleNameMapper: {
         '^@/(.*)$': '<rootDir>/path/to/your/src/$1',
       },
     };
     ```
   - Make sure your editor/IDE and tsconfig.json also reflect this alias.

2. **Direct Access to Stack C**: The `frontend/__tests__/integration/stack-c-forbidden.test.ts` fails with errors like:

   ```
   ReferenceError: clearImmediate is not defined
   TypeError: markResourceTiming is not a function
   ```

   These errors occur in tests that expect frontend requests to Stack C to be blocked. They're acceptable if the intent is to ensure Stack C is inaccessible due to browser/network/CORS restrictions, but they shouldn't cause the test runner to crash.

   **Solution:**
   - For environment-specific globals (like `clearImmediate`), make sure your Node.js version and the `testEnvironment` setting in Jest are compatible or mock these where appropriate.
   - To prevent these errors from breaking tests, wrap direct-access cases in `try-catch` and assert that the error is indeed thrown, as shown in the test files. If the issue persists, ensure that the test runner's environment matches the one where these globals exist (for example, setting `'testEnvironment': 'node'` if you're running non-browser tests).

3. **Codecov Rate Limiting**: The log:
   ```
   There was an error running the uploader: Error uploading to https://codecov.io: Error: There was an error fetching the storage URL during POST: 429 - {"message":"Rate limit reached. Please upload with the Codecov repository upload tok
   ```
   This doesn't affect test results but will affect coverage reporting. Configure a Codecov upload token or increase the plan if you need coverage results.

**Summary of Code Fixes:**

- Fix path aliasing for `'@/lib/api-clients'` in your test runner configuration.
- Check your Node.js version and Jest environment for compatibility with required globals.
- Properly handle expected failures in direct Stack C tests.
- (Optional) Address Codecov token configuration for CI.

You can view the test file sources for further reference via this [ref](afde7494221691197520afee64f5176dc75222d3).
