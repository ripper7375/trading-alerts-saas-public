The failing job 60728343941 is the "Test Summary" job. According to the logs and the workflow definition, this job fails when any of the required jobs (unit-and-component-tests, integration-tests, build-check) fails. The log indicates:

❌ One or more test jobs failed
exit 1

To resolve this:

1. Check which prerequisite job failed: Review the results of unit-and-component-tests, integration-tests, and build-check jobs to locate the original failure. The Test Summary job only reports aggregate status; root cause will be in the logs from its dependencies.
2. Address the root job failure: For each failed job:
   - Review its log output for errors.
   - For test jobs, fix any failing tests, lint, or type errors.
   - For build-check, ensure the Next.js bundle size or build process does not exit with an error. For example, reduce the bundle size if it exceeds the configured limit.
3. Once the root failure is fixed, the "Test Summary" job will automatically pass since it simply checks the results of prior jobs.

Example fix for excessive bundle size in build-check:

- Optimize code and dependencies to stay below bundle size thresholds.
- Remove unnecessary packages or use dynamic imports.

Example fix for test failures:

- Update broken tests to reflect code changes.
- Fix underlying application bugs causing test failures.

Review and repair the job(s) that failed before the Test Summary step, then re-run the workflow.
