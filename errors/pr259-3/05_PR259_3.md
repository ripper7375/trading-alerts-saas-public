The failing job 60384058786 is due to multiple errors in the test file: `__tests__/components/charts/trading-chart.test.tsx`. The main issues are:

- The tests expect to find elements with the text "Failed to load chart" and "Try Again," but these texts are not present in the rendered output.
- This commonly happens if the error UI or loading UI in the TradingChart component does not match what the test expects.

## Solution Steps

1. **Check TradingChart Error and Loading UI**
   - Inspect the TradingChart component and confirm:
     - It renders a loading spinner with an accessible text for the test `"should show loading spinner initially"`.
     - It renders an error message `"Failed to load chart"` when fetch fails.
     - It displays a `"Try Again"` button when there's an error.

2. **Ensure Consistent Text**
   - The text provided to your components should **exactly match** the text used in tests. Otherwise, tests will fail.

   ```tsx
   {
     /* Example: Error UI */
   }
   {
     error && (
       <div>
         <p>Failed to load chart</p>
         <button onClick={handleRetry}>Try Again</button>
       </div>
     );
   }
   ```

3. **Test Improvements (Optional)**
   - If the error or button text is split across elements or changed, update the tests' queries to use a text matcher function or regex.

   ```tsx
   // Instead of screen.getByText('Failed to load chart')
   screen.getByText((content) => content.includes('Failed to load chart'));
   ```

4. **Check Data Fetch Mocks**
   - Make sure your mocks in the test file match the error-handling scenario you want to trigger in the TradingChart.

## Code Suggestions

- In your TradingChart component, ensure you are returning something like:

  ```tsx
  // Pseudocode, update to your structure
  if (isLoading) {
    return <div>Loading...</div>; // test should expect 'Loading...'
  }
  if (error) {
    return (
      <div>
        <p>Failed to load chart</p>
        <button onClick={onRetry}>Try Again</button>
      </div>
    );
  }
  ```

- In your tests, you may need to update your queries if the element structure changed:

  ```tsx
  // Example test update (line 94, 171, 202, 434)
  expect(screen.getByText('Failed to load chart')).toBeInTheDocument();
  expect(screen.getByText('Try Again')).toBeInTheDocument();

  // Or, use a matcher function for flexibility
  expect(
    screen.getByText((text) => text.includes('Failed to load chart'))
  ).toBeInTheDocument();
  ```

## Files to Review

- [TradingChart Component (likely src/components/charts/TradingChart.tsx)](https://github.com/ripper7375/trading-alerts-saas-public/blob/b371ebce54d58901757eccbeb76d197c66280245/src/components/charts/TradingChart.tsx)
- [Test File](https://github.com/ripper7375/trading-alerts-saas-public/blob/b371ebce54d58901757eccbeb76d197c66280245/__tests__/components/charts/trading-chart.test.tsx)

---

**Summary:**  
Align the error and loading UI in TradingChart with the expected test texts ("Failed to load chart" and "Try Again"). Update tests to use flexible queries if needed. This will resolve the failing job. If you need a review of specific lines, let me know!
