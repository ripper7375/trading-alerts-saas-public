I'll analyze the failing job to find a solution for you. Let me examine the failing test files to understand the issues better: ## Solution for Failing Tests

The job is failing due to **Jest parsing errors with `next-auth`** and **database query errors in integration tests**. Here are the fixes:

### 1. Fix Jest Configuration for `next-auth` (2 test files failing)

The tests `__tests__/part20/api/indicators. test.ts` and `__tests__/part20/api/confluence.test.ts` are failing because Jest cannot parse the `next-auth` package. The mock is being defined after the import statement executes.

**Solution:** Move the mock setup before any imports that depend on `next-auth`:

**For `__tests__/part20/api/indicators.test.ts`**, move the route import to after all mocks:

```typescript
// At line 59, REMOVE this line:
// import { GET } from '@/app/api/indicators/[symbol]/[timeframe]/route';

// Keep all the mocks (lines 16-56) in place

// ADD the import AFTER all mocks, around line 60:
// Import the route handler after mocking
import { GET } from '@/app/api/indicators/[symbol]/[timeframe]/route';
```

The import at line 59 is already correctly placed after the mocks, so this file should work. However, ensure your Jest config properly transforms `next-auth`.

**For `__tests__/part20/api/confluence.test.ts`**, the import at line 95 is also correctly placed.

**Add to your `jest.config.js`:**

```javascript
transformIgnorePatterns: ['node_modules/(?!(next-auth)/)'];
```

### 2. Fix Database Query Integration Tests (2 test files failing)

The tests `__tests__/part20/integration/cache-integration.test.ts` and `__tests__/part20/integration/db-queries.test.ts` are failing because the SQL query uses string interpolation for table names, which is vulnerable and fails with the mock.

**Fix in `lib/db/queries.ts` (lines 37-44):**

Replace the string interpolation with proper parameterized queries:

```typescript
export async function getIndicatorDataFromDb(
  symbol: string,
  timeframe: string,
  limit: number = 1000
): Promise<IndicatorData> {
  const tableName = `${symbol.toLowerCase()}_${timeframe.toLowerCase()}`;

  // Use identifier escaping instead of string interpolation
  const rows = await query<RawRow>(
    `SELECT timestamp, open, high, low, close,
            fractals, horizontal_trendlines, diagonal_trendlines,
            momentum_candles, keltner_channels, tema, hrma, smma, zigzag
     FROM "${tableName}"
     ORDER BY timestamp DESC
     LIMIT $1`,
    [limit]
  );
```

**Also update `getDataFreshness` (line 105):**

```typescript
const rows = await query<{ timestamp: Date }>(
  `SELECT timestamp FROM "${tableName}" ORDER BY timestamp DESC LIMIT 1`
);
```

### 3. Alternative: Update Mock Implementation

If you prefer not to change the query function, update the mock in the test file (`__tests__/part20/integration/db-queries.test.ts`) to properly handle the query call:

```typescript
mockQuery.mockImplementation((sql: string, params: any[]) => {
  // Return empty array or mock data based on the SQL query
  return Promise.resolve([]);
});
```

**Recommended approach:** Apply fix #2 (proper table name escaping) as it's both more secure and fixes the test issues.
