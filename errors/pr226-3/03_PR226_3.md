I'll analyze the failing job to find a solution. Let me first get the logs for job 59544007097.The log shows this is a check/summary job that's failing because upstream test jobs failed. Let me get the full workflow run details to see which specific test job is failing. The job is failing because it's part of a workflow that checks if other test jobs have failed. The log shows "❌ One or more test jobs failed" which indicates that upstream test jobs in the workflow are failing, causing this summary job to fail as well.

To identify the root cause, I need to check the workflow file to see what the actual failing tests are:
