The failing job 59547377174 is triggered by the "Test Summary" job, which fails with the message: "❌ One or more test jobs failed". According to the workflow definition in .github/workflows/tests.yml (ref: 1288e5af2da3b8e8dcc26267822cbc68e28f8100), this job runs after "unit-and-component-tests", "integration-tests", and "build-check".

The summary step checks the individual results of each required job. It fails if any dependency failed:

```yaml
if: |
  needs.unit-and-component-tests.result == 'failure' ||
  needs.integration-tests.result == 'failure' ||
  needs.build-check.result == 'failure'
```

From your logs, both integration tests and build checks were "skipped", and the summary hard-coded the job outcome to "failed". This suggests jobs may not have run or finished due to conditional logic, matrix settings, or upstream issues.

To address this:

1. Ensure all dependencies ("unit-and-component-tests", "integration-tests", "build-check") are running and finishing with a proper status (success/failure).
2. Check that all jobs are included in the matrix or do not use limited branch filters that skip the jobs.
3. Do not skip required jobs or force early returns that prevent their execution.
4. If you intend to skip tests (e.g., for a quick run), update the "Test Summary" job to account for 'skipped' as a neutral status:

```yaml
if: |
  needs.unit-and-component-tests.result == 'failure' ||
  needs.integration-tests.result == 'failure' ||
  needs.build-check.result == 'failure'
```

Replace with:

```yaml
if: |
  needs.unit-and-component-tests.result == 'failure' ||
  needs.unit-and-component-tests.result == 'cancelled' ||
  needs.integration-tests.result == 'failure' ||
  needs.integration-tests.result == 'cancelled' ||
  needs.build-check.result == 'failure' ||
  needs.build-check.result == 'cancelled'
```

Or, if skipping is acceptable, adjust your summary and exit logic accordingly so that 'skipped' does not count as a failure.

In summary: The failure is due to skipped or missing dependent jobs. Review job triggers and dependencies, and update the "Test Summary" job's logic to reflect your real pass/fail criteria.
