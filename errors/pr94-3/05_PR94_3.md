Two tests are failing in this job:

1. **tests**/api/indicators.test.ts

- The failing test is: "should return indicator data for FREE tier with valid symbol/timeframe" (line 282).
- The error occurs because the test expects data.data.proIndicatorsTransformed to be defined:
  ```typescript
  expect(data.data.proIndicatorsTransformed).toBeDefined();
  ```
- The mock data defined in this test does not include a proIndicatorsTransformed field:
  ```typescript
  const mockData = {
    ohlc: [...],
    horizontal: {...},
    diagonal: {...},
    fractals: {...},
    metadata: {...},
  };
  ```

**Solution:**  
Update the mockData object in the test (**tests**/api/indicators.test.ts, line 287) to include a proIndicatorsTransformed key (can be an empty object or suitable mock):

```typescript
const mockData = {
  ohlc: [
    /* ... */
  ],
  horizontal: {
    /* ... */
  },
  diagonal: {
    /* ... */
  },
  fractals: {
    /* ... */
  },
  metadata: { symbol: 'XAUUSD', timeframe: 'H1', bars: 1000 },
  proIndicatorsTransformed: {}, // <-- Add this line
};
```

See the relevant part of the test here:  
https://github.com/ripper7375/trading-alerts-saas-public/blob/8c1a0567507549044481f899ef65e4a29405c4dd/__tests__/api/indicators.test.ts#L287

2. **tests**/lib/validations/auth.test.ts

- The failing test is: "signupSchema › should validate valid signup data".
- The failure is similar—likely, the test expects a specific output or behavior from the schema that was recently changed.
- The code at line 239 in the test is:
  ```typescript
  expect(result.success).toBe(true);
  ```
- If you have recently updated the validation rules in `signupSchema`, ensure the input defined in the test is still valid according to the new schema.

**Solution:**

- Review the current implementation of `signupSchema` in `@/lib/validations/auth`.
- Make sure that the valid test input meets all your validation requirements (for example: minimum characters, required fields, password strength).
- If requirements changed, update the test input to satisfy the current schema or adjust the schema if appropriate.

See the test code here:  
https://github.com/ripper7375/trading-alerts-saas-public/blob/8c1a0567507549044481f899ef65e4a29405c4dd/__tests__/lib/validations/auth.test.ts#L19

**Summary:**

- For `indicators.test.ts`, add proIndicatorsTransformed to the mock data in the relevant test.
- For `auth.test.ts`, align the test's input data or schema with your latest validation logic.

Once these changes are made, re-run your test suite.
