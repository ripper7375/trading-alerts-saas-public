I'll analyze the failing job to find a solution. Let me get the logs first. The logs show this is a job summary step that's detecting failures from previous test jobs. Let me get more detailed logs about the workflow run to see which specific test jobs failed. The job is failing because it's a summary/status check job that depends on other test jobs. The error message "❌ One or more test jobs failed" indicates that one or more upstream test jobs in the workflow failed, causing this check to fail as well.

## Solution

You need to check the workflow file to identify which dependent jobs are failing. Let me examine the workflow:
