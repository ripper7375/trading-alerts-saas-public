The job failed due to this step:
echo "❌ One or more test jobs failed"
exit 1

This happens because one of the dependent jobs (unit-and-component-tests, integration-tests, or build-check) failed. However, the logs indicate that both Integration Tests and Build Check were "skipped," which means unit-and-component-tests likely failed or was skipped first.

Here’s how to resolve the root issue:

1. Confirm the actual point of failure:
   - Check the unit-and-component-tests step logs to identify why it might have failed or been skipped. Common reasons include dependency install failures, test errors, or TypeScript/ESLint failures.

2. Make sure tests are not skipped:
   - If you intended to skip tests (e.g., via workflow condition filters or commit message), adjust the workflow triggers or remove the skip logic.
   - Otherwise, ensure all project dependencies and test scripts are correct and up to date in your codebase.

3. If failures are in the test script (pnpm run test:ci), fix test/code errors. If it’s a dependency installation, verify your package.json and pnpm-lock.yaml files for consistency.

Example fix (in your workflow or code, depending on error):

- Double check the pnpm install and test:ci script in your CI configuration.
- Ensure the following commands run successfully locally before pushing:
  pnpm install --frozen-lockfile
  pnpm run type-check
  pnpm run lint
  pnpm run test:ci

If specific test or lint errors show up in the CI log, address those errors directly in your codebase.

Summary: The immediate solution is to review and fix failures in the unit-and-component-tests job so downstream jobs are not skipped, allowing the entire workflow to pass as intended.

For more details, check the relevant section in the workflow file: .github/workflows/tests.yml (ref: 6a796e05369a1ab621fd2ee6c92cbc923787ade4).
