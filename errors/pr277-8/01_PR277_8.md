Your failing job (60730077248) in the "Next.js CI (Progressive)" workflow is primarily failing due to these issues:

### 1. Redis Connection Error

The logs show:

```
[Redis] Invalidate pattern error: Error: Connection is closed.
```

This occurs in `lib/cache/redis.ts` at line 110. It likely means your tests are trying to use Redis but the connection has been lost or never established.

#### Solution:

- Ensure a Redis server is running and accessible when tests start.
- In the test setup, check you are initializing your Redis client properly and handling possible connection drops or timeouts. Example fix:
  ```typescript
  // Before using Redis client
  if (!redisClient.status || redisClient.status !== 'ready') {
    await redisClient.connect();
  }
  ```
- Consider mocking Redis in unit tests to avoid real connections, unless you are doing integration tests.

---

### 2. Query Timeout Error

The logs show:

```
GET /api/indicators/[symbol]/[timeframe] error: { error: 'Query timeout exceeded' }
```

This is from `app/api/indicators/[symbol]/[timeframe]/route.ts` at line 294. A database or external API query is taking too long, possibly due to environmental issues or real network waits in tests.

#### Solution:

- Mock or stub slow dependencies (database, Redis, external APIs) in your Jest test configuration.
- If you must call live services, consider increasing the Jest timeout:
  ```typescript
  jest.setTimeout(30000); // 30 seconds
  ```

---

### 3. Module Not Found in Integration Tests

For multiple integration tests such as `stack-a.test.ts`, `stack-b.test.ts`, etc., logs say:

```
at Resolver._throwModNotFoundError
```

This means required modules for tests are missing, probably in the `frontend/__tests__/integration/` folder.

#### Solution:

- Check your dependencies (`undici`, etc.) and install any missing packages:
  ```
  pnpm install
  ```
- Verify your `jest.config.js` module paths are correct.
- Make sure your test environment properly simulates Node.js globals where needed (e.g., `clearImmediate`).

---

### 4. clearImmediate Not Defined / Undici Errors

In some integration tests using `undici`, you see:

```
ReferenceError: clearImmediate is not defined
```

This suggests a missing Node.js global in your test environment (possibly running in JSDOM, which doesn't provide all Node.js globals).

#### Solution:

- Add a setup file for Jest to polyfill `clearImmediate`:
  ```typescript
  // jest.setup.js
  global.clearImmediate = global.clearImmediate || ((id) => clearTimeout(id));
  ```
- Reference this setup file in your `jest.config.js`:
  ```js
  setupFilesAfterEnv: ['<rootDir>/jest.setup.js'],
  testEnvironment: 'node', // or switch from jsdom to node if possible
  ```

---

## Quick Action Items:

1. Ensure Redis is running or mock it for tests.
2. Polyfill missing Node.js globals for JSDOM-based or browser-like test environments.
3. Install any missing packages (`pnpm install`) and verify all module imports.
4. Mock/stub long-running or flaky external integrations for faster/safer tests.

Let me know if you want targeted code suggestions for any individual test or file!
