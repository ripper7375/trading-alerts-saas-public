The failing job encountered two main issues:

1. **Redis Connection Error**
   - Error: `[Redis] Invalidate pattern error: Error: Connection is closed.`
   - This appears in `lib/cache/redis.ts` at or around line 110, affecting cache-clearing logic called from `lib/cache/indicator-cache.ts`.
   - Solution: Ensure that the Redis client is connected before attempting operations. Add a connection check and auto-reconnect logic if using ioredis.

     ```typescript
     // lib/cache/redis.ts
     try {
       if (!redisClient.status || redisClient.status !== 'ready') {
         await redisClient.connect();
       }
       // ... your existing logic
     } catch (error) {
       console.error('[Redis] Invalidate pattern error:', error);
       return 0;
     }
     ```

     Also consider wrapping Redis calls with a retry mechanism or listening to connection events to handle unexpected disconnects more gracefully.

2. **Test Module Not Found**
   - Error: `Could not locate module @/components/charts/indicator-overlay`
   - Appears in `__tests__/components/charts/trading-chart.test.tsx`.
   - Solution: Ensure the module path is correct and the file exists. If you use path aliasing (`@`), configure Jest to understand it. Add or update the `moduleNameMapper` setting in your Jest config (e.g., `jest.config.js`):

     ```js
     // jest.config.js
     module.exports = {
       // ... other config ...
       moduleNameMapper: {
         '^@/(.*)$': '<rootDir>/src/$1', // Adjust path as needed
       },
     };
     ```

     Make sure that `src/components/charts/indicator-overlay` exists or adjust the alias mapping to your correct project structure.

**Next Steps**

- Verify Redis server is running and accessible in the CI environment.
- Update your Jest config to resolve alias imports.
- If you use environment variables for host/port (e.g., `127.0.0.1:5432` for Postgres), ensure your tests can reach those services, or mock them as needed for isolated tests.

Let me know if you’d like specific code or config file paths reviewed directly for these changes.
