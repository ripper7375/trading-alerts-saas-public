The failing job (ID: 60381747902) encountered a configuration error during tests:

**Error Summary**:

```
FAIL __tests__/components/charts/trading-chart.test.tsx
  ● Test suite failed to run

    Configuration error:
    Could not locate module @/components/charts/indicator-overlay mapped as:
    ...
    at createNoMappedModuleFoundError
    at Object.mock (__tests__/components/charts/trading-chart.test.tsx:38:6)
```

**Root Cause**:

- The test file is trying to import `@/components/charts/indicator-overlay`, but Jest cannot locate this module. This usually happens because:
  - The file is missing.
  - Path alias (`@/`) is not correctly mapped in Jest configuration.
  - The mapping exists but the actual file does not.

---

## Solution Steps

### 1. Check Module Existence

- Confirm that `components/charts/indicator-overlay.{tsx|ts|js}` actually exists in your repository.
  - If missing, add or restore this file if it's required for the tests.

### 2. Fix Path Alias Mapping in Jest Config

If you're using the `@/` alias (commonly configured via `jsconfig.json`/`tsconfig.json`), ensure that your Jest config knows about it.  
**Example (jest.config.js):**

```js
module.exports = {
  // ...other config
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1', // <--- Adjust <rootDir>/src/ as appropriate!
  },
};
```

- Make sure `<rootDir>/src/` matches your actual project structure.

### 3. Update Imports in Test File

If alias configuration is hard to maintain, directly change imports to relative paths.

**From:**

```js
import IndicatorOverlay from '@/components/charts/indicator-overlay';
```

**To (example):**

```js
import IndicatorOverlay from '../../indicator-overlay'; // adjust as needed
```

---

## Summary

- **Fix 1**: Ensure `components/charts/indicator-overlay` exists.
- **Fix 2**: Add/update `moduleNameMapper` in `jest.config.js`:
  ```js
  moduleNameMapper: {
    '^@/(.*)$': '<rootDir>/src/$1', // Adjust src/ as needed
  }
  ```
- **Fix 3**: If you prefer, use direct relative imports in your test files.

Apply these fixes and re-run your tests. If the file is missing, restore or recreate it as needed.

If you need specific help updating your `jest.config.js` or confirming the file location, let me know and I can provide tailored code suggestions!
