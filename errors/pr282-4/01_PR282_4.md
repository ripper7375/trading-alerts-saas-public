The job failed due to ESLint errors in lib/api/index.ts: the code uses the type any in multiple function parameters and return types, which violates the @typescript-eslint/no-explicit-any rule.

**Solution: Replace any with proper, explicit type definitions.**  
Below are code suggestions for the relevant locations (see details in [lib/api/index.ts@677b0857](https://github.com/ripper7375/trading-alerts-saas-public/blob/677b0857b975d24a089c75f48dc988a0e219a35e/lib/api/index.ts)):

Example:  
Instead of:

```typescript
// Existing
createAlert: (data: any) =>
  apiCall('/api/alerts', { method: 'POST', body: JSON.stringify(data) }),
```

Use a specific type:

```typescript
// Suggested
interface AlertPayload { /* define fields here */ }
createAlert: (data: AlertPayload) =>
  apiCall('/api/alerts', { method: 'POST', body: JSON.stringify(data) }),
```

Apply similar changes wherever any is used (lines: 35, 37, 44, 55, 63, 79, 86, 94, 133).  
Define and import the appropriate interfaces or types for each API payload and parameter.

Alternatively, you can temporarily disable the lint rule for required lines, but using explicit types is the recommended fix for production code quality and type safety.

For more information or examples, refer to the [TypeScript documentation on typing](https://www.typescriptlang.org/docs/handbook/2/types-from-types.html) or Next.js ESLint options.

**Summary of needed changes:**

- Replace `any` with explicit interface/type for API function arguments and parameters.
- Make sure all references to `any` in lib/api/index.ts have proper types defined and imported.

This will resolve the build failure caused by ESLint and type-checking in your CI workflow.
