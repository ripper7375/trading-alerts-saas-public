Now let me get the file where the error is occurring to see the issue: ## Solution

The TypeScript build is failing due to stricter type checking on environment variable access. The error occurs at line 9 in `lib/api/index.ts` where `process.env.NEXT_PUBLIC_API_URL` is accessed using dot notation.

**Fix:** Change the property access from dot notation to bracket notation:

```typescript
const BASE_URL =
  typeof window !== 'undefined' ? '' : process.env['NEXT_PUBLIC_API_URL'] || '';
```

This matches the pattern already used in your `frontend/lib/api/index. ts` file (line 51), which correctly uses bracket notation: `process.env['NEXT_PUBLIC_API_URL']`.

**Why this happens:** With TypeScript's strict configuration, `process.env` is treated as an object with an index signature, requiring bracket notation for property access rather than dot notation.
