The job 60684766840 failed with the error: "❌ One or more test jobs failed" and exited with code 1. The logs show both Integration Tests and Build Check were skipped. This suggests the workflow is set to fail if any required test jobs are skipped.

**Solution:**

1. Check the conditions or matrix setup in `.github/workflows/tests.yml` for sections that might be skipping jobs.
2. Verify all prerequisites (such as environment variables, dependencies, or required steps) for Integration Tests and Build Check.
3. Ensure the workflow does not skip required jobs or update it so skipped jobs do not cause failure. For example, revise the logic that handles job skipping:

```yaml
# In .github/workflows/tests.yml (link)
# Before: Failing if any test job is skipped
- name: Check if all required tests ran
  run: |
    if [[ $SKIP_TESTS == "true" ]]; then
      echo "Some tests were skipped"
      exit 1
    fi

# Suggestion: Only fail if non-optional jobs are skipped
- name: Check if all required tests ran
  run: |
    if [[ $SKIP_REQUIRED_TESTS == "true" ]]; then
      echo "Required tests were skipped"
      exit 1
    else
      echo "Optional tests may be skipped, but required tests ran"
    fi
```

**Recommended Actions:**

- Review your workflow logic for job skipping.
- Ensure all required jobs are properly defined and not conditionally skipped.

If you need exact lines or want the current workflow file contents, let me know, and I can retrieve the full file for tailored code editing.
