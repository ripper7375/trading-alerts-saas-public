Here are focused solutions for the errors found in the job 60684741019 from your workflow:

1. **Cannot find module '@/lib/api-client' or its corresponding type declarations**
   - Check that `frontend/lib/api-client.ts` exists.
   - Ensure your TypeScript config (`tsconfig.json`) includes the following under `compilerOptions`:
     ```json
     {
       "baseUrl": "./frontend",
       "paths": {
         "@/*": ["*"]
       }
     }
     ```
   - If it already exists, try restarting your build environment to clear cache.

2. **'error' is of type 'unknown' ([lines 91, 97](https://github.com/ripper7375/trading-alerts-saas-public/blob/dbac2b603c784334218dde7c4014986a3ddfe2f1/frontend/app/api-test/page.tsx))**
   - You need to cast or narrow error:
     ```typescript
     const message = error instanceof Error ? error.message : String(error);
     ```
   - Alternatively:
     ```typescript
     if (typeof error === 'string') {
       /* ... */
     }
     ```

3. **Cannot redeclare exported variable 'testCurrentAPIRoutes' ([lines 188, 284](https://github.com/ripper7375/trading-alerts-saas-public/blob/dbac2b603c784334218dde7c4014986a3ddfe2f1/frontend/lib/api-client.test.example.ts))**
   - Only export the variable once in the file. If you have:
     ```typescript
     export const testCurrentAPIRoutes = ...;
     // later:
     export const testCurrentAPIRoutes = ...;
     ```
     Remove/rewrite such that there's only one export per variable name.

4. **Export declaration conflicts with exported declaration of 'testCurrentAPIRoutes' (line 284)**
   - Same as above. Consolidate your exports so `testCurrentAPIRoutes` is exported just once.

5. **Property 'NEXT_PUBLIC_API_URL' comes from an index signature ([line 56](https://github.com/ripper7375/trading-alerts-saas-public/blob/dbac2b603c784334218dde7c4014986a3ddfe2f1/frontend/lib/api-client.ts))**
   - Change dot notation to bracket notation:
     ```typescript
     process.env['NEXT_PUBLIC_API_URL'];
     ```
   - Avoid `process.env.NEXT_PUBLIC_API_URL`.

---

Apply these targeted fixes in your code for the build to pass. Let me know if you need the exact lines from any file!
