The job failed due to a TypeScript error in app/api/admin/settings/affiliate/route.ts at line 86:

Property 'systemConfig' does not exist on type 'PrismaClient'.

This means your generated Prisma client does not have a systemConfig model.

Solution:

1. Check your Prisma schema (schema.prisma) and ensure you have a model named SystemConfig (the table name is case-insensitive in Prisma, but the model name matters).
2. If the model exists, run npx prisma generate to update the Prisma client.
3. If the model does not exist, add it to your Prisma schema. For example:

```prisma
model SystemConfig {
  key        String  @id
  value      String
  valueType  String
  category   String
  description String?
  updatedBy  String?
  updatedAt  DateTime @updatedAt
}
```

4. Run npx prisma migrate dev to apply the schema.
5. Commit and push your changes.

If you rename or add the model, make sure your usage in route.ts matches the Prisma model name. For reference, see line 86 in the code:

```typescript
const configs = await prisma.systemConfig.findMany({
  where: {
    key: {
      in: Object.values(CONFIG_KEYS),
    },
  },
});
```

Update systemConfig to match your actual Prisma model name if it’s different (e.g., prisma.systemConfig → prisma.systemConfig if the model is SystemConfig).

After syncing the schema and regenerating the Prisma client, this error will be resolved.
