The failing job is caused by a test failure in Indicators API Routes, specifically:  
GET /api/indicators/[symbol]/[timeframe] › should return indicator data for FREE tier with valid symbol/timeframe

Error detail:

```
at Object.toEqual (__tests__/api/indicators.test.ts:311:25)
```

This test expects the returned data in the .data property of the response to strictly match the mockData object. However, in the API implementation ([route.ts, ref 2a2d3bef69fc8ca0171fd9f044a5fa4f47c5c0af](https://github.com/ripper7375/trading-alerts-saas-public/blob/2a2d3bef69fc8ca0171fd9f044a5fa4f47c5c0af/app/api/indicators/%5Bsymbol%5D/%5Btimeframe%5D/route.ts)), the .data property adds a proIndicatorsTransformed field:

```typescript
const response: IndicatorDataResponse = {
  success: true,
  data: {
    ...data,
    proIndicatorsTransformed, // <-- Extra field
  },
  tier: userTier,
  requestedAt: new Date().toISOString(),
};
```

But the test's expected result is just mockData—without proIndicatorsTransformed:

```typescript
expect(data.data).toEqual(mockData); // __tests__/api/indicators.test.ts line 311
```

**Solution:**  
Update the test expectation to match the real API response. Replace:

```typescript
expect(data.data).toEqual(mockData);
```

with:

```typescript
expect(data.data).toMatchObject(mockData);
```

or, if you want to keep strict equality, include proIndicatorsTransformed in the expected object:

```typescript
expect(data.data).toEqual({
  ...mockData,
  proIndicatorsTransformed: expect.anything(), // or a more specific matcher if appropriate
});
```

This ensures the test allows the proIndicatorsTransformed field, preventing a strict mismatch.

- Test file: [**tests**/api/indicators.test.ts (ref 2a2d3bef69fc8ca0171fd9f044a5fa4f47c5c0af)](https://github.com/ripper7375/trading-alerts-saas-public/blob/2a2d3bef69fc8ca0171fd9f044a5fa4f47c5c0af/__tests__/api/indicators.test.ts)

**Summary:**  
The API was enhanced to always return proIndicatorsTransformed; the test must account for that field. Update the test expectation as shown above to resolve the failure.
