The job failed due to ESLint errors in lib/fraud/fraud-detection.service.ts:

- Line 217: deviceFingerprint is assigned a value but never used.
- Line 369: byStatus is assigned a value but never used.

To resolve:

1. For deviceFingerprint (line 217):
   If you don’t plan to use deviceFingerprint, prefix it with an underscore in the destructuring:

```typescript
const {
  userId,
  ipAddress,
  country,
  deviceFingerprint: _deviceFingerprint,
} = context;
```

Or simply omit it if you don’t need it.

2. For byStatus (line 369):
   If byStatus is unused, prefix it with an underscore:

```typescript
const [total, bySeverity, _byStatus, pending] = await Promise.all([
  prisma.fraudAlert.count(),
  prisma.fraudAlert.groupBy({
    by: ['severity'],
    _count: { severity: true },
  }),
  prisma.fraudAlert.groupBy({
    by: ['status'],
    _count: { status: true },
  }),
  prisma.fraudAlert.count({
    where: { status: 'PENDING' },
  }),
]);
```

Or remove byStatus if it’s not used.

These changes will satisfy @typescript-eslint/no-unused-vars and allow your job to complete successfully. See lines 217 and 369 in lib/fraud/fraud-detection.service.ts ([view file](https://github.com/ripper7375/trading-alerts-saas-public/blob/c7e4149a76a43bb8669f02240fb0ab0f5666ebd3/lib/fraud/fraud-detection.service.ts)).
