The build is failing due to several TypeScript type errors:

1. **ZigZagData type mismatch** ([archive/part6-flask-mt5/lib/api/mt5-transform.ts#117](https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/archive/part6-flask-mt5/lib/api/mt5-transform.ts)):
   - Your code is returning an object with `peaks` and `bottoms` arrays only, but the expected type requires `zigzag_peak`, `zigzag_bottom`, `ema_26`, etc.
   - **Solution:** Update the `transformProIndicators` function to return all required properties for the ZigZagData structure, not just peaks and bottoms.

2. **Incorrect property name 'color' on IndicatorMetadata** ([components/charts/indicator-toggles.tsx#102](https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/components/charts/indicator-toggles.tsx)):
   - The code references `meta.color` but the correct property is likely `meta.colors`.
   - **Solution:** Replace all occurrences of `meta.color` with `meta.colors` if that matches your metadata structure.

   Example:

   ```tsx
   // Before
   {
     meta.color && hasAccess && (
       <span style={{ backgroundColor: meta.color }} />
     );
   }
   // After
   {
     meta.colors && hasAccess && (
       <span style={{ backgroundColor: meta.colors[0] }} />
     );
   }
   ```

   Adjust for your actual data shape.

3. **Unrecognized indicator IDs ('tema', 'hrma', 'smma')** ([components/charts/pro-indicator-overlay.tsx#152–154](https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/components/charts/pro-indicator-overlay.tsx)):
   - The allowed values for indicator IDs do not include these strings.
   - **Solution:** Update your enum/type to include these IDs, or cast as needed if these are correct but not yet declared.

   Example (if using a TypeScript union):

   ```ts
   type IndicatorId = 'keltner_channels' | 'zigzag' | 'fractal_diagonal' | ... | 'tema' | 'hrma' | 'smma';
   ```

**Summary of actions:**

- Update `transformProIndicators` and related ZigZag transformation to conform to the `ZigZagData` type.
- Change `meta.color` to `meta.colors` wherever used.
- Add `"tema"`, `"hrma"`, and `"smma"` to your allowed `IndicatorId` type.

Once you update these areas, your build should pass the type checks.
