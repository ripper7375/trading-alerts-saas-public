The workflow is failing due to several TypeScript errors:

### 1. Type Error: `ZigZagData` Structure

**Error:**

```
archive/part6-flask-mt5/lib/api/mt5-transform.ts(117,5): error TS2322: Type '{ peaks: ...; bottoms: ...; } | undefined' is not assigna...
Type '{ peaks: ...; bottoms: ...; }' is missing the following properties from type 'ZigZagData': zigzag_peak, zigzag_bottom, ema_26
```

**Solution:**  
Update the zigzag transformation and return value to include the required properties (`zigzag_peak`, `zigzag_bottom`, `ema_26`). Refer to the expected `ZigZagData` type and ensure all required fields are present in the returned object.

**Code correction example:**

```typescript
// Ensure you construct ZigZagData with all required properties
const zigzag = mt5Data.zigzag
  ? {
      peaks: Array.isArray(mt5Data.zigzag.peaks) ? mt5Data.zigzag.peaks : [],
      bottoms: Array.isArray(mt5Data.zigzag.bottoms)
        ? mt5Data.zigzag.bottoms
        : [],
      zigzag_peak: mt5Data.zigzag.zigzag_peak ?? [], // Add as required by the type
      zigzag_bottom: mt5Data.zigzag.zigzag_bottom ?? [], // Add as required by the type
      ema_26: mt5Data.zigzag.ema_26 ?? [], // Add as required by the type
    }
  : undefined;
```

Apply this update in both `archive/part6-flask-mt5/lib/api/mt5-transform.ts`, `lib/api/mt5-transform.ts`, and `frontend/lib/api/mt5-transform.ts` ([ref](https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/lib/api/mt5-transform.ts)).

---

### 2. Incorrect Property Reference in Components

**Error:**

```
components/charts/indicator-toggles.tsx(102,19): error TS2551: Property 'color' does not exist on type 'IndicatorMetadata'. Did you mean 'colors'?
components/charts/indicator-toggles.tsx(105,48): error TS2551: Property 'color' does not exist on type 'IndicatorMetadata'. Did you mean 'colors'?
```

**Solution:**  
Replace `meta.color` with the correct property from your `IndicatorMetadata` type. It seems it should be `meta.colors` or an appropriate key in that object.

**Code correction example:**

```tsx
// Replace all instances of meta.color with meta.colors?.primary (or the correct color key)
{
  meta.colors?.primary && hasAccess && (
    <span
      className="h-3 w-3 rounded-full"
      style={{ backgroundColor: meta.colors.primary }}
    />
  );
}
```

Update this in:

- `components/charts/indicator-toggles.tsx`
- `frontend/components/charts/indicator-toggles.tsx` ([ref](https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/frontend/components/charts/indicator-toggles.tsx))

---

### 3. Unrecognized Indicator IDs

**Error:**

```
components/charts/pro-indicator-overlay.tsx(152,14): error TS2345: Argument of type '\"tema\"' is not assignable to parameter of type '...'
components/charts/pro-indicator-overlay.tsx(153,14): error TS2345: Argument of type '\"hrma\"' is not assignable...
components/charts/pro-indicator-overlay.tsx(154,14): error TS2345: Argument of type '\"smma\"' is not assignable...
```

**Solution:**  
Ensure that the `selectedIndicators` and render functions (`renderMA`) accept the correct indicator IDs. If `tema`, `hrma`, or `smma` are newly added, update your IndicatorId type to include them, or update the function signatures and usage to match valid values.

**Code correction example:**

```typescript
// Update IndicatorId to include 'tema', 'hrma', 'smma' if needed
type IndicatorId = 'keltner_channels' | 'zigzag' | 'fractal_diagonal' | ... | 'tema' | 'hrma' | 'smma';
```

Check `constants` or wherever `IndicatorId` is defined in your codebase.

Update in:

- `components/charts/pro-indicator-overlay.tsx`
- `frontend/components/charts/pro-indicator-overlay.tsx` ([ref](https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/frontend/components/charts/pro-indicator-overlay.tsx))

---

**Summary of fixes:**

- Update all zigzag transformation and structure to match the required type.
- Change `meta.color` to `meta.colors` or correct property in indicator toggles.
- Update the set of valid indicator IDs where referenced in pro indicator overlay code.

After making these changes, re-run your workflow to confirm the errors are resolved.
