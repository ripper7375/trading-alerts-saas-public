The CI job failed due to a TypeScript type error in archive/part6-flask-mt5/lib/api/mt5-transform.ts at line 117. The error indicates that the object assigned to the zigzag property is missing required fields (zigzag_peak, zigzag_bottom, ema_26) that are part of the ZigZagData type.

Solution:

Update the zigzag transformation in transformProIndicators to include the missing properties and correctly match the ZigZagData type. Assuming ZigZagData looks something like this:

```typescript
type ZigZagData = {
  peaks: { index: number; price: number; timestamp?: number }[];
  bottoms: { index: number; price: number; timestamp?: number }[];
  zigzag_peak: number[];
  zigzag_bottom: number[];
  ema_26: number[];
};
```

Adjust the code in transformProIndicators as follows:

```typescript
const zigzag = mt5Data.zigzag
  ? {
      peaks: Array.isArray(mt5Data.zigzag.peaks) ? mt5Data.zigzag.peaks : [],
      bottoms: Array.isArray(mt5Data.zigzag.bottoms)
        ? mt5Data.zigzag.bottoms
        : [],
      zigzag_peak: Array.isArray(mt5Data.zigzag.zigzag_peak)
        ? mt5Data.zigzag.zigzag_peak
        : [],
      zigzag_bottom: Array.isArray(mt5Data.zigzag.zigzag_bottom)
        ? mt5Data.zigzag.zigzag_bottom
        : [],
      ema_26: Array.isArray(mt5Data.zigzag.ema_26) ? mt5Data.zigzag.ema_26 : [],
    }
  : undefined;
```

Ensure all required ZigZagData fields are provided. If mt5Data.zigzag might be missing any of these arrays, use an empty array as a fallback as in the example above.

You can see and edit the problematic section here (ref 019ddbdddd9d387d0801cfc583707a04d9d7bcf3):
archive/part6-flask-mt5/lib/api/mt5-transform.ts

After applying this correction, the job should pass the type check.
