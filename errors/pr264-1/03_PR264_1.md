The build is failing because of a TypeScript type mismatch in archive/part6-flask-mt5/lib/api/mt5-transform.ts at line 117. The error says the returned zigzag object is missing required properties (zigzag_peak, zigzag_bottom, ema_26) expected by the ZigZagData type.

To fix this, ensure you include these properties in the zigzag assignment. Replace the zigzag transformation in the transformProIndicators function with the following:

```typescript
const zigzag = mt5Data.zigzag
  ? {
      peaks: Array.isArray(mt5Data.zigzag.peaks) ? mt5Data.zigzag.peaks : [],
      bottoms: Array.isArray(mt5Data.zigzag.bottoms)
        ? mt5Data.zigzag.bottoms
        : [],
      zigzag_peak: Array.isArray(mt5Data.zigzag.zigzag_peak)
        ? mt5Data.zigzag.zigzag_peak
        : [],
      zigzag_bottom: Array.isArray(mt5Data.zigzag.zigzag_bottom)
        ? mt5Data.zigzag.zigzag_bottom
        : [],
      ema_26: Array.isArray(mt5Data.zigzag.ema_26) ? mt5Data.zigzag.ema_26 : [],
    }
  : undefined;
```

Check that mt5Data.zigzag provides these properties or optionally default them to empty arrays as above. This will match the ZigZagData type and resolve the type error.

See the code causing the failure here (ref: 019ddbdddd9d387d0801cfc583707a04d9d7bcf3):  
https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/archive/part6-flask-mt5/lib/api/mt5-transform.ts
