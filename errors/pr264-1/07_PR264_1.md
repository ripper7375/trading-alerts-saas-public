The root cause of the failing job is a mismatch between your backend ("lib/") and frontend ("frontend/lib/") indicator tier definitions, tests, and metadata. The backend now defines all indicator access by new groupings (e.g., 'fractal_diagonal', 'moving_averages'), but the frontend/tests are using legacy flat indicator IDs (e.g., 'fractals', 'tema').

Here's a targeted plan to resolve the CI failures:

1. **Sync Indicator IDs and Tier Logic**  
   Update your backend and frontend so both reference the same indicator grouping IDs everywhere—use only those defined in `lib/tier/constants.ts` as the source of truth:
   - Replace outdated indicator IDs like 'fractals', 'trendlines', 'tema', etc., with the correct backend ones: 'fractal_diagonal', 'fractal_horizontal', 'moving_averages', etc.
   - Update all related tier-check logic and tests to match these groupings.

2. **Update Tests to Reflect New Backend Groupings**  
   **For example:**
   - Instead of `canAccessIndicator('FREE', 'fractals')`, use `canAccessIndicator('FREE', 'fractal_diagonal')`
   - Instead of expecting 'tema' to be PRO-only, expect the whole 'moving_averages' group to be PRO-only.
   - If an indicator ID no longer exists (e.g., plain 'tema'), update all tests, component props, and metadata to reference 'moving_averages' (and update display accordingly).

3. **Consolidate Metadata and Helper Definitions**
   - Ensure your frontend and backend use a single definitive list of valid indicators and their tiers. Prefer importing from `lib/tier/constants.ts` everywhere (possibly symlink or move this file if necessary).

**Code Fix Example for canAccessIndicator usage:**

Update from:

```ts
canAccessIndicator('FREE', 'fractals'); // OLD
canAccessIndicator('PRO', 'tema'); // OLD
```

To:

```ts
canAccessIndicator('FREE', 'fractal_diagonal'); // NEW
canAccessIndicator('PRO', 'moving_averages'); // NEW
```

**Summary**:  
Update indicator IDs and tier handling code and tests everywhere to use the backend's grouped indicator constants (`fractal_diagonal`, `moving_averages`, etc.), NOT the old flat IDs. This alignment will resolve all test expectation mismatches leading to CI failures.

Refer to:

- [lib/tier/constants.ts (ref 019ddbdddd9d387d0801cfc583707a04d9d7bcf3)](https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/lib/tier/constants.ts)
- [lib/tier/validator.ts (ref 019ddbdddd9d387d0801cfc583707a04d9d7bcf3)](https://github.com/ripper7375/trading-alerts-saas-public/blob/019ddbdddd9d387d0801cfc583707a04d9d7bcf3/lib/tier/validator.ts)

Implement these structural fixes, and your failing jobs will pass.
