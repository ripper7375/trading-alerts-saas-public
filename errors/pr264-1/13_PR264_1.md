The failing job is caused by the "TypeScript type check" step. The logs indicate that pnpm run type-check failed, resulting in: ❌ Type check: FAILED.

To fix this:

1. Run the type check locally to view detailed errors:
   ```
   pnpm run type-check
   ```
2. Address any TypeScript errors shown. Common issues include type mismatches, undefined variables, or missing types in your codebase.
3. Once the errors are resolved, commit and push your changes.

For reference, the type check step is defined in .github/workflows/security-checks.yml (ref: 019ddbdddd9d387d0801cfc583707a04d9d7bcf3):

```yaml
- name: Run TypeScript type check
  run: |
    echo "🔍 Running TypeScript type check..."
    pnpm run type-check
    echo "✅ No type errors found"
```

Focus on addressing the TypeScript errors, then re-run the workflow. If you need help with any specific error message, share it here for targeted advice.
