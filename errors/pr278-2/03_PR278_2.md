The failing job 60742426730 failed because at least one of the dependent jobs (unit-and-component-tests, integration-tests, or build-check) did not succeed, triggering the final “test-summary” job to exit with an error. The log contained the message: “❌ One or more test jobs failed”.

To resolve the issue, you need to:

1. Check the actual results of unit-and-component-tests, integration-tests, and build-check jobs to find which one(s) failed.
2. Address the root cause in the failing job:
   - For unit-and-component-tests: check test and lint scripts, make sure dependencies install correctly, and no TypeScript/ESLint/build errors occur.
   - For integration-tests: ensure the environment is set up, Prisma client is generated, and integration tests run successfully.
   - For build-check: verify your code successfully builds and the Next.js bundle remains below threshold as set in the workflow (see lines 126–150 in the tests.yml for thresholds).

Common fixes:

- If the bundle size is too large (build-check), optimize dependencies or static assets.
- If tests are failing, run pnpm run test:ci and pnpm run test:integration locally to identify and fix failures.
- If lint or type checks fail, fix errors in your codebase and re-run pnpm run lint or pnpm run type-check.

After making fixes, push your changes and let the workflow re-run.

You can review and modify the workflow logic in .github/workflows/tests.yml (ref: 791f2fa3), especially the jobs starting at line 10. If you need help with a specific step’s error output, provide its logs or failure details for targeted guidance.
