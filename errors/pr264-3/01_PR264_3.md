Here are the solutions for the failing job 60555756636:

---

### 1. "An update to PlanSelector inside a test was not wrapped in act(...)"

- **Problem:** Your PlanSelector component triggers updates (state or effects) during tests but these updates aren't wrapped in React's testing act() helper, leading to warnings/errors.
- **Solution:**
  - In your test files that mount or interact with PlanSelector (for example: **tests**/components/payments/PlanSelector.test.tsx), ensure updates (e.g., render, user events, async waits) are wrapped in `await act(async () => { ... })` if not already.
  - Example:
    ```tsx
    import { act } from 'react-dom/test-utils';
    // ...
    await act(async () => {
      render(<PlanSelector ... />);
    });
    ```

---

### 2. Test Failures Related to Tiers and Indicators

**Tests:**

- **tests**/lib/tier-validation.test.ts
- **tests**/lib/tier/validator.test.ts
- **tests**/components/charts/indicator-toggles.test.tsx
- **tests**/lib/tier/constants.test.ts

#### a) "momentum_candles" not allowed or missing for PRO tier

- **Problem:** The indicator "momentum_candles" is missing from the PRO tier's allowed indicators or from the list of PRO_ONLY_INDICATORS.

- **Solution:** Update your tier constants/validator to include "momentum_candles" for the PRO tier. Specifically, check and edit the files:
  - `lib/tier/constants.ts` – Ensure "momentum_candles" appears in PRO_ONLY_INDICATORS.
  - `lib/tier/validator.ts` – Make certain the PRO tier’s allowed indicators include "momentum_candles".
  - Example:
    ```ts
    // lib/tier/constants.ts
    export const PRO_ONLY_INDICATORS = [
      // ...other indicators,
      'momentum_candles',
    ];
    ```

#### b) Indicator Access for FREE tier and Indicator Validation

- **Problem:** The FREE tier is expected to have access to 2 basic indicators. Validate that the list of indicators for that tier matches test expectations. If not, update either the tier configuration or the test cases for alignment.

---

### 3. "should render PRO indicators" (indicator-toggles.test.tsx)

- **Problem:** The IndicatorToggles component does not render or recognize PRO indicators as expected in the tests.
- **Solution:** Review the rendering logic and props of IndicatorToggles. Ensure PRO indicators are passed in/available, especially after adding "momentum_candles" to relevant indicator lists.
- Example fix (if indicators are missing from props/config sent to the component):
  ```tsx
  // Check where IndicatorToggles is used in tests:
  render(<IndicatorToggles indicators={PRO_ONLY_INDICATORS} />);
  ```

---

### 4. Admin Users Test: "Admin users error: Error: Database error"

- **Problem:** The test for admin users (**tests**/api/admin.test.ts) is encountering a database error. This might be unrelated to your core code and instead due to missing/incorrect test mocks or environment setup.
- **Solution:** Ensure:
  - The database or any required mock/stub is properly set up before tests run.
  - If tests depend on external resources, use test doubles or mock those dependencies.
  - Example using Jest:
    ```ts
    jest.mock('your-database-module', () => ({
      // ...your mock implementation
    }));
    ```

---

If you need help with a specific test or file, or want sample code for your specific tier config structure, let me know! Here are relevant file locations for your fixes:

- [lib/tier/constants.ts](https://github.com/ripper7375/trading-alerts-saas-public/blob/79cad39ff5ac1bdb8759c6b7f32e72d3695ed2a2/lib/tier/constants.ts)
- [lib/tier/validator.ts](https://github.com/ripper7375/trading-alerts-saas-public/blob/79cad39ff5ac1bdb8759c6b7f32e72d3695ed2a2/lib/tier/validator.ts)
- [**tests**/components/payments/PlanSelector.test.tsx](https://github.com/ripper7375/trading-alerts-saas-public/blob/79cad39ff5ac1bdb8759c6b7f32e72d3695ed2a2/__tests__/components/payments/PlanSelector.test.tsx)

Focus on:

1. Wrapping stateful updates in `act()` in tests.
2. Making sure your indicators tier data is consistent with test expectations.
3. Mocking/stubbing database or API calls in admin user tests.

Let me know if you want to drill into any specific test or file!
