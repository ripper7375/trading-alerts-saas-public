The job failed due to several unit and integration test failures, all related to indicator tier access and constants. The root problem is a mismatch between back-end and front-end indicator definitions:

- The backend (lib/tier/constants.ts) now uses:
  - FREE_TIER_INDICATORS: ['fractal_diagonal', 'fractal_horizontal']
  - PRO_ONLY_INDICATORS: [
    'moving_averages',
    'body_momentum',
    'heiken_ashi',
    'keltner_channels',
    'support_resistance',
    'zigzag'
    ]
- The frontend (frontend/lib/tier/constants.ts) still uses old single-column indicator names such as 'fractals', 'trendlines', etc.

Your tests and logic expect indicator IDs like 'momentum_candles', 'tema', etc., while the new backend groups these columns (e.g., 'moving_averages' covers 'tema', 'hrma', 'smma').

**How to fix:**

1. **Align Frontend Constants:**  
   Update frontend/lib/tier/constants.ts and related frontend usage to use the new grouped indicator names:

   ```typescript
   // Change from:
   export const PRO_ONLY_INDICATORS = [
     'momentum_candles',
     'keltner_channels',
     'tema',
     'hrma',
     'smma',
     'zigzag',
   ];
   export const BASIC_INDICATORS = ['fractals', 'trendlines'];
   // To:
   export const PRO_ONLY_INDICATORS = [
     'moving_averages',
     'body_momentum',
     'heiken_ashi',
     'keltner_channels',
     'support_resistance',
     'zigzag',
   ];
   export const BASIC_INDICATORS = ['fractal_diagonal', 'fractal_horizontal'];
   ```

2. **Update All Indicator References:**  
   Find all uses of legacy indicator IDs in the frontend/backend/tests and replace them with the new IDs. For example, replace 'tema' checks/labels/references with 'moving_averages'.

3. **Fix Metadata Access:**  
   In places where indicator metadata is accessed (e.g., `INDICATOR_METADATA[tema]`), ensure you reference the correct group (`moving_averages`), and if needed, specify which sub-indicator you wish to use.

4. **Adjust Tests:**  
   Modify tests to refer to the new indicator IDs/groups.
   - **tests**/lib/tier-validation.test.ts expects 'fractals', 'trendlines' as basic indicators, which should now be 'fractal_diagonal', 'fractal_horizontal'.
   - PRO indicators in tests should use only valid group IDs.

5. **UI Display:**  
   Update UI rendering logic to reflect the new structure; if the UI should show individual sub-indicators, map from groups to columns as in the new metadata.

**Example code adjustment (frontend/lib/tier/constants.ts):**

```typescript
export const BASIC_INDICATORS = ['fractal_diagonal', 'fractal_horizontal'];
export const PRO_ONLY_INDICATORS = [
  'moving_averages', // [tema, hrma, smma]
  'body_momentum', // [z_score_of_body_size, candle_classification]
  'heiken_ashi', // [ha_open, ...]
  'keltner_channels',
  'support_resistance',
  'zigzag',
];
```

**Summary:**  
Unify the indicator IDs and structure across back-end, front-end, and tests per the new schema ([ref](https://github.com/ripper7375/trading-alerts-saas-public/blob/79cad39ff5ac1bdb8759c6b7f32e72d3695ed2a2/lib/tier/constants.ts)). Update all access logic, test assertions, and UI displays to use the grouped indicator names instead of the old single-column names.

Once done, all tier validation and access tests should pass.
