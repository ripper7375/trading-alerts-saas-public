The failure in job 60555816581 is due to the step that runs exit 1 when one or more test jobs fails. According to the logs and workflow definition, this job ("test-summary") gathers results from the following jobs: unit-and-component-tests, integration-tests, and build-check. At least one of these jobs failed or was skipped, causing the summary to fail with exit code 1.

To resolve the issue:

1. Investigate why the required jobs (unit-and-component-tests, integration-tests, build-check) are failing or being skipped. Look at the output and logs of these jobs to diagnose the exact cause.
2. If test jobs are intentionally skipped (e.g., conditional logic, missing triggers, or matrix exclusions), adjust the workflow to handle or acknowledge skipped jobs without marking the workflow as a failure.

Example solution: To allow the summary to succeed even when some jobs are skipped (not failed), modify the condition on the "Fail if any test failed" step in .github/workflows/tests.yml so it only fails on actual failures:

```yaml
170|   if: |
171|     needs.unit-and-component-tests.result == 'failure' ||
172|     needs.integration-tests.result == 'failure' ||
173|     needs.build-check.result == 'failure'
```

Ensure this does not treat 'skipped' as failure, only 'failure'. If required, adjust the logic to:

```yaml
if: |
  needs.unit-and-component-tests.result == 'failure' ||
  needs.integration-tests.result == 'failure' ||
  needs.build-check.result == 'failure'
```

Next, run the workflow and ensure that only true failures cause a non-zero exit, while skipped jobs are handled gracefully.

Review and fix any failures or misconfigurations in the earlier jobs to allow the summary to pass. For more details, see the current workflow: .github/workflows/tests.yml at ref 79cad39ff5ac1bdb8759c6b7f32e72d3695ed2a2.
