I'll analyze the failing job logs to identify the issue and provide a solution. I can see the job is failing with a common issue across multiple test files. The main error is:

**`Cannot find module '../../lib/api' from '__tests__/integration/api-client-workflow.test.ts'`**

This is a missing module import error. Let me examine the failing test files to understand the structure: ## Solution

The tests are failing because the module `@/lib/api` cannot be found. All three test files are attempting to dynamically import the API client:

```typescript
const { api } = await import('@/lib/api');
```

### Root Cause

The `lib/api/index.ts` file is either:

1. Missing entirely
2. Not exporting the `api` object correctly
3. Has incorrect exports

### Fix Required

Create or update `lib/api/index.ts` to properly export the API clients:

```typescript
// lib/api/index.ts
import { StackAClient } from './stack-a-client';
import { StackBClient } from './stack-b-client';

export const api = {
  stackA: new StackAClient(),
  stackB: new StackBClient(),
};

export { StackAClient, StackBClient };
```

### Verify Supporting Files Exist

Ensure these client class files exist:

- `lib/api/stack-a-client. ts` - Should export `StackAClient` class
- `lib/api/stack-b-client.ts` - Should export `StackBClient` class

### Alternative: Check TypeScript Path Aliases

If the index file exists, verify your `tsconfig.json` has the path alias configured:

```json
{
  "compilerOptions": {
    "paths": {
      "@/*": ["./*"]
    }
  }
}
```

And ensure `jest.config.js` has matching module name mapping:

```javascript
moduleNameMapper: {
  '^@/(.*)$': '<rootDir>/$1',
}
```

The tests expect `api. stackA` and `api.stackB` to be available with methods like `getAlerts()`, `getUser()`, `getLeaderBoard()`, etc. Once the index file properly exports these clients, all 41 failing tests should pass.
