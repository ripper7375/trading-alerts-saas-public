The main failures in job 60827498067 are caused by:

1. **Missing Module Error:**  
   Several tests, such as in `__tests__/lib/api/stack-a-client.test.ts`, `stack-b-client.test.ts`, and `integration/api-client-workflow.test.ts`, are trying to import `@/lib/api` but are unable to resolve the module:
   ```
   Cannot find module '../../lib/api' from '__tests__/integration/api-client-workflow.test.ts'
   ```
   **Solution:**
   - Ensure that the `@/lib/api` alias is correctly configured in your project (check your TypeScript `paths` in `tsconfig.json` and your Jest config/moduleNameMapper).
   - If you moved or renamed the file, restore or update the path accordingly.
   - The file should exist at `lib/api/index.ts` or similar, matching the test import.
2. **Test Expectations for Stack B:**  
   Tests using StackBClient (see the [test file](https://github.com/ripper7375/trading-alerts-saas-public/blob/92490f70941cd69e047390ad0b6665fb1672dc18/__tests__/lib/api/stack-b-client.test.ts)) are expected to throw 404 errors because Stack B is not deployed yet. This is intentional per test comments, so no change required unless Stack B is now available.

3. **React Testing Warnings:**  
   Many tests for components like `PlanSelector` output:
   ```
   An update to PlanSelector inside a test was not wrapped in act(...).
   ```
   **Solution:**
   - Wrap any state-updating or async component updates in `await act(async () => { ... })` in your React tests.  
     For example:
   ```typescript
   import { act } from 'react-dom/test-utils';
   await act(async () => {
     // trigger your updates here
   });
   ```

   - This will clear the React warning, ensuring your test simulates updates properly.

**Summary of action items:**

- Restore or correctly map `@/lib/api` for all relevant test imports.
- Update your React tests to use `act(...)` for all state updates.
- No action needed for Stack B 404 errors unless actual endpoints are now available.

These changes should resolve the core failures and warnings reported by your CI job.
