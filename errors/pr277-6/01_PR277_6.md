The failing job 60729054412 results from Redis cache invalidation errors and database timeouts during your test runs:

**1. Redis Errors:**
In `lib/cache/redis.ts` ([source](https://github.com/ripper7375/trading-alerts-saas-public/blob/2c99c6a0fa8ff05c4ef3a4acc8457d748b83bb67/lib/cache/redis.ts)), the logs show `Connection is closed` errors during cache invalidation (see function `invalidatePattern`). This likely means the Redis connection is being closed/disposed before all cache actions complete, or there's an issue with Redis client lifecycle management.

**Solution:**

- Ensure the Redis client instance remains alive for the duration of all test runs, especially when running `clearAllCache` in test setup/teardown. Gracefully handle connection errors and consider waiting for the Redis connection to be ready before making calls.
- Add a check before invalidation attempts:

```typescript
if (!redis.status || redis.status === 'end') {
  await redis.connect();
}
```

or ensure your test teardown doesn't prematurely close Redis connections.

- If using ioredis, verify all Redis commands use a live connection and that tests do not globally disconnect/re-instantiate the client.

**2. Database Timeouts:**
Your logs show `Query timeout exceeded` and related Postgres connection timeouts, particularly when fetching indicator data (`getIndicatorDataFromDb` in `lib/cache/indicator-cache.ts`).

**Solution:**

- Increase the timeout settings on your database connection pool for tests.
- Ensure that the Postgres database is up and accessible in the test environment.
- If this is intermittent, introduce retry logic or increase test timeouts:

```typescript
// Example: with pg library
const client = new Client({ /*...*/, statement_timeout: 10000 }); // 10 seconds
```

**3. Test Coverage/Setup:**
Keep the cache and database mock/real environments isolated and predictable for tests (see `__tests__/api/indicators.test.ts`):

- Ensure `clearAllCache` only runs after Redis is ready and handle any connection failures gracefully.
- Avoid running integration tests if Redis or the database are unavailable.

**Summary of code suggestions:**

- Check Redis connection is live before running cache ops in tests.
- Add retry/connection handling to Redis and database calls in both runtime and test code.
- Increase DB and test timeouts as needed.

This should resolve the core failures seen in job 60729054412.
