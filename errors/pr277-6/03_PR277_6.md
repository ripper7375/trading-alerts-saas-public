The primary cause of the failing job is missing modules required by integration tests in frontend/**tests**/integration/stack-a.test.ts, stack-b.test.ts, and multi-stack.test.ts. The errors are:

- Resolver.\_throwModNotFoundError (jest-resolve): This happens when an imported module/file cannot be found.
- ReferenceError: clearImmediate is not defined and TypeError: markResourceTiming is not a function in undici/jsdom usage: This indicates some Node.js globals (like clearImmediate) and certain web platform APIs are not available/shimmed in your test environment.

**Steps to Fix**

1. **Missing Modules in Integration Tests**
   - Check the import statements at the top of:
     - frontend/**tests**/integration/stack-a.test.ts (line 20)
     - frontend/**tests**/integration/stack-b.test.ts (line 20)
     - frontend/**tests**/integration/multi-stack.test.ts (line 18)
   - Make sure all imported modules/files exist and are correctly referenced (case sensitive, and correct relative/absolute paths).
   - If a module’s import path is wrong, correct it.
   - If the file/module doesn’t exist, add it or modify the test to import from an existing file.

2. **Provide Node.js Globals (clearImmediate) for JSDOM/Undici**
   - In your test setup file (e.g. jest.setup.js or setupFilesAfterEnv entry), add:
     ```js
     // jest.setup.js
     if (typeof global.clearImmediate === 'undefined') {
       global.clearImmediate = clearTimeout; // fallback if tests require it, not perfect but prevents crashes
     }
     ```
   - Add this setup file in your Jest config:
     ```json
     "setupFilesAfterEnv": ["<rootDir>/jest.setup.js"]
     ```
   - If your tests depend on full Node.js timers, consider adding the node:test-environment preset or use node as a testEnvironment instead of jsdom (in jest.config.js):
     ```js
     module.exports = {
       testEnvironment: 'node',
     };
     ```
     Or, if you need browser APIs, continue using 'jsdom', but ensure polyfills/shims are present.

3. **Shim markResourceTiming if Needed**
   - If a dependency calls window.performance.markResourceTiming (which may not exist in jsdom), add a polyfill in your setup:
     ```js
     if (
       typeof window !== 'undefined' &&
       (!window.performance ||
         typeof window.performance.markResourceTiming !== 'function')
     ) {
       window.performance = window.performance || {};
       window.performance.markResourceTiming = () => {};
     }
     ```

**Code Example for test setup:**

```js
// jest.setup.js
if (typeof global.clearImmediate === 'undefined') {
  global.clearImmediate = clearTimeout;
}
if (
  typeof window !== 'undefined' &&
  (!window.performance ||
    typeof window.performance.markResourceTiming !== 'function')
) {
  window.performance = window.performance || {};
  window.performance.markResourceTiming = () => {};
}
```

**Summary:**

- Fix broken imports in the integration test files.
- Add polyfills for missing Node.js/web APIs in your Jest setup.
- Verify the dependencies (undici/jsdom) don’t require additional setup based on recent breaking changes.

Apply these fixes, re-run your tests, and the job should pass. If you need help with specific import errors, paste those lines or share the relevant import statements.
