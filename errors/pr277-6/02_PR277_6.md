The logs show the job failed with the error "❌ One or more test jobs failed" due to at least one of the dependent jobs (unit-and-component-tests, integration-tests, or build-check) failing. To resolve this:

1. Identify which specific job failed. In the workflow, the test-summary job checks the status of all three jobs and fails if any did not succeed.
2. Check the logs for unit-and-component-tests, integration-tests, and build-check jobs to find the actual failure (e.g., test, lint, build).

Common solutions based on this workflow:

- If unit tests fail: Run pnpm run test:ci and fix any failing tests.
- If lint fails: Run pnpm run lint and address linting errors.
- If type-checking fails: Run pnpm run type-check and resolve TypeScript errors.
- If the build-check fails, especially from a bundle size check (see the build-check job at lines 95-153 in .github/workflows/tests.yml): Reduce the size of your Next.js bundle to stay under the configured thresholds (ideally <340MB, or at minimum <450MB).
- If integration tests fail: Fix test failures from pnpm run test:integration.

After correcting the code or tests as indicated by the failing logs, push your changes and re-run the workflow.

You can view and edit the workflow file here (ref 2c99c6a0fa8ff05c4ef3a4acc8457d748b83bb67): .github/workflows/tests.yml.
