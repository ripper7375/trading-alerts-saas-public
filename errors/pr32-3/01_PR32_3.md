The failure is caused by a type error in lib/disbursement/cron/disbursement-processor.ts at line 213:

Type error: Type 'null' is not assignable to type 'string | NestedStringFilter<"AffiliateRiseAccount"> | undefined'.

Your code:

```typescript
211| const accounts = await this.prisma.affiliateRiseAccount.findMany({
212|   where: {
213|     riseId: { not: null },
214|   },
215| });
```

The Prisma schema expects riseId to be either a string, a filter, or undefined—not null. To fix this, update the filter to use 'not: undefined' for optional fields or use 'not: ""' if riseId is never actually null but may be an empty string. However, Prisma's type system expects you to use 'not: undefined' if you're filtering out missing values, but if riseId is nullable in your schema use:

```typescript
riseId: {
  not: undefined;
}
```

Or, if you want all accounts where riseId is not null:

```typescript
riseId: {
  not: null as any;
}
```

But the most robust solution with Prisma is:

```typescript
riseId: { not: undefined, not: null }
```

Final recommendation: update line 213 as follows:

```typescript
riseId: {
  not: null as any;
}
```

or more concisely if TypeScript allows:

```typescript
riseId: {
  not: null as unknown as string;
}
```

Or you may use:

```typescript
riseId: {
  not: undefined;
}
```

Example:

```typescript
const accounts = await this.prisma.affiliateRiseAccount.findMany({
  where: {
    riseId: { not: null as any },
  },
});
```

See the code here.
