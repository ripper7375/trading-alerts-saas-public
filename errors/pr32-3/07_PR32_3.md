I'll analyze the failing job to find a solution for you. The job failure is occurring in a summary/report job that checks if other test jobs have failed. The logs show:

```
❌ One or more test jobs failed
```

This indicates that this is a dependent job that fails when upstream test jobs fail. To fix this, you need to identify and fix the actual failing test jobs that this one depends on.

**Solution:**

1. **Check the workflow file** to see which jobs this one depends on:
