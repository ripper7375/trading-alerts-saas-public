I'll analyze the failing job to find a solution for you. ## Solution

The build is failing due to a TypeScript type error on **line 213** in `lib/disbursement/cron/disbursement-processor.ts`. The issue is that `null` cannot be assigned to the `riseId` filter, which expects `string | NestedStringFilter<"AffiliateRiseAccount"> | undefined`.

### The Problem

On line 213, you're using:

```typescript
riseId: { not: null },
```

TypeScript's strict null checks don't allow `null` in this context. Prisma expects `undefined` when filtering for non-null values.

### The Fix

Replace line 213 with:

```typescript
riseId: { not: undefined },
```

Or alternatively, use Prisma's more explicit syntax:

```typescript
riseId: { isSet: true },
```

### Complete Code Change

Update lines 211-215 in [`lib/disbursement/cron/disbursement-processor.ts`](https://github.com/ripper7375/trading-alerts-saas-public/blob/cfb83ea11a163d3eb52c2e0c3bdc85582620a90e/lib/disbursement/cron/disbursement-processor.ts#L211-L215):

```typescript
const accounts = await this.prisma.affiliateRiseAccount.findMany({
  where: {
    riseId: { not: undefined },
  },
});
```

This change will resolve the TypeScript compilation error and allow your build to succeed.
