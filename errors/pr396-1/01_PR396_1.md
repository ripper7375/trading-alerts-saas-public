The job failed due to a Prisma migration issue: the error is ERROR: generation expression is not immutable. This means your PostgreSQL migration attempted to add a generated column whose expression isn’t immutable. PostgreSQL requires generated expressions to be immutable (no dependency on changing data/functions).

**How to fix:**

- Update the migration file `20260214000000_rag_dual_memory` (in `prisma/migrations`).
- Locate the column with a generated expression.
- Ensure the expression uses only immutable functions and operators.
- If you used functions like `now()`, `random()`, or any function that depends on external state, change the expression to use simple arithmetic, string manipulations, or other immutable operations.

**Example fix:**
If your migration has:

```sql
ALTER TABLE example ADD COLUMN computed_value INTEGER GENERATED ALWAYS AS (random()) STORED;
```

Replace with:

```sql
ALTER TABLE example ADD COLUMN computed_value INTEGER GENERATED ALWAYS AS (col1 * col2) STORED;
```

**Action steps:**

1. Find the offending generated column in the migration.
2. Rewrite the expression to meet immutability requirements.
3. Redeploy the migration.

If you share the exact failing section from `prisma/migrations/20260214000000_rag_dual_memory`, I can provide a code fix for your case.
