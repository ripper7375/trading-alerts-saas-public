The job is failing due to issues with the Prisma Client integration in your TypeScript code:

1. **UserSession is not exported from @prisma/client**
   - In `lib/auth/session-tracker.ts`, you have:
     ```typescript
     import type { UserSession } from '@prisma/client';
     ```
     Fix: Ensure your Prisma schema defines the `UserSession` model and regenerate Prisma types with `npx prisma generate`. If the model is named differently in your schema, update the import accordingly.

2. **Property 'userSession' does not exist on type 'PrismaClient'**
   - You reference `prisma.userSession` throughout `lib/auth/session-tracker.ts` (e.g., `prisma.userSession.upsert`).  
     Fix: This property will only exist if your Prisma schema has a `model UserSession`. If the table/model is named differently, change all `prisma.userSession` calls to match your schema’s model name.

3. **Unused req parameter in route**
   - In `app/api/user/sessions/[id]/route.ts`, the `req` parameter in the `DELETE` function is not used.  
     Fix: Remove or use the `req` parameter (line 26). If unused, change the function signature to:
     ```typescript
     export async function DELETE(_req: NextRequest, context: RouteContext) { ... }
     ```

4. **Parameter 't' implicitly has an 'any' type**
   - In `lib/auth/session-tracker.ts` (line 293), you filter and use `t` in:
     ```typescript
     .filter((t): t is string => t !== null && t !== undefined);
     ```
     Fix: Add an explicit type annotation to the parameter, as shown above, which should resolve the TS7006 error.

**Summary of Code Changes Needed:**

- Check your Prisma schema and confirm the existence/spelling of the `UserSession` model.
- Regenerate Prisma client types if you modify the schema.
- Replace all `userSession` references with your actual model name, if different.
- Fix unused parameter and type annotation issues.

**References:**

- [lib/auth/session-tracker.ts (ref: 9878cfc123af292ceeaf96b5347fbd1c9c58c111)](https://github.com/ripper7375/trading-alerts-saas-public/blob/9878cfc123af292ceeaf96b5347fbd1c9c58c111/lib/auth/session-tracker.ts)
- [app/api/user/sessions/[id]/route.ts (ref: 9878cfc123af292ceeaf96b5347fbd1c9c58c111)](https://github.com/ripper7375/trading-alerts-saas-public/blob/9878cfc123af292ceeaf96b5347fbd1c9c58c111/app/api/user/sessions/%5Bid%5D/route.ts)

Apply these fixes and the failing job should resolve.
