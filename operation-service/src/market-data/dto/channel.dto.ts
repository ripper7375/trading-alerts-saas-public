/**
 * Market-data channel query DTO (Session 4B-12). Type-only re-export
 * giving the controller/service signature a name, matching the
 * Drawings/Notifications precedent.
 *
 * @module market-data/dto/channel.dto
 */
export type { ChannelQueryInput as ChannelQueryDto } from '../market-data.schemas';
