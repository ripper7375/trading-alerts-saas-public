# Nixpacks configuration for Railway deployment
# Explicitly defines cache directories to prevent Nixpacks from
# auto-scanning the repo (which finds seed-code dirs with spaces in names)

[phases.build]
cacheDirectories = [".next/cache", "node_modules/.cache"]
